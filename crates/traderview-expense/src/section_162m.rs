//! IRC § 162(m) — $1 million deduction limit on
//! compensation paid to "covered employees" of
//! publicly held corporations. Direct companion to
//! section_280g (Golden Parachute — iter 444),
//! section_409a (NQDC), section_422 (ISO — iter 438),
//! section_423 (ESPP — iter 440).
//!
//! Trader-critical because traders at venture-backed
//! startups that go public, or who later become C-
//! suite executives at public companies, encounter
//! § 162(m) on every compensation package: cash bonus,
//! deferred compensation, equity awards, severance.
//! The "once covered always covered" rule under TCJA
//! 2017 amendments means the deduction limitation
//! follows the executive FOREVER, even after departure
//! and after death (paid to estate).
//!
//! § 162(m)(1) DENIES employer deduction for
//! "applicable employee remuneration" of "covered
//! employees" of a "publicly held corporation" that
//! EXCEEDS $1,000,000 for the taxable year.
//!
//! Companion to section_280g (Golden Parachute 3×
//! base cliff for change-in-control), section_409a
//! (NQDC compliance), section_422 (ISO), section_423
//! (ESPP), section_280e (deduction disallowance for
//! controlled-substance trafficking — different
//! disallowance regime).
//!
//! **§ 162(m)(2) PUBLICLY HELD CORPORATION definition**
//! includes:
//! 1. Any corporation that has SECURITIES REGISTERED
//!    UNDER § 12 OF THE SECURITIES EXCHANGE ACT OF
//!    1934 (NYSE/NASDAQ/AMEX listing);
//! 2. SECURITIES AND EXCHANGE ACT REPORTING
//!    requirements (§ 15(d) reporting due to public
//!    offering even without exchange listing);
//! 3. TCJA 2017 expansion (effective 2018) added
//!    foreign private issuers + any corporation
//!    required to file SEC reports.
//!
//! **§ 162(m)(3) COVERED EMPLOYEE definition**:
//! 1. CEO at any time during taxable year;
//! 2. CFO at any time during taxable year;
//! 3. Top three (3) other most highly compensated
//!    officers for the year;
//! 4. ANY individual who was a covered employee for
//!    any TAXABLE YEAR BEGINNING AFTER 2016 (the
//!    "ONCE COVERED, ALWAYS COVERED" rule);
//! 5. AMERICAN RESCUE PLAN ACT OF 2021 (ARPA, Pub.
//!    L. 117-2 § 9708) — for taxable years beginning
//!    after December 31, 2026, adds the next FIVE
//!    most highly compensated employees (the "ARPA
//!    FIVE"), bringing total to 10 covered employees;
//!    ARPA FIVE retested ANNUALLY (no once-covered-
//!    always-covered rule); ARPA FIVE includes high-
//!    paid employees who are NOT officers.
//!
//! **§ 162(m)(4) APPLICABLE EMPLOYEE REMUNERATION
//! definition** — aggregate amount allowable as
//! deduction during taxable year for compensation
//! services performed; INCLUDES:
//! 1. Cash compensation;
//! 2. Equity compensation (RSU vesting, stock option
//!    exercise spread for NQSOs);
//! 3. Bonus payments;
//! 4. Deferred compensation when paid;
//! 5. Severance payments;
//! 6. Post-termination payments (still subject under
//!    once-covered-always-covered rule).
//!
//! **TCJA 2017 changes (Pub. L. 115-97, effective
//! taxable years beginning after December 31, 2017)**:
//! 1. ELIMINATED performance-based compensation
//!    exception (previously § 162(m)(4)(C) excluded
//!    qualified performance-based compensation from
//!    the $1M cap);
//! 2. ELIMINATED commission exception;
//! 3. Once-covered-always-covered rule (after 2016,
//!    covered employee status survives departure +
//!    death);
//! 4. Expanded publicly held corporation definition
//!    to include foreign private issuers + § 15(d)
//!    reporting companies;
//! 5. TRANSITION RULE — pre-TCJA written binding
//!    contracts in effect on November 2, 2017 and not
//!    materially modified preserved performance-based
//!    exception.
//!
//! **ARPA 2021 changes (Pub. L. 117-2 § 9708)**:
//! 1. Effective taxable years beginning AFTER
//!    December 31, 2026 (for 2027 calendar-year
//!    filers);
//! 2. Adds NEXT FIVE most highly compensated
//!    employees beyond CEO + CFO + top three (the
//!    "ARPA FIVE");
//! 3. ARPA FIVE includes employees who are NOT
//!    officers;
//! 4. ARPA FIVE RETESTED ANNUALLY (no permanent
//!    once-covered status);
//! 5. Brings total covered employees from 5 to 10.
//!
//! **§ 162(m) interaction with § 280G** — both regimes
//! deny employer deduction for executive compensation
//! but DIFFERENT TRIGGERS:
//! - § 162(m) — $1M cap on annual remuneration to
//!   covered employees of public corporation;
//! - § 280G — change-in-control parachute payment
//!   exceeding 3× base amount denies deduction on
//!   entire excess over 1× base PLUS § 4999 20%
//!   recipient excise tax.
//!
//! Both apply simultaneously; same payment can be
//! disallowed under BOTH if it exceeds $1M cap AND
//! is change-in-control parachute.
//!
//! **Trader-critical fact patterns**:
//! 1. Trader at NASDAQ-listed company CEO total
//!    compensation $5M cash + $3M RSU vesting = $8M;
//!    $1M deductible; $7M EXCESS NON-DEDUCTIBLE to
//!    employer; trader still receives full $8M
//!    pre-tax; no § 4999 trigger absent CIC.
//! 2. Trader departs public company in 2023; receives
//!    deferred bonus of $2M in 2026; STILL covered
//!    employee under ONCE-COVERED-ALWAYS-COVERED rule;
//!    $1M deductible, $1M non-deductible to employer.
//! 3. Post-2027 ARPA FIVE — trader is sixth-highest-
//!    paid employee at public company (not officer);
//!    $1.5M compensation; subject to § 162(m) cap;
//!    $500K non-deductible to employer; retested
//!    annually so 2028 status independent.
//! 4. Pre-TCJA grandfather — performance-based RSU
//!    grant under binding contract dated October 15,
//!    2017 and not materially modified; preserves
//!    pre-TCJA performance-based compensation
//!    exception (excluded from $1M cap).
//! 5. § 162(m) interacts with § 280G — trader's
//!    CIC parachute payment $4.5M on $1M base
//!    triggers § 280G excess parachute of $3.5M
//!    over 1× base PLUS § 162(m) disallowance on
//!    annual remuneration over $1M; same dollars
//!    can be disallowed under both regimes
//!    simultaneously.
//!
//! Citations: 26 USC § 162(m)(1)-(6); Pub. L. 115-97
//! § 13601 (Tax Cuts and Jobs Act of 2017); Pub. L.
//! 117-2 § 9708 (American Rescue Plan Act of 2021);
//! Treas. Reg. § 1.162-27 (current — TCJA-implementing
//! regulations); Treas. Reg. § 1.162-33 (proposed
//! January 16, 2025 — ARPA implementation); IRS Pub.
//! 6014 (§ 162(m) Audit Technique Guide); Notice
//! 2018-68 (TCJA transition rule guidance); Rev.
//! Proc. 2024-19.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, Deserialize, Serialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum CoveredEmployeeType {
    /// CEO at any time during taxable year.
    Ceo,
    /// CFO at any time during taxable year.
    Cfo,
    /// Top 3 other most highly compensated officers.
    TopThreeOfficer,
    /// Once-covered-always-covered (former CEO/CFO/top
    /// 3 from prior year).
    FormerCoveredEmployee,
    /// ARPA FIVE — next 5 highest compensated (post-
    /// 2026 effective).
    ArpaFive,
    /// Not a covered employee.
    NotCovered,
}

#[derive(Debug, Clone, Deserialize)]
pub struct Section162mInput {
    pub covered_employee_type: CoveredEmployeeType,
    /// Whether corporation has SEC § 12 registration OR
    /// § 15(d) reporting requirement (TCJA-expanded
    /// publicly held corporation definition).
    pub publicly_held_corporation: bool,
    /// Total applicable employee remuneration for
    /// taxable year in cents.
    pub total_remuneration_cents: u64,
    /// Taxable year (post-2026 enables ARPA FIVE
    /// expansion).
    pub taxable_year: u32,
    /// Whether compensation arose from pre-TCJA
    /// written binding contract in effect on
    /// November 2, 2017 and not materially modified
    /// (grandfather rule).
    pub pre_tcja_grandfathered_contract: bool,
    /// Whether grandfathered amount is qualified
    /// performance-based compensation under former
    /// § 162(m)(4)(C) (pre-TCJA exception).
    pub qualified_performance_based_pre_tcja: bool,
    /// Whether trader is subject to § 280G excess
    /// parachute payment (signals dual-regime
    /// disallowance).
    pub also_section_280g_excess_parachute: bool,
}

#[derive(Debug, Clone, Serialize, PartialEq, Eq)]
pub struct Section162mResult {
    pub is_publicly_held_corporation: bool,
    pub is_covered_employee: bool,
    pub one_million_cap_cents: u64,
    pub deductible_compensation_cents: u64,
    pub non_deductible_compensation_cents: u64,
    pub grandfather_exception_applies: bool,
    pub arpa_expansion_applies: bool,
    pub dual_regime_disallowance: bool,
    pub failure_reasons: Vec<String>,
    pub citation: &'static str,
    pub notes: Vec<String>,
}

pub fn check(input: &Section162mInput) -> Section162mResult {
    let mut failure_reasons: Vec<String> = Vec::new();

    let is_publicly_held_corporation = input.publicly_held_corporation;

    let arpa_expansion_applies = input.taxable_year >= 2027;

    let is_covered_employee = match input.covered_employee_type {
        CoveredEmployeeType::NotCovered => false,
        CoveredEmployeeType::ArpaFive => arpa_expansion_applies && is_publicly_held_corporation,
        _ => is_publicly_held_corporation,
    };

    let one_million_cap_cents: u64 = 100_000_000;

    let grandfather_exception_applies =
        input.pre_tcja_grandfathered_contract && input.qualified_performance_based_pre_tcja;

    let (deductible_compensation_cents, non_deductible_compensation_cents) = if !is_covered_employee
        || grandfather_exception_applies
        || input.total_remuneration_cents <= one_million_cap_cents
    {
        (input.total_remuneration_cents, 0)
    } else {
        (
            one_million_cap_cents,
            input.total_remuneration_cents - one_million_cap_cents,
        )
    };

    let dual_regime_disallowance =
        input.also_section_280g_excess_parachute && non_deductible_compensation_cents > 0;

    if !is_publicly_held_corporation {
        failure_reasons.push(
            "26 USC § 162(m)(2) — corporation is NOT a publicly held corporation (no SEC § 12 registration nor § 15(d) reporting requirement); § 162(m) inapplicable; compensation fully deductible".to_string(),
        );
    }

    if !is_covered_employee && is_publicly_held_corporation {
        let label = match input.covered_employee_type {
            CoveredEmployeeType::NotCovered => "individual is NOT among CEO + CFO + top 3 most highly compensated officers + former covered employee from prior year + ARPA FIVE (post-2026)",
            CoveredEmployeeType::ArpaFive => "ARPA FIVE covered status NOT YET EFFECTIVE (taxable year < 2027); Pub. L. 117-2 § 9708 effective taxable years beginning after December 31, 2026",
            _ => "not covered",
        };
        failure_reasons.push(format!(
            "26 USC § 162(m)(3) — {}; § 162(m) cap inapplicable; compensation fully deductible",
            label
        ));
    }

    if is_covered_employee && grandfather_exception_applies {
        failure_reasons.push(
            "PRE-TCJA TRANSITION RULE — compensation paid under written binding contract IN EFFECT ON NOVEMBER 2, 2017 and NOT MATERIALLY MODIFIED preserves former § 162(m)(4)(C) qualified performance-based compensation exception; compensation EXCLUDED from $1M cap; fully deductible to employer".to_string(),
        );
    }

    if is_covered_employee
        && !grandfather_exception_applies
        && non_deductible_compensation_cents > 0
    {
        failure_reasons.push(format!(
            "26 USC § 162(m)(1) — applicable employee remuneration {} cents EXCEEDS $1,000,000 ({} cents) cap; ${} cents NON-DEDUCTIBLE to employer; covered employee remains entitled to full pre-tax compensation",
            input.total_remuneration_cents,
            one_million_cap_cents,
            non_deductible_compensation_cents
        ));

        let cet_label = match input.covered_employee_type {
            CoveredEmployeeType::Ceo => "§ 162(m)(3)(A) CEO",
            CoveredEmployeeType::Cfo => "§ 162(m)(3)(A) CFO",
            CoveredEmployeeType::TopThreeOfficer => "§ 162(m)(3)(A) top 3 most highly compensated officer",
            CoveredEmployeeType::FormerCoveredEmployee => "§ 162(m)(3)(C) ONCE-COVERED-ALWAYS-COVERED — was covered employee for any taxable year beginning after 2016; status survives departure and death (TCJA 2017 Pub. L. 115-97 § 13601)",
            CoveredEmployeeType::ArpaFive => "ARPA FIVE (Pub. L. 117-2 § 9708 effective taxable years beginning after December 31, 2026) — among next 5 most highly compensated employees (NOT necessarily officers); retested ANNUALLY (no permanent status)",
            CoveredEmployeeType::NotCovered => "(none)",
        };
        failure_reasons.push(format!("Covered employee category: {}", cet_label));
    }

    if dual_regime_disallowance {
        failure_reasons.push(
            "26 USC § 162(m) plus § 280G DUAL-REGIME DISALLOWANCE — same compensation can be disallowed under BOTH § 162(m) $1M cap AND § 280G change-in-control 3× base parachute payment cliff; separately also subject to § 4999 20% recipient excise tax; aggregate cost to employer + executive substantially exceeds single-regime calculation".to_string(),
        );
    }

    if arpa_expansion_applies && is_publicly_held_corporation {
        failure_reasons.push(
            "AMERICAN RESCUE PLAN ACT OF 2021 (Pub. L. 117-2 § 9708) — for taxable years beginning AFTER DECEMBER 31, 2026, adds NEXT FIVE most highly compensated employees (the ARPA FIVE); brings total covered employees from 5 to 10; ARPA FIVE includes employees who are NOT officers; ARPA FIVE retested ANNUALLY (no once-covered-always-covered rule for these five)".to_string(),
        );
    }

    let notes: Vec<String> = vec![
        "26 USC § 162(m)(1) — DENIES employer DEDUCTION for applicable employee remuneration of covered employees of publicly held corporation that EXCEEDS $1,000,000 for taxable year; complements § 280G change-in-control regime + § 280E controlled-substance disallowance".to_string(),
        "26 USC § 162(m)(2) PUBLICLY HELD CORPORATION definition — (1) securities registered under § 12 of Securities Exchange Act of 1934 (NYSE/NASDAQ/AMEX); (2) § 15(d) reporting requirement; TCJA 2017 expanded to include foreign private issuers and § 15(d) reporting companies".to_string(),
        "26 USC § 162(m)(3) COVERED EMPLOYEE definition — (1) CEO at any time during year; (2) CFO at any time during year; (3) top 3 other most highly compensated officers; (4) ANY individual who was covered employee for any taxable year beginning AFTER 2016 (ONCE-COVERED-ALWAYS-COVERED rule); (5) post-2026 ARPA FIVE — next 5 most highly compensated employees".to_string(),
        "ONCE-COVERED-ALWAYS-COVERED rule (TCJA 2017 § 13601) — covered employee status survives DEPARTURE FROM EMPLOYER + DEATH (paid to estate); applies to taxable years beginning after 2016; rule does NOT apply to ARPA FIVE (which is retested annually)".to_string(),
        "26 USC § 162(m)(4) APPLICABLE EMPLOYEE REMUNERATION — aggregate amount otherwise allowable as deduction; INCLUDES cash compensation + equity compensation (RSU vesting + NQSO exercise spread) + bonuses + deferred compensation when paid + severance + post-termination payments".to_string(),
        "TCJA 2017 (Pub. L. 115-97 § 13601, effective taxable years beginning after December 31, 2017) — (1) ELIMINATED performance-based compensation exception; (2) ELIMINATED commission exception; (3) introduced once-covered-always-covered rule; (4) expanded publicly held corporation definition; (5) pre-TCJA TRANSITION RULE — written binding contracts in effect on November 2, 2017 and not materially modified preserve former § 162(m)(4)(C) performance-based exception".to_string(),
        "ARPA 2021 (Pub. L. 117-2 § 9708) — for taxable years beginning AFTER DECEMBER 31, 2026, adds NEXT FIVE most highly compensated employees (the ARPA FIVE) bringing total covered employees from 5 to 10; ARPA FIVE includes employees NOT necessarily officers; ARPA FIVE retested ANNUALLY (no permanent once-covered status); proposed regulations Treas. Reg. § 1.162-33 issued January 16, 2025".to_string(),
        "§ 162(m) interaction with § 280G — both deny employer deduction for executive compensation but DIFFERENT TRIGGERS: § 162(m) = $1M annual cap on covered-employee remuneration; § 280G = change-in-control parachute payment ≥ 3× base amount triggers cliff on entire excess over 1× base + § 4999 20% recipient excise tax; both apply simultaneously".to_string(),
        "Trader-critical fact patterns: (1) NASDAQ CEO total comp $8M ($5M cash + $3M RSU) — $1M deductible + $7M non-deductible to employer; (2) trader departed 2023 receives deferred bonus $2M in 2026 — still covered under once-always rule; (3) post-2027 ARPA FIVE — sixth-highest-paid employee $1.5M comp; (4) pre-TCJA grandfather — binding contract October 15, 2017 + qualified performance-based preserves exception; (5) § 162(m) + § 280G dual regime — $4.5M parachute on $1M base triggers both regimes simultaneously".to_string(),
        "Authority and guidance: Treas. Reg. § 1.162-27 (current TCJA-implementing regulations); Treas. Reg. § 1.162-33 (proposed January 16, 2025 ARPA implementation); IRS Pub. 6014 (§ 162(m) Audit Technique Guide); Notice 2018-68 (TCJA transition rule guidance); Rev. Proc. 2024-19".to_string(),
        "Companion to section_280g (Golden Parachute change-in-control regime) + section_409a (NQDC compliance) + section_422 (ISO equity compensation) + section_423 (ESPP) + section_280e (different deduction disallowance regime — controlled-substance trafficking)".to_string(),
    ];

    Section162mResult {
        is_publicly_held_corporation,
        is_covered_employee,
        one_million_cap_cents,
        deductible_compensation_cents,
        non_deductible_compensation_cents,
        grandfather_exception_applies,
        arpa_expansion_applies,
        dual_regime_disallowance,
        failure_reasons,
        citation: "26 USC § 162(m)(1)-(6); Pub. L. 115-97 § 13601 (Tax Cuts and Jobs Act of 2017); Pub. L. 117-2 § 9708 (American Rescue Plan Act of 2021); Treas. Reg. § 1.162-27 (current TCJA-implementing); Treas. Reg. § 1.162-33 (proposed January 16, 2025 ARPA implementation); IRS Pub. 6014 (§ 162(m) Audit Technique Guide); Notice 2018-68 (TCJA transition rule guidance); Rev. Proc. 2024-19",
        notes,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn ceo_8m_comp() -> Section162mInput {
        Section162mInput {
            covered_employee_type: CoveredEmployeeType::Ceo,
            publicly_held_corporation: true,
            total_remuneration_cents: 800_000_000,
            taxable_year: 2026,
            pre_tcja_grandfathered_contract: false,
            qualified_performance_based_pre_tcja: false,
            also_section_280g_excess_parachute: false,
        }
    }

    #[test]
    fn ceo_8m_comp_only_1m_deductible() {
        let r = check(&ceo_8m_comp());
        assert!(r.is_publicly_held_corporation);
        assert!(r.is_covered_employee);
        assert_eq!(r.deductible_compensation_cents, 100_000_000);
        assert_eq!(r.non_deductible_compensation_cents, 700_000_000);
    }

    #[test]
    fn ceo_under_1m_fully_deductible() {
        let mut i = ceo_8m_comp();
        i.total_remuneration_cents = 80_000_000;
        let r = check(&i);
        assert_eq!(r.deductible_compensation_cents, 80_000_000);
        assert_eq!(r.non_deductible_compensation_cents, 0);
    }

    #[test]
    fn ceo_at_1m_boundary_fully_deductible() {
        let mut i = ceo_8m_comp();
        i.total_remuneration_cents = 100_000_000;
        let r = check(&i);
        assert_eq!(r.deductible_compensation_cents, 100_000_000);
        assert_eq!(r.non_deductible_compensation_cents, 0);
    }

    #[test]
    fn not_publicly_held_no_cap() {
        let mut i = ceo_8m_comp();
        i.publicly_held_corporation = false;
        let r = check(&i);
        assert!(!r.is_publicly_held_corporation);
        assert!(!r.is_covered_employee);
        assert_eq!(r.deductible_compensation_cents, 800_000_000);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("§ 162(m)(2)") && f.contains("NOT a publicly held")));
    }

    #[test]
    fn not_covered_employee_fully_deductible() {
        let mut i = ceo_8m_comp();
        i.covered_employee_type = CoveredEmployeeType::NotCovered;
        let r = check(&i);
        assert!(!r.is_covered_employee);
        assert_eq!(r.deductible_compensation_cents, 800_000_000);
    }

    #[test]
    fn cfo_subject_to_cap() {
        let mut i = ceo_8m_comp();
        i.covered_employee_type = CoveredEmployeeType::Cfo;
        let r = check(&i);
        assert!(r.is_covered_employee);
        assert_eq!(r.non_deductible_compensation_cents, 700_000_000);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("§ 162(m)(3)(A) CFO")));
    }

    #[test]
    fn top_three_officer_subject_to_cap() {
        let mut i = ceo_8m_comp();
        i.covered_employee_type = CoveredEmployeeType::TopThreeOfficer;
        let r = check(&i);
        assert!(r.is_covered_employee);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("top 3 most highly compensated officer")));
    }

    #[test]
    fn once_covered_always_covered_rule() {
        let mut i = ceo_8m_comp();
        i.covered_employee_type = CoveredEmployeeType::FormerCoveredEmployee;
        let r = check(&i);
        assert!(r.is_covered_employee);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("§ 162(m)(3)(C) ONCE-COVERED-ALWAYS-COVERED")
                && f.contains("survives departure and death")
                && f.contains("§ 13601")));
    }

    #[test]
    fn arpa_five_pre_2027_not_yet_effective() {
        let mut i = ceo_8m_comp();
        i.covered_employee_type = CoveredEmployeeType::ArpaFive;
        i.taxable_year = 2026;
        let r = check(&i);
        assert!(!r.is_covered_employee);
        assert!(!r.arpa_expansion_applies);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("ARPA FIVE covered status NOT YET EFFECTIVE")));
    }

    #[test]
    fn arpa_five_post_2027_effective() {
        let mut i = ceo_8m_comp();
        i.covered_employee_type = CoveredEmployeeType::ArpaFive;
        i.taxable_year = 2027;
        let r = check(&i);
        assert!(r.is_covered_employee);
        assert!(r.arpa_expansion_applies);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("ARPA FIVE (Pub. L. 117-2 § 9708")
                && f.contains("retested ANNUALLY")));
    }

    #[test]
    fn arpa_expansion_disclosed_post_2027() {
        let mut i = ceo_8m_comp();
        i.taxable_year = 2028;
        let r = check(&i);
        assert!(r.arpa_expansion_applies);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("AMERICAN RESCUE PLAN ACT OF 2021")
                && f.contains("§ 9708")
                && f.contains("DECEMBER 31, 2026")
                && f.contains("ARPA FIVE")));
    }

    #[test]
    fn pre_tcja_grandfather_with_performance_based_exempt() {
        let mut i = ceo_8m_comp();
        i.pre_tcja_grandfathered_contract = true;
        i.qualified_performance_based_pre_tcja = true;
        let r = check(&i);
        assert!(r.grandfather_exception_applies);
        assert_eq!(r.deductible_compensation_cents, 800_000_000);
        assert_eq!(r.non_deductible_compensation_cents, 0);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("PRE-TCJA TRANSITION RULE")
                && f.contains("NOVEMBER 2, 2017")
                && f.contains("§ 162(m)(4)(C)")));
    }

    #[test]
    fn pre_tcja_contract_without_performance_based_not_exempt() {
        let mut i = ceo_8m_comp();
        i.pre_tcja_grandfathered_contract = true;
        i.qualified_performance_based_pre_tcja = false;
        let r = check(&i);
        assert!(!r.grandfather_exception_applies);
        assert_eq!(r.non_deductible_compensation_cents, 700_000_000);
    }

    #[test]
    fn dual_regime_section_280g_disclosed() {
        let mut i = ceo_8m_comp();
        i.also_section_280g_excess_parachute = true;
        let r = check(&i);
        assert!(r.dual_regime_disallowance);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("DUAL-REGIME DISALLOWANCE")
                && f.contains("§ 280G")
                && f.contains("§ 4999")));
    }

    #[test]
    fn covered_employee_type_truth_table_six_cells() {
        for (cet, expect_covered_in_2026, expect_covered_in_2027) in [
            (CoveredEmployeeType::Ceo, true, true),
            (CoveredEmployeeType::Cfo, true, true),
            (CoveredEmployeeType::TopThreeOfficer, true, true),
            (CoveredEmployeeType::FormerCoveredEmployee, true, true),
            (CoveredEmployeeType::ArpaFive, false, true),
            (CoveredEmployeeType::NotCovered, false, false),
        ] {
            let mut i_2026 = ceo_8m_comp();
            i_2026.covered_employee_type = cet;
            i_2026.taxable_year = 2026;
            let r_2026 = check(&i_2026);
            assert_eq!(
                r_2026.is_covered_employee, expect_covered_in_2026,
                "cet={:?} year=2026",
                cet
            );

            let mut i_2027 = ceo_8m_comp();
            i_2027.covered_employee_type = cet;
            i_2027.taxable_year = 2027;
            let r_2027 = check(&i_2027);
            assert_eq!(
                r_2027.is_covered_employee, expect_covered_in_2027,
                "cet={:?} year=2027",
                cet
            );
        }
    }

    #[test]
    fn arpa_five_uniquely_year_gated_invariant() {
        let mut arpa_pre = ceo_8m_comp();
        arpa_pre.covered_employee_type = CoveredEmployeeType::ArpaFive;
        arpa_pre.taxable_year = 2026;
        let r_pre = check(&arpa_pre);
        assert!(!r_pre.is_covered_employee);

        let mut arpa_post = ceo_8m_comp();
        arpa_post.covered_employee_type = CoveredEmployeeType::ArpaFive;
        arpa_post.taxable_year = 2027;
        let r_post = check(&arpa_post);
        assert!(r_post.is_covered_employee);

        // CEO is always covered regardless of year
        let mut ceo_pre = ceo_8m_comp();
        ceo_pre.taxable_year = 2018;
        let r_ceo_pre = check(&ceo_pre);
        assert!(r_ceo_pre.is_covered_employee);
    }

    #[test]
    fn one_million_cap_pinned_at_100m_cents() {
        let r = check(&ceo_8m_comp());
        assert_eq!(r.one_million_cap_cents, 100_000_000);
    }

    #[test]
    fn citation_pins_all_authorities() {
        let r = check(&ceo_8m_comp());
        assert!(r.citation.contains("§ 162(m)(1)-(6)"));
        assert!(r.citation.contains("Pub. L. 115-97 § 13601"));
        assert!(r.citation.contains("Tax Cuts and Jobs Act of 2017"));
        assert!(r.citation.contains("Pub. L. 117-2 § 9708"));
        assert!(r.citation.contains("American Rescue Plan Act of 2021"));
        assert!(r.citation.contains("Treas. Reg. § 1.162-27"));
        assert!(r.citation.contains("Treas. Reg. § 1.162-33"));
        assert!(r.citation.contains("IRS Pub. 6014"));
        assert!(r.citation.contains("Notice 2018-68"));
        assert!(r.citation.contains("Rev. Proc. 2024-19"));
    }

    #[test]
    fn note_pins_subsection_1_deduction_denial() {
        let r = check(&ceo_8m_comp());
        assert!(r.notes.iter().any(|n| n.contains("§ 162(m)(1)")
            && n.contains("$1,000,000")
            && n.contains("§ 280G")
            && n.contains("§ 280E")));
    }

    #[test]
    fn note_pins_subsection_2_publicly_held() {
        let r = check(&ceo_8m_comp());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("§ 162(m)(2) PUBLICLY HELD CORPORATION")
                && n.contains("§ 12 of Securities Exchange Act")
                && n.contains("§ 15(d)")
                && n.contains("foreign private issuers")));
    }

    #[test]
    fn note_pins_subsection_3_covered_employee() {
        let r = check(&ceo_8m_comp());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("§ 162(m)(3) COVERED EMPLOYEE")
                && n.contains("CEO")
                && n.contains("CFO")
                && n.contains("top 3")
                && n.contains("ONCE-COVERED-ALWAYS-COVERED")
                && n.contains("ARPA FIVE")));
    }

    #[test]
    fn note_pins_once_covered_always_covered_tcja() {
        let r = check(&ceo_8m_comp());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("ONCE-COVERED-ALWAYS-COVERED rule")
                && n.contains("§ 13601")
                && n.contains("DEPARTURE FROM EMPLOYER")
                && n.contains("DEATH")));
    }

    #[test]
    fn note_pins_subsection_4_applicable_remuneration() {
        let r = check(&ceo_8m_comp());
        assert!(r.notes.iter().any(
            |n| n.contains("§ 162(m)(4) APPLICABLE EMPLOYEE REMUNERATION")
                && n.contains("equity compensation")
                && n.contains("deferred compensation")
                && n.contains("severance")
                && n.contains("post-termination")
        ));
    }

    #[test]
    fn note_pins_tcja_2017_five_changes() {
        let r = check(&ceo_8m_comp());
        assert!(r.notes.iter().any(|n| n.contains("TCJA 2017")
            && n.contains("§ 13601")
            && n.contains("ELIMINATED performance-based")
            && n.contains("ELIMINATED commission")
            && n.contains("TRANSITION RULE")
            && n.contains("November 2, 2017")));
    }

    #[test]
    fn note_pins_arpa_2021_five_features() {
        let r = check(&ceo_8m_comp());
        assert!(r.notes.iter().any(|n| n.contains("ARPA 2021")
            && n.contains("§ 9708")
            && n.contains("DECEMBER 31, 2026")
            && n.contains("ARPA FIVE")
            && n.contains("retested ANNUALLY")
            && n.contains("January 16, 2025")));
    }

    #[test]
    fn note_pins_section_280g_interaction() {
        let r = check(&ceo_8m_comp());
        assert!(r.notes.iter().any(|n| n.contains("interaction with § 280G")
            && n.contains("$1M annual cap")
            && n.contains("3× base amount")
            && n.contains("§ 4999 20%")
            && n.contains("simultaneously")));
    }

    #[test]
    fn note_pins_trader_fact_patterns_five() {
        let r = check(&ceo_8m_comp());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("Trader-critical fact patterns")
                && n.contains("$8M")
                && n.contains("once-always")
                && n.contains("ARPA FIVE")
                && n.contains("dual regime")));
    }

    #[test]
    fn note_pins_authority_guidance() {
        let r = check(&ceo_8m_comp());
        assert!(r.notes.iter().any(|n| n.contains("Treas. Reg. § 1.162-27")
            && n.contains("Treas. Reg. § 1.162-33")
            && n.contains("IRS Pub. 6014")
            && n.contains("Notice 2018-68")));
    }

    #[test]
    fn note_pins_companion_modules() {
        let r = check(&ceo_8m_comp());
        assert!(r.notes.iter().any(|n| n.contains("section_280g")
            && n.contains("section_409a")
            && n.contains("section_422")
            && n.contains("section_423")
            && n.contains("section_280e")));
    }

    #[test]
    fn defensive_overflow_saturating() {
        let mut i = ceo_8m_comp();
        i.total_remuneration_cents = u64::MAX;
        let r = check(&i);
        let _ = r.non_deductible_compensation_cents;
    }
}
