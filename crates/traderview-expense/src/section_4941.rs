//! IRC § 4941 — Taxes on self-dealing. Private foundation
//! parallel to section_4958 (intermediate sanctions for
//! public charities — iter 466). § 4958(e) EXPLICITLY
//! excludes private foundations from § 4958 because
//! § 4941 self-dealing imposes stricter regime first
//! enacted by Tax Reform Act of 1969, Pub. L. 91-172.
//! Direct companion to section_4958 (iter 466),
//! section_4960 (ATEO comp 21% — iter 464), section_4973
//! (excess contribution — iter 442), section_4974 (RMD —
//! iter 436), section_4975 (qualified plan prohibited
//! transactions — iter 434), section_4980 (employer
//! reversion — iter 460).
//!
//! Four-tier excise tax structure:
//! 1. § 4941(a)(1) TIER 1 DISQUALIFIED PERSON: 10% of
//!    AMOUNT INVOLVED per year of taxable period
//! 2. § 4941(a)(2) TIER 1 FOUNDATION MANAGER (knowing
//!    willful participant): 5% of amount involved per
//!    year of taxable period, capped at $20,000 per act
//!    per § 4941(c)(2)
//! 3. § 4941(b)(1) TIER 2 DISQUALIFIED PERSON
//!    (uncorrected within taxable period): 200% of amount
//!    involved
//! 4. § 4941(b)(2) TIER 2 FOUNDATION MANAGER (refusing to
//!    agree to correction): 50% of amount involved,
//!    capped at $20,000 per act
//!
//! Six self-dealing categories per § 4941(d)(1):
//! 1. § 4941(d)(1)(A) — SALE, EXCHANGE, or LEASING of
//!    property between private foundation and disqualified
//!    person, direct OR indirect
//! 2. § 4941(d)(1)(B) — LENDING OF MONEY or other
//!    extension of credit between PF and DP. Exception:
//!    interest-free loan FROM DP TO PF used exclusively
//!    for charitable purposes is permitted per
//!    § 4941(d)(2)(B)
//! 3. § 4941(d)(1)(C) — FURNISHING OF GOODS, SERVICES, OR
//!    FACILITIES between PF and DP. Exception: DP may
//!    furnish goods/services/facilities to PF WITHOUT
//!    CHARGE per § 4941(d)(2)(C); PF may furnish to DP on
//!    NO MORE FAVORABLE BASIS than to public per
//!    § 4941(d)(2)(D)
//! 4. § 4941(d)(1)(D) — PAYMENT OF COMPENSATION (or
//!    reimbursement of expenses) by PF to DP. Exception:
//!    REASONABLE compensation for PERSONAL SERVICES that
//!    are NECESSARY to carrying out exempt purpose is
//!    permitted per § 4941(d)(2)(E) — but NOT to a
//!    government official
//! 5. § 4941(d)(1)(E) — TRANSFER TO, or USE BY OR FOR
//!    BENEFIT OF, disqualified person of the INCOME OR
//!    ASSETS of a private foundation
//! 6. § 4941(d)(1)(F) — AGREEMENT BY PF to make any
//!    payment of money or other property to a GOVERNMENT
//!    OFFICIAL (as defined in § 4946(c)), other than an
//!    agreement to employ such individual for any period
//!    after such individual terminates government service
//!    if such termination is to occur within 90 days
//!
//! Disqualified person per § 4946:
//! 1. § 4946(a)(1)(A) — Substantial contributor under
//!    § 507(d)(2) where aggregate contributions exceed
//!    $5,000 AND exceed 2% of total contributions received
//!    by PF before close of taxable year
//! 2. § 4946(a)(1)(B) — Foundation manager (officer,
//!    director, trustee, or individual having similar
//!    powers/responsibilities; § 4946(b))
//! 3. § 4946(a)(1)(C) — Owner of more than 20% of
//!    contributor entity (combined voting power of
//!    corporation, profits interest of partnership,
//!    beneficial interest of trust)
//! 4. § 4946(a)(1)(D) — Family member per § 4946(d):
//!    spouse, ancestors, lineal descendants, spouses of
//!    lineal descendants
//! 5. § 4946(a)(1)(E) — 35%-controlled entities such as
//!    a corporation, partnership, or trust where DPs hold
//!    more than 35% combined voting power, profits
//!    interest, or beneficial interest
//! 6. § 4946(a)(1)(F) — Other private foundations
//!    effectively controlled by same persons or that have
//!    more than 35% common ownership
//! 7. § 4946(a)(1)(G) — Government officials (only for
//!    § 4941(d)(1)(F) agreement category)
//!
//! Amount involved per § 4941(e)(1): the GREATER of (i)
//! the amount of money and the fair market value of OTHER
//! PROPERTY given OR (ii) the amount of money and the
//! fair market value of OTHER PROPERTY received, with
//! special rule for LOANS per § 4941(e)(2) — amount
//! involved is the GREATER of the amount paid for the use
//! of the money OR property OR the fair market value of
//! the use for the period for which the money or property
//! is used.
//!
//! Taxable period per § 4941(e)(3) BEGINS on the date the
//! act of self-dealing occurs and ENDS on the EARLIEST of
//! (a) the date the statutory notice of deficiency is
//! issued, (b) the date the § 4941(b)(1) tax is assessed,
//! or (c) the date correction is completed.
//!
//! Correction per § 4941(e)(4): undoing the transaction
//! to the extent possible AND placing the PF in a
//! FINANCIAL POSITION no worse than it would have been in
//! had the DP been dealing with another person under the
//! highest fiduciary standards.
//!
//! Distinction from § 4958 (iter 466): § 4958 applies to
//! PUBLIC charities + § 501(c)(4) social welfare + ACA
//! § 501(c)(29); private foundations are EXCLUDED from
//! § 4958 per § 4958(e) and instead use § 4941 self-
//! dealing regime which has a LOWER threshold (no need to
//! show benefit exceeds consideration — any prohibited
//! act is per se self-dealing) but LOWER initial tax
//! rate (10% Tier-1 vs § 4958's 25%).
//!
//! Authority: 26 U.S.C. § 4941; § 4941(a)(1); § 4941(a)(2);
//! § 4941(b)(1); § 4941(b)(2); § 4941(c)(2);
//! § 4941(d)(1)(A); § 4941(d)(1)(B); § 4941(d)(1)(C);
//! § 4941(d)(1)(D); § 4941(d)(1)(E); § 4941(d)(1)(F);
//! § 4941(d)(2)(B); § 4941(d)(2)(C); § 4941(d)(2)(D);
//! § 4941(d)(2)(E); § 4941(e)(1); § 4941(e)(2); § 4941(e)(3);
//! § 4941(e)(4); § 4946; § 4946(a)(1); § 4946(b);
//! § 4946(c); § 4946(d); § 507(d)(2); 26 C.F.R. § 53.4941-0;
//! 26 C.F.R. § 53.4941(a)-1; 26 C.F.R. § 53.4941(b)-1;
//! 26 C.F.R. § 53.4941(d)-1; 26 C.F.R. § 53.4941(d)-2;
//! 26 C.F.R. § 53.4941(d)-3; 26 C.F.R. § 53.4941(d)-4;
//! 26 C.F.R. § 53.4941(e)-1; 26 C.F.R. § 53.4946-1; Tax
//! Reform Act of 1969, Pub. L. 91-172 (Dec. 30, 1969) —
//! original § 4941 enactment.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Deserialize, Serialize)]
#[serde(rename_all = "snake_case")]
pub enum OrganizationStatus {
    PrivateFoundation,
    PublicCharity,
    NonExempt,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Deserialize, Serialize)]
#[serde(rename_all = "snake_case")]
pub enum SelfDealingCategory {
    SaleExchangeLease,
    LoanExtensionOfCredit,
    FurnishGoodsServicesFacilities,
    CompensationOrExpenseReimbursement,
    UseOfIncomeOrAssets,
    GovernmentOfficialPayment,
}

#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct Input {
    pub organization_status: OrganizationStatus,
    pub category: SelfDealingCategory,
    pub counterparty_is_disqualified_person: bool,
    pub counterparty_is_government_official: bool,
    pub amount_involved_cents: u64,
    pub corrected_within_taxable_period: bool,
    pub correction_complete_amount_cents: u64,
    pub manager_knowing_willful_participant: bool,
    pub manager_refuses_to_agree_to_correction: bool,
    pub reasonable_compensation_for_personal_services_exception: bool,
    pub goods_services_furnished_without_charge_to_pf_exception: bool,
    pub goods_services_to_dp_no_more_favorable_than_public_exception: bool,
    pub interest_free_loan_from_dp_to_pf_charitable_purpose_exception: bool,
}

#[derive(Debug, Clone, PartialEq, Eq, Deserialize, Serialize)]
#[serde(rename_all = "snake_case")]
pub enum Severity {
    NotApplicable,
    Compliant,
    ExceptionApplies,
    Tier1TaxOwed,
    Tier2TaxOwed,
}

#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct Output {
    pub severity: Severity,
    pub tier1_dp_tax_cents: u64,
    pub tier1_manager_tax_cents: u64,
    pub tier2_dp_tax_cents: u64,
    pub tier2_manager_tax_cents: u64,
    pub total_tax_cents: u64,
    pub notes: Vec<String>,
}

pub const TIER1_DP_RATE_PCT: u64 = 10;
pub const TIER1_MANAGER_RATE_PCT: u64 = 5;
pub const TIER2_DP_RATE_PCT: u64 = 200;
pub const TIER2_MANAGER_RATE_PCT: u64 = 50;
pub const MANAGER_TAX_CAP_CENTS: u64 = 2_000_000;

pub type Section4941Input = Input;
pub type Section4941Result = Output;

fn category_exception_applies(input: &Input) -> Option<&'static str> {
    match input.category {
        SelfDealingCategory::CompensationOrExpenseReimbursement => {
            if input.reasonable_compensation_for_personal_services_exception
                && !input.counterparty_is_government_official
            {
                Some("§ 4941(d)(2)(E): reasonable compensation for personal services necessary to carrying out exempt purpose (NOT permitted to government official)")
            } else {
                None
            }
        }
        SelfDealingCategory::FurnishGoodsServicesFacilities => {
            if input.goods_services_furnished_without_charge_to_pf_exception {
                Some("§ 4941(d)(2)(C): disqualified person furnishes goods/services/facilities to PF WITHOUT CHARGE")
            } else if input.goods_services_to_dp_no_more_favorable_than_public_exception {
                Some("§ 4941(d)(2)(D): PF furnishes to disqualified person on NO MORE FAVORABLE basis than to general public")
            } else {
                None
            }
        }
        SelfDealingCategory::LoanExtensionOfCredit => {
            if input.interest_free_loan_from_dp_to_pf_charitable_purpose_exception {
                Some("§ 4941(d)(2)(B): INTEREST-FREE loan FROM disqualified person TO PF used exclusively for charitable purposes")
            } else {
                None
            }
        }
        _ => None,
    }
}

pub fn check(input: &Input) -> Output {
    let notes: Vec<String> = vec![
        "§ 4941(a)(1) Tier-1 DISQUALIFIED PERSON 10% excise + § 4941(a)(2) Tier-1 FOUNDATION MANAGER 5% excise (knowing willful participant, capped at $20,000 per § 4941(c)(2)) + § 4941(b)(1) Tier-2 DP 200% excise (uncorrected within taxable period) + § 4941(b)(2) Tier-2 MANAGER 50% excise (refusing correction, capped at $20,000).".to_string(),
        "Six self-dealing categories per § 4941(d)(1): (A) sale/exchange/lease; (B) lending of money or extension of credit; (C) furnishing goods/services/facilities; (D) payment of compensation/expenses; (E) transfer to/use by/for benefit of disqualified person of PF income/assets; (F) agreement to pay government official (§ 4946(c) defined).".to_string(),
        "Disqualified person per § 4946: substantial contributors (§ 507(d)(2) > $5K AND > 2% of total contributions) + foundation managers (officers/directors/trustees + § 4946(b)) + 20%-owners of contributor entities + family per § 4946(d) (spouse + ancestors + lineal descendants + spouses of lineal descendants) + 35%-controlled entities + other related PFs + government officials (§ 4946(c) for § 4941(d)(1)(F) only).".to_string(),
        "Amount involved per § 4941(e)(1): GREATER of money and FMV of property GIVEN vs RECEIVED; loans per § 4941(e)(2) use FMV of use of money/property for use period.".to_string(),
        "Taxable period per § 4941(e)(3) begins on date of self-dealing act; ends on EARLIEST of (a) statutory notice of deficiency, (b) § 4941(b)(1) tax assessment, (c) correction completed.".to_string(),
        "Correction per § 4941(e)(4): UNDO transaction + place PF in financial position no worse than it would have been with another person under HIGHEST FIDUCIARY STANDARDS.".to_string(),
        "Self-dealing exceptions per § 4941(d)(2): § 4941(d)(2)(B) interest-free loan FROM DP TO PF for charitable purpose; § 4941(d)(2)(C) DP furnishes goods/services/facilities to PF WITHOUT CHARGE; § 4941(d)(2)(D) PF furnishes to DP on NO MORE FAVORABLE basis than public; § 4941(d)(2)(E) reasonable compensation for personal services necessary to exempt purpose (NOT permitted to government official).".to_string(),
        "Coordination with § 4958 (iter 466): § 4958 applies to PUBLIC charities + § 501(c)(4) + § 501(c)(29); private foundations EXCLUDED from § 4958 per § 4958(e), use § 4941 instead — LOWER threshold (per se rule, no excess-benefit comparison) but LOWER initial tax (10% vs 25%).".to_string(),
        "Companion: section_4958 (iter 466), section_4960 (iter 464), section_4973 (iter 442), section_4974 (iter 436), section_4975 (iter 434), section_4980 (iter 460).".to_string(),
    ];

    if !matches!(
        input.organization_status,
        OrganizationStatus::PrivateFoundation
    ) {
        let mut n = notes;
        n.push(
            "Organization is not a private foundation — § 4941 does not apply. Public charities use § 4958 intermediate sanctions instead."
                .to_string(),
        );
        return Output {
            severity: Severity::NotApplicable,
            tier1_dp_tax_cents: 0,
            tier1_manager_tax_cents: 0,
            tier2_dp_tax_cents: 0,
            tier2_manager_tax_cents: 0,
            total_tax_cents: 0,
            notes: n,
        };
    }

    if !input.counterparty_is_disqualified_person && !input.counterparty_is_government_official {
        let mut n = notes;
        n.push("Counterparty is not a disqualified person or government official — § 4941 does not apply.".to_string());
        return Output {
            severity: Severity::NotApplicable,
            tier1_dp_tax_cents: 0,
            tier1_manager_tax_cents: 0,
            tier2_dp_tax_cents: 0,
            tier2_manager_tax_cents: 0,
            total_tax_cents: 0,
            notes: n,
        };
    }

    if let Some(exception) = category_exception_applies(input) {
        let mut n = notes;
        n.push(format!("Exception applies: {}", exception));
        return Output {
            severity: Severity::ExceptionApplies,
            tier1_dp_tax_cents: 0,
            tier1_manager_tax_cents: 0,
            tier2_dp_tax_cents: 0,
            tier2_manager_tax_cents: 0,
            total_tax_cents: 0,
            notes: n,
        };
    }

    let amount = input.amount_involved_cents;

    let tier1_dp = amount
        .saturating_mul(TIER1_DP_RATE_PCT)
        .checked_div(100)
        .unwrap_or(0);

    let tier1_manager = if input.manager_knowing_willful_participant {
        let uncapped = amount
            .saturating_mul(TIER1_MANAGER_RATE_PCT)
            .checked_div(100)
            .unwrap_or(0);
        uncapped.min(MANAGER_TAX_CAP_CENTS)
    } else {
        0
    };

    let corrected =
        input.corrected_within_taxable_period && input.correction_complete_amount_cents >= amount;

    let tier2_dp = if corrected {
        0
    } else {
        amount
            .saturating_mul(TIER2_DP_RATE_PCT)
            .checked_div(100)
            .unwrap_or(0)
    };

    let tier2_manager = if !corrected && input.manager_refuses_to_agree_to_correction {
        let uncapped = amount
            .saturating_mul(TIER2_MANAGER_RATE_PCT)
            .checked_div(100)
            .unwrap_or(0);
        uncapped.min(MANAGER_TAX_CAP_CENTS)
    } else {
        0
    };

    let total = tier1_dp
        .saturating_add(tier1_manager)
        .saturating_add(tier2_dp)
        .saturating_add(tier2_manager);

    let severity = if tier2_dp > 0 || tier2_manager > 0 {
        Severity::Tier2TaxOwed
    } else {
        Severity::Tier1TaxOwed
    };

    let mut n = notes;
    if tier2_dp == 0 {
        n.push(
            "Tier-2 200% DP tax avoided: act corrected within taxable period per § 4941(e)(4)."
                .to_string(),
        );
    }
    if input.manager_knowing_willful_participant {
        n.push(format!(
            "Tier-1 manager tax under § 4941(a)(2): ${}.{:02} (5% rate capped at $20,000 per § 4941(c)(2)).",
            tier1_manager / 100,
            tier1_manager % 100
        ));
    }

    Output {
        severity,
        tier1_dp_tax_cents: tier1_dp,
        tier1_manager_tax_cents: tier1_manager,
        tier2_dp_tax_cents: tier2_dp,
        tier2_manager_tax_cents: tier2_manager,
        total_tax_cents: total,
        notes: n,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn baseline() -> Input {
        Input {
            organization_status: OrganizationStatus::PrivateFoundation,
            category: SelfDealingCategory::SaleExchangeLease,
            counterparty_is_disqualified_person: true,
            counterparty_is_government_official: false,
            amount_involved_cents: 100_000_00, // $100K
            corrected_within_taxable_period: false,
            correction_complete_amount_cents: 0,
            manager_knowing_willful_participant: false,
            manager_refuses_to_agree_to_correction: false,
            reasonable_compensation_for_personal_services_exception: false,
            goods_services_furnished_without_charge_to_pf_exception: false,
            goods_services_to_dp_no_more_favorable_than_public_exception: false,
            interest_free_loan_from_dp_to_pf_charitable_purpose_exception: false,
        }
    }

    #[test]
    fn public_charity_not_subject_to_4941() {
        let mut i = baseline();
        i.organization_status = OrganizationStatus::PublicCharity;
        let out = check(&i);
        assert_eq!(out.severity, Severity::NotApplicable);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4958"));
    }

    #[test]
    fn non_exempt_not_applicable() {
        let mut i = baseline();
        i.organization_status = OrganizationStatus::NonExempt;
        let out = check(&i);
        assert_eq!(out.severity, Severity::NotApplicable);
    }

    #[test]
    fn non_dp_counterparty_not_applicable() {
        let mut i = baseline();
        i.counterparty_is_disqualified_person = false;
        let out = check(&i);
        assert_eq!(out.severity, Severity::NotApplicable);
    }

    #[test]
    fn sale_exchange_lease_self_dealing_per_se() {
        // PF sells property to DP at FMV → still per-se self-dealing
        let i = baseline();
        let out = check(&i);
        assert_eq!(out.severity, Severity::Tier2TaxOwed);
        assert_eq!(out.tier1_dp_tax_cents, 10_000_00); // 10% of $100K
        assert_eq!(out.tier2_dp_tax_cents, 200_000_00); // 200% uncorrected
    }

    #[test]
    fn corrected_within_period_no_tier2() {
        let mut i = baseline();
        i.corrected_within_taxable_period = true;
        i.correction_complete_amount_cents = 100_000_00;
        let out = check(&i);
        assert_eq!(out.tier1_dp_tax_cents, 10_000_00);
        assert_eq!(out.tier2_dp_tax_cents, 0);
        assert_eq!(out.severity, Severity::Tier1TaxOwed);
    }

    #[test]
    fn partial_correction_tier2_imposed() {
        let mut i = baseline();
        i.corrected_within_taxable_period = true;
        i.correction_complete_amount_cents = 50_000_00; // partial
        let out = check(&i);
        assert_eq!(out.tier2_dp_tax_cents, 200_000_00);
    }

    #[test]
    fn manager_knowing_5_percent_capped_at_20k() {
        let mut i = baseline();
        i.manager_knowing_willful_participant = true;
        i.amount_involved_cents = 1_000_000_00; // $1M
                                                // 5% × $1M = $50K, capped at $20K
        let out = check(&i);
        assert_eq!(out.tier1_manager_tax_cents, 20_000_00);
    }

    #[test]
    fn manager_knowing_under_cap() {
        let mut i = baseline();
        i.manager_knowing_willful_participant = true;
        // 5% × $100K = $5K (under cap)
        let out = check(&i);
        assert_eq!(out.tier1_manager_tax_cents, 5_000_00);
    }

    #[test]
    fn manager_not_knowing_no_tier1_manager_tax() {
        let i = baseline();
        let out = check(&i);
        assert_eq!(out.tier1_manager_tax_cents, 0);
    }

    #[test]
    fn manager_refuses_correction_50_percent_capped() {
        let mut i = baseline();
        i.manager_refuses_to_agree_to_correction = true;
        i.amount_involved_cents = 100_000_00;
        // 50% × $100K = $50K, capped at $20K
        let out = check(&i);
        assert_eq!(out.tier2_manager_tax_cents, 20_000_00);
    }

    #[test]
    fn manager_refuses_correction_under_cap() {
        let mut i = baseline();
        i.manager_refuses_to_agree_to_correction = true;
        i.amount_involved_cents = 30_000_00;
        // 50% × $30K = $15K (under cap)
        let out = check(&i);
        assert_eq!(out.tier2_manager_tax_cents, 15_000_00);
    }

    #[test]
    fn manager_refuses_but_act_corrected_no_tier2_manager() {
        let mut i = baseline();
        i.corrected_within_taxable_period = true;
        i.correction_complete_amount_cents = 100_000_00;
        i.manager_refuses_to_agree_to_correction = true; // moot when corrected
        let out = check(&i);
        assert_eq!(out.tier2_manager_tax_cents, 0);
    }

    #[test]
    fn lending_exception_interest_free_from_dp_to_pf_charitable() {
        let mut i = baseline();
        i.category = SelfDealingCategory::LoanExtensionOfCredit;
        i.interest_free_loan_from_dp_to_pf_charitable_purpose_exception = true;
        let out = check(&i);
        assert_eq!(out.severity, Severity::ExceptionApplies);
        assert_eq!(out.total_tax_cents, 0);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4941(d)(2)(B)"));
        assert!(joined.contains("INTEREST-FREE"));
    }

    #[test]
    fn lending_no_exception_self_dealing() {
        let mut i = baseline();
        i.category = SelfDealingCategory::LoanExtensionOfCredit;
        // No exception → self-dealing
        let out = check(&i);
        assert_eq!(out.severity, Severity::Tier2TaxOwed);
    }

    #[test]
    fn furnish_goods_without_charge_to_pf_exception() {
        let mut i = baseline();
        i.category = SelfDealingCategory::FurnishGoodsServicesFacilities;
        i.goods_services_furnished_without_charge_to_pf_exception = true;
        let out = check(&i);
        assert_eq!(out.severity, Severity::ExceptionApplies);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4941(d)(2)(C)"));
        assert!(joined.contains("WITHOUT CHARGE"));
    }

    #[test]
    fn furnish_to_dp_no_more_favorable_than_public_exception() {
        let mut i = baseline();
        i.category = SelfDealingCategory::FurnishGoodsServicesFacilities;
        i.goods_services_to_dp_no_more_favorable_than_public_exception = true;
        let out = check(&i);
        assert_eq!(out.severity, Severity::ExceptionApplies);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4941(d)(2)(D)"));
        assert!(joined.contains("NO MORE FAVORABLE"));
    }

    #[test]
    fn reasonable_compensation_for_personal_services_exception() {
        let mut i = baseline();
        i.category = SelfDealingCategory::CompensationOrExpenseReimbursement;
        i.reasonable_compensation_for_personal_services_exception = true;
        let out = check(&i);
        assert_eq!(out.severity, Severity::ExceptionApplies);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4941(d)(2)(E)"));
        assert!(joined.contains("personal services"));
    }

    #[test]
    fn reasonable_compensation_to_government_official_no_exception() {
        let mut i = baseline();
        i.category = SelfDealingCategory::CompensationOrExpenseReimbursement;
        i.reasonable_compensation_for_personal_services_exception = true;
        i.counterparty_is_government_official = true;
        let out = check(&i);
        // Exception does not apply to government officials
        assert_eq!(out.severity, Severity::Tier2TaxOwed);
    }

    #[test]
    fn government_official_payment_category() {
        let mut i = baseline();
        i.category = SelfDealingCategory::GovernmentOfficialPayment;
        i.counterparty_is_government_official = true;
        i.counterparty_is_disqualified_person = false;
        let out = check(&i);
        assert_eq!(out.severity, Severity::Tier2TaxOwed);
    }

    #[test]
    fn use_of_assets_self_dealing() {
        let mut i = baseline();
        i.category = SelfDealingCategory::UseOfIncomeOrAssets;
        let out = check(&i);
        assert_eq!(out.severity, Severity::Tier2TaxOwed);
        assert_eq!(out.tier1_dp_tax_cents, 10_000_00);
    }

    #[test]
    fn all_four_tiers_stack() {
        let mut i = baseline();
        i.manager_knowing_willful_participant = true;
        i.manager_refuses_to_agree_to_correction = true;
        let out = check(&i);
        // Tier-1 DP: 10% × $100K = $10K
        // Tier-1 Manager: 5% × $100K = $5K
        // Tier-2 DP: 200% × $100K = $200K
        // Tier-2 Manager: 50% × $100K = $50K → capped at $20K
        // Total: $235K
        assert_eq!(out.tier1_dp_tax_cents, 10_000_00);
        assert_eq!(out.tier1_manager_tax_cents, 5_000_00);
        assert_eq!(out.tier2_dp_tax_cents, 200_000_00);
        assert_eq!(out.tier2_manager_tax_cents, 20_000_00);
        assert_eq!(out.total_tax_cents, 235_000_00);
    }

    #[test]
    fn citation_pins_all_authorities() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4941(a)(1)"));
        assert!(joined.contains("§ 4941(a)(2)"));
        assert!(joined.contains("§ 4941(b)(1)"));
        assert!(joined.contains("§ 4941(b)(2)"));
        assert!(joined.contains("§ 4941(c)(2)"));
        assert!(joined.contains("§ 4941(d)(1)"));
        assert!(joined.contains("§ 4941(d)(2)"));
        assert!(joined.contains("§ 4941(e)(1)"));
        assert!(joined.contains("§ 4941(e)(2)"));
        assert!(joined.contains("§ 4941(e)(3)"));
        assert!(joined.contains("§ 4941(e)(4)"));
        assert!(joined.contains("§ 4946"));
        assert!(joined.contains("§ 4946(b)"));
        assert!(joined.contains("§ 4946(c)"));
        assert!(joined.contains("§ 4946(d)"));
        assert!(joined.contains("§ 507(d)(2)"));
        assert!(joined.contains("§ 4958"));
    }

    #[test]
    fn note_pins_six_self_dealing_categories() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("sale/exchange/lease"));
        assert!(joined.contains("lending"));
        assert!(joined.contains("furnishing goods/services/facilities"));
        assert!(joined.contains("compensation"));
        assert!(joined.contains("transfer to/use by"));
        assert!(joined.contains("government official"));
    }

    #[test]
    fn note_pins_four_tax_tiers() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("10%"));
        assert!(joined.contains("5%"));
        assert!(joined.contains("200%"));
        assert!(joined.contains("50%"));
        assert!(joined.contains("$20,000"));
    }

    #[test]
    fn note_pins_disqualified_person_categories() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("substantial contributors"));
        assert!(joined.contains("foundation managers"));
        assert!(joined.contains("> 2%"));
        assert!(joined.contains("20%"));
        assert!(joined.contains("family"));
        assert!(joined.contains("35%"));
    }

    #[test]
    fn note_pins_four_exceptions() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4941(d)(2)(B)"));
        assert!(joined.contains("§ 4941(d)(2)(C)"));
        assert!(joined.contains("§ 4941(d)(2)(D)"));
        assert!(joined.contains("§ 4941(d)(2)(E)"));
    }

    #[test]
    fn note_pins_4958_coordination() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("§ 4958 (iter 466)"));
        assert!(joined.contains("LOWER threshold"));
        assert!(joined.contains("per se rule"));
        assert!(joined.contains("LOWER initial tax"));
    }

    #[test]
    fn note_pins_correction_highest_fiduciary_standards() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("HIGHEST FIDUCIARY STANDARDS"));
        assert!(joined.contains("UNDO"));
    }

    #[test]
    fn note_pins_amount_involved_definition() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("GREATER of money and FMV"));
        assert!(joined.contains("§ 4941(e)(2)"));
    }

    #[test]
    fn note_pins_companion_modules() {
        let i = baseline();
        let out = check(&i);
        let joined = out.notes.join(" ");
        assert!(joined.contains("section_4958"));
        assert!(joined.contains("section_4960"));
        assert!(joined.contains("section_4975"));
        assert!(joined.contains("section_4980"));
    }

    #[test]
    fn truth_table_three_cells() {
        // Cell 1: PF + DP + per-se act = Tier-2 if uncorrected
        let c1 = check(&baseline());
        assert_eq!(c1.severity, Severity::Tier2TaxOwed);

        // Cell 2: PF + DP + corrected = Tier-1 only
        let mut c2 = baseline();
        c2.corrected_within_taxable_period = true;
        c2.correction_complete_amount_cents = 100_000_00;
        assert_eq!(check(&c2).severity, Severity::Tier1TaxOwed);

        // Cell 3: Public charity = not applicable (uses § 4958)
        let mut c3 = baseline();
        c3.organization_status = OrganizationStatus::PublicCharity;
        assert_eq!(check(&c3).severity, Severity::NotApplicable);
    }

    #[test]
    fn saturating_arithmetic_overflow_defense() {
        let mut i = baseline();
        i.amount_involved_cents = u64::MAX;
        i.manager_knowing_willful_participant = true;
        i.manager_refuses_to_agree_to_correction = true;
        let out = check(&i);
        // No panic; manager caps bind
        assert_eq!(out.tier1_manager_tax_cents, MANAGER_TAX_CAP_CENTS);
        assert_eq!(out.tier2_manager_tax_cents, MANAGER_TAX_CAP_CENTS);
    }

    #[test]
    fn boundary_one_cent_amount_involved() {
        let mut i = baseline();
        i.amount_involved_cents = 1;
        let out = check(&i);
        // 10% of 1 cent = 0
        assert_eq!(out.tier1_dp_tax_cents, 0);
        // 200% of 1 cent = 2
        assert_eq!(out.tier2_dp_tax_cents, 2);
    }

    #[test]
    fn boundary_correction_exact_amount_sufficient() {
        let mut i = baseline();
        i.corrected_within_taxable_period = true;
        i.correction_complete_amount_cents = 100_000_00; // exactly equal
        let out = check(&i);
        assert_eq!(out.tier2_dp_tax_cents, 0);
    }

    #[test]
    fn correction_overpaid_treated_as_sufficient() {
        let mut i = baseline();
        i.corrected_within_taxable_period = true;
        i.correction_complete_amount_cents = 200_000_00; // overpaid
        let out = check(&i);
        assert_eq!(out.tier2_dp_tax_cents, 0);
    }

    #[test]
    fn correction_late_outside_period_no_relief() {
        let mut i = baseline();
        i.corrected_within_taxable_period = false; // late
        i.correction_complete_amount_cents = 100_000_00; // would be sufficient but late
        let out = check(&i);
        assert_eq!(out.tier2_dp_tax_cents, 200_000_00);
    }
}
