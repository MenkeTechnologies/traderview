//! IRC § 4980H — Shared responsibility for employers
//! regarding health coverage (ACA employer mandate /
//! "pay or play"). Direct trader companion to
//! section_162m ($1M public-company exec comp
//! deduction — iter 446), section_415 (umbrella limits
//! — iter 452), section_409a (NQDC), section_280g
//! (golden parachute — iter 444).
//!
//! § 4980H imposes EMPLOYER SHARED RESPONSIBILITY
//! PAYMENT (ESRP) on "applicable large employer" (ALE)
//! that fails to:
//! 1. § 4980H(a) — OFFER minimum essential coverage
//!    (MEC) to at least 95% of full-time employees
//!    and their dependents; OR
//! 2. § 4980H(b) — offer coverage that is BOTH
//!    AFFORDABLE under § 4980H(c)(4) AND meets
//!    MINIMUM VALUE (covers at least 60% of expected
//!    healthcare costs).
//!
//! Trader-critical because LLC traders, S-corp traders,
//! and traders running multi-property landlord
//! operations with maintenance staff and property
//! managers can quickly cross the 50-FTE ALE threshold
//! and become subject to § 4980H penalties.
//!
//! Companion to section_162m (exec comp), section_409a
//! (NQDC compliance), section_415 (umbrella limits),
//! section_4973 (excess contribution excise — iter
//! 442), section_280g (change-in-control parachute
//! payment — iter 444).
//!
//! **§ 4980H(c)(2) Applicable Large Employer (ALE)
//! definition**:
//! 1. Employer that employed AVERAGE OF AT LEAST 50
//!    FULL-TIME EMPLOYEES (including FTE-equivalents)
//!    on business days during preceding calendar
//!    year;
//! 2. § 4980H(c)(2)(C) FULL-TIME EMPLOYEE = 30+ hours
//!    of service per week (130 hours per month);
//! 3. § 4980H(c)(2)(E) FTE-EQUIVALENT calculation:
//!    aggregate hours of service of part-time
//!    employees in month / 120;
//! 4. § 414(b)/(c)/(m)/(o) CONTROLLED GROUP
//!    AGGREGATION — related employers treated as
//!    SINGLE employer for ALE threshold;
//! 5. SEASONAL WORKER EXCEPTION — if ALE crossed 50
//!    threshold only because of seasonal workers for
//!    120 days or fewer, NOT subject to mandate.
//!
//! **§ 4980H(a) "A PENALTY" — failure to offer MEC**:
//! Triggered if (1) ALE fails to offer MEC to at least
//! 95% of full-time employees + dependents for any
//! month; AND (2) at least one full-time employee
//! certified as having received premium tax credit
//! (PTC) or cost-sharing reduction for Exchange
//! coverage.
//!
//! Penalty calculation: FTEs minus 30 × 1/12 of
//! annual penalty amount × number of months in
//! violation.
//!
//! 2026 § 4980H(a) annual penalty = **$3,340 per
//! FTE** (up from $2,900 for 2025); monthly = $278.33;
//! original 2014 amount $2,000 indexed under § 4980H(c)
//! (5) CPI adjustment.
//!
//! **§ 4980H(b) "B PENALTY" — unaffordable or non-MV
//! coverage**:
//! Triggered if (1) ALE offers coverage to 95%+ of
//! full-time employees BUT (2) coverage is either
//! UNAFFORDABLE under § 4980H(c)(4) OR fails to
//! provide MINIMUM VALUE under § 36B(c)(2)(C)(ii);
//! AND (3) full-time employee certified as having
//! received PTC.
//!
//! Penalty calculation: per non-compliant full-time
//! employee × 1/12 of annual penalty amount × number
//! of months in violation.
//!
//! 2026 § 4980H(b) annual penalty = **$5,010 per
//! employee receiving PTC** (up from $4,350 for
//! 2025); monthly = $417.50; original 2014 amount
//! $3,000 indexed under § 4980H(c)(5).
//!
//! **§ 4980H(c)(4) AFFORDABILITY** — employee
//! contribution for self-only coverage cannot exceed
//! affordability percentage of household income:
//! - 2026: **9.96%** of household income (up from
//!   9.02% for 2025);
//! - § 36B(c)(2)(C)(i) federal poverty line safe
//!   harbor;
//! - Rev. Proc. 2014-37 + IRS Notice 2015-87 three
//!   safe harbors: (1) W-2 wages box 1; (2) rate of
//!   pay; (3) federal poverty line.
//!
//! **§ 36B(c)(2)(C)(ii) MINIMUM VALUE** — plan must
//! cover at least 60% of expected healthcare costs;
//! actuarial value calculation per Treas. Reg.
//! § 1.36B-6.
//!
//! **§ 4980H(d) MINIMUM ESSENTIAL COVERAGE (MEC)**:
//! 1. § 5000A(f) MEC includes employer-sponsored
//!    group health plan; individual market plan;
//!    Medicare; Medicaid; CHIP; TRICARE; VA medical
//!    benefits; Peace Corps;
//: 2. NOT MEC: dental-only; vision-only; long-term
//!    care; disability income; workers' compensation;
//!    accident-only; on-site medical clinics.
//!
//! **§ 6056 REPORTING — Form 1094-C / 1095-C**:
//! 1. § 6056(a) — ALEs must file annual return
//!    with IRS showing health coverage offered to
//!    each full-time employee;
//! 2. § 6056(c) — ALEs must furnish written
//!    statement to each full-time employee (Form
//!    1095-C) by January 31 (or extended date) of
//!    year following calendar year reported;
//! 3. § 6721/§ 6722 — civil penalties for failure
//!    to file/furnish increased significantly post-
//!    PATH Act of 2015.
//!
//! **§ 414(b)/(c)/(m)/(o) CONTROLLED GROUP
//! AGGREGATION**:
//! 1. Related employers (parent-subsidiary,
//!    brother-sister, affiliated service group)
//!    aggregated for ALE threshold determination;
//! 2. CAN PUSH small business across 50 FTE line
//!    via related-employer headcount;
//! 3. EACH ENTITY in controlled group is
//!    SEPARATELY ASSESSED ESRP penalty for its own
//!    full-time employees;
//! 4. Cannot circumvent mandate by splitting
//!    workforce among related entities.
//!
//! **§ 4980H(c)(2)(D) SEASONAL WORKER EXCEPTION**:
//! 1. Employee employed FOR 120 DAYS OR FEWER in
//!    seasonal capacity NOT counted for ALE
//!    threshold;
//! 2. Common seasonal occupations: agriculture,
//!    retail (holiday), summer camp, ski resort,
//:    tax preparation, landscaping.
//!
//! **TRANSITION RULE — § 4980H(a) "MINUS 30"
//! REDUCTION**:
//! ALE's first 30 full-time employees EXCLUDED from
//! § 4980H(a) penalty calculation; effective
//! penalty floor at 31+ employees; controlled
//! group aggregation allocates 30-employee
//! reduction proportionally per § 4980H(c)(2)(D)(i).
//!
//! **Trader-landlord critical fact patterns**:
//!
//! Trader-LLC operates 200-unit apartment portfolio
//! with 60 maintenance staff + property managers
//! all 30+ hours/week — crosses 50 FTE threshold +
//! becomes ALE; fails to offer MEC; one FT employee
//! receives Exchange PTC — § 4980H(a) penalty
//! 2026: (60 - 30) × $3,340 = $100,200 annual
//! penalty.
//!
//! Trader-S-corp employer offers high-deductible
//! plan + employee contribution 12% of W-2 wages —
//! exceeds 9.96% affordability threshold; ALE
//! offers coverage but it's UNAFFORDABLE; 3 full-
//! time employees receive PTC — § 4980H(b)
//! penalty: 3 × $5,010 = $15,030 annual.
//!
//! Trader operates 4 related LLCs each with 20-25
//! employees + treated as single ALE under § 414
//! controlled group aggregation; aggregate 95
//! employees — combined ALE; each entity subject
//! to ESRP per its own headcount.
//!
//! Trader engages 25 full-time employees + 30
//! seasonal workers (120 days or fewer); seasonal
//! worker exception under § 4980H(c)(2)(D) — NOT
//! ALE; no mandate.
//!
//: § 6056 reporting — trader-LLC ALE files Form
//! 1094-C (transmittal) + 1095-C (employee
//! statement) by January 31; failure to furnish
//! 1095-C to employees + IRS triggers § 6722
//: civil penalty up to $310 per return for 2026.
//!
//! Citations: 26 USC § 4980H(a) (failure to offer
//! MEC); 26 USC § 4980H(b) (unaffordable or non-MV
//! coverage); 26 USC § 4980H(c)(2) (ALE definition);
//! 26 USC § 4980H(c)(2)(C) (full-time employee 30
//! hours/week); 26 USC § 4980H(c)(2)(D) (seasonal
//! worker exception); 26 USC § 4980H(c)(2)(E) (FTE-
//! equivalent calculation); 26 USC § 4980H(c)(4)
//! (affordability); 26 USC § 4980H(c)(5) (CPI
//! adjustments); 26 USC § 4980H(d) (MEC); 26 USC
//! § 5000A(f) (MEC definition); 26 USC § 36B(c)(2)
//! (C)(i) (federal poverty line safe harbor);
//! 26 USC § 36B(c)(2)(C)(ii) (minimum value); 26
//! USC § 6056(a) (ALE return); 26 USC § 6056(c)
//! (employee statement); 26 USC § 6721/§ 6722
//! (civil penalties); 26 USC § 414(b)/(c)/(m)/(o)
//! (controlled group aggregation); Treas. Reg.
//! § 54.4980H-1 through § 54.4980H-6; Treas. Reg.
//! § 1.36B-6 (minimum value); Rev. Proc. 2014-37
//! (W-2 safe harbor); IRS Notice 2015-87 (rate-of-
//! pay + federal poverty line safe harbors); IRS
//! Notice 2025-XX (2026 affordability percentage
//! 9.96%); IRS Rev. Proc. 2025-XX (2026 penalty
//! amounts); Patient Protection and Affordable
//! Care Act of 2010 (Pub. L. 111-148); Health Care
//! and Education Reconciliation Act of 2010 (Pub.
//! L. 111-152); Form 1094-C; Form 1095-C.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, Deserialize, Serialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum CoverageStatus {
    /// No MEC offered to at least 95% of FT employees.
    NoMecOffered,
    /// MEC offered but UNAFFORDABLE under § 4980H(c)
    /// (4).
    MecUnaffordable,
    /// MEC offered but fails MINIMUM VALUE under
    /// § 36B(c)(2)(C)(ii).
    MecNonMinimumValue,
    /// MEC offered, affordable, minimum value
    /// (compliant).
    CompliantOffering,
}

#[derive(Debug, Clone, Deserialize)]
pub struct Section4980hInput {
    /// Number of full-time employees (30+ hours/week
    /// per § 4980H(c)(2)(C)).
    pub full_time_employees: u32,
    /// FTE-equivalent count from part-time employees
    /// (aggregate part-time hours / 120 per § 4980H(c)
    /// (2)(E)).
    pub fte_equivalents: u32,
    /// Seasonal workers employed 120 days or fewer
    /// (excepted from ALE count under § 4980H(c)(2)
    /// (D)).
    pub seasonal_workers_120_days_or_fewer: u32,
    pub coverage_status: CoverageStatus,
    /// Whether at least one full-time employee
    /// received § 36B premium tax credit for
    /// Exchange coverage (trigger for § 4980H
    /// assessment).
    pub at_least_one_ft_received_ptc: bool,
    /// Number of FT employees receiving PTC (for
    /// § 4980H(b) per-employee penalty calculation).
    pub ft_employees_receiving_ptc: u32,
    /// Months in violation in the assessment year.
    pub months_in_violation: u32,
    /// Employee contribution percentage of household
    /// income for self-only coverage (§ 4980H(c)(4)
    /// affordability test); compared to 9.96% for
    /// 2026.
    pub employee_contribution_percent: u32,
    /// Minimum value percentage of expected
    /// healthcare costs covered by plan; compared to
    /// 60% under § 36B(c)(2)(C)(ii).
    pub minimum_value_percent: u32,
    /// Whether § 414(b)/(c)/(m)/(o) controlled group
    /// aggregation applies.
    pub controlled_group_aggregation: bool,
    /// Whether § 6056 ALE return (Form 1094-C) and
    /// employee statements (Form 1095-C) were filed
    /// and furnished timely.
    pub section_6056_reporting_compliant: bool,
}

#[derive(Debug, Clone, Serialize, PartialEq, Eq)]
pub struct Section4980hResult {
    pub combined_fte_count: u32,
    pub is_applicable_large_employer: bool,
    pub seasonal_worker_exception_engaged: bool,
    pub section_4980h_a_engaged: bool,
    pub section_4980h_b_engaged: bool,
    pub section_4980h_a_annual_penalty_cents: u64,
    pub section_4980h_b_annual_penalty_cents: u64,
    pub affordability_threshold_2026_percent: u32,
    pub minimum_value_threshold_percent: u32,
    pub section_6056_reporting_compliant: bool,
    pub failure_reasons: Vec<String>,
    pub citation: &'static str,
    pub notes: Vec<String>,
}

pub fn check(input: &Section4980hInput) -> Section4980hResult {
    let mut failure_reasons: Vec<String> = Vec::new();

    // 2026 amounts
    let section_4980h_a_annual_per_fte_cents: u64 = 334_000;
    let section_4980h_b_annual_per_employee_cents: u64 = 501_000;
    let affordability_threshold_2026_percent: u32 = 996;
    let minimum_value_threshold_percent: u32 = 60;

    let combined_fte_count = input
        .full_time_employees
        .saturating_add(input.fte_equivalents);

    let seasonal_worker_exception_engaged = combined_fte_count >= 50
        && combined_fte_count.saturating_sub(input.seasonal_workers_120_days_or_fewer) < 50;

    let is_applicable_large_employer =
        combined_fte_count >= 50 && !seasonal_worker_exception_engaged;

    let section_4980h_a_engaged = is_applicable_large_employer
        && matches!(input.coverage_status, CoverageStatus::NoMecOffered)
        && input.at_least_one_ft_received_ptc;

    let section_4980h_b_engaged = is_applicable_large_employer
        && matches!(
            input.coverage_status,
            CoverageStatus::MecUnaffordable | CoverageStatus::MecNonMinimumValue
        )
        && input.at_least_one_ft_received_ptc;

    let ft_minus_30 = input.full_time_employees.saturating_sub(30);
    let monthly_a_per_fte = section_4980h_a_annual_per_fte_cents / 12;
    let section_4980h_a_annual_penalty_cents = if section_4980h_a_engaged {
        monthly_a_per_fte
            .saturating_mul(ft_minus_30 as u64)
            .saturating_mul(input.months_in_violation as u64)
    } else {
        0
    };

    let monthly_b_per_employee = section_4980h_b_annual_per_employee_cents / 12;
    let section_4980h_b_annual_penalty_cents = if section_4980h_b_engaged {
        monthly_b_per_employee
            .saturating_mul(input.ft_employees_receiving_ptc as u64)
            .saturating_mul(input.months_in_violation as u64)
    } else {
        0
    };

    if !is_applicable_large_employer {
        let reason = if seasonal_worker_exception_engaged {
            "§ 4980H(c)(2)(D) SEASONAL WORKER EXCEPTION applies — workforce only crossed 50-FTE threshold because of seasonal workers employed 120 days or fewer; NOT ALE; no mandate"
        } else {
            "combined full-time + FTE-equivalent headcount BELOW 50-employee ALE threshold under § 4980H(c)(2); NOT ALE; no mandate"
        };
        failure_reasons.push(format!(
            "26 USC § 4980H(c)(2) — {} (combined FTE count: {})",
            reason, combined_fte_count
        ));
    }

    if section_4980h_a_engaged {
        failure_reasons.push(format!(
            "26 USC § 4980H(a) 'A PENALTY' ENGAGED — ALE failed to offer minimum essential coverage to at least 95% of full-time employees + dependents; at least one FT employee received § 36B premium tax credit; 2026 annual penalty = (FT - 30) × $3,340 × months/12 = ({} - 30) × $3,340 × {} months = {} cents annual; original 2014 amount $2,000 indexed under § 4980H(c)(5) CPI adjustment",
            input.full_time_employees,
            input.months_in_violation,
            section_4980h_a_annual_penalty_cents
        ));
    }

    if section_4980h_b_engaged {
        let coverage_failure = match input.coverage_status {
            CoverageStatus::MecUnaffordable => format!(
                "UNAFFORDABLE under § 4980H(c)(4) — employee contribution {}% of household income exceeds 2026 affordability threshold of 9.96%",
                input.employee_contribution_percent as f64 / 100.0
            ),
            CoverageStatus::MecNonMinimumValue => format!(
                "fails MINIMUM VALUE under § 36B(c)(2)(C)(ii) — plan covers only {}% of expected healthcare costs (below 60% threshold)",
                input.minimum_value_percent
            ),
            _ => "(none)".to_string(),
        };
        failure_reasons.push(format!(
            "26 USC § 4980H(b) 'B PENALTY' ENGAGED — ALE offers coverage that is {}; {} FT employees received § 36B premium tax credit; 2026 annual penalty = $5,010 × FT receiving PTC × months/12 = $5,010 × {} × {} months = {} cents annual; original 2014 amount $3,000 indexed under § 4980H(c)(5)",
            coverage_failure,
            input.ft_employees_receiving_ptc,
            input.ft_employees_receiving_ptc,
            input.months_in_violation,
            section_4980h_b_annual_penalty_cents
        ));
    }

    if !input.at_least_one_ft_received_ptc && is_applicable_large_employer {
        failure_reasons.push(
            "26 USC § 4980H — NO full-time employee certified as receiving § 36B premium tax credit; ESRP NOT TRIGGERED regardless of coverage offering status; PTC certification is essential prerequisite to penalty assessment".to_string(),
        );
    }

    if !input.section_6056_reporting_compliant && is_applicable_large_employer {
        failure_reasons.push(
            "26 USC § 6056 REPORTING — ALE failed to file Form 1094-C transmittal with IRS or furnish Form 1095-C to each full-time employee by January 31 of year following calendar year reported; § 6721/§ 6722 civil penalties up to $310 per return for 2026 ($630 per intentional failure); separate from § 4980H ESRP".to_string(),
        );
    }

    if input.controlled_group_aggregation {
        failure_reasons.push(
            "26 USC § 414(b)/(c)/(m)/(o) CONTROLLED GROUP AGGREGATION ENGAGED — related employers (parent-subsidiary, brother-sister, affiliated service group) treated as SINGLE EMPLOYER for ALE threshold determination; can push small business across 50-FTE line via related-employer headcount; EACH ENTITY in controlled group SEPARATELY ASSESSED ESRP penalty for its own full-time employees; cannot circumvent mandate by splitting workforce".to_string(),
        );
    }

    let notes: Vec<String> = vec![
        "26 USC § 4980H EMPLOYER SHARED RESPONSIBILITY PAYMENT (ESRP) — Patient Protection and Affordable Care Act of 2010 (Pub. L. 111-148); 'pay or play' mandate requiring Applicable Large Employer (ALE) to either offer minimum essential coverage to 95% of full-time employees OR pay penalty when FT employee receives § 36B premium tax credit for Exchange coverage".to_string(),
        "26 USC § 4980H(c)(2) APPLICABLE LARGE EMPLOYER (ALE) — employer that employed AVERAGE OF AT LEAST 50 FULL-TIME EMPLOYEES (including FTE-equivalents) on business days during preceding calendar year; § 4980H(c)(2)(C) FULL-TIME EMPLOYEE = 30+ hours/week (130/month); § 4980H(c)(2)(E) FTE-EQUIVALENT = aggregate part-time hours / 120; § 414(b)/(c)/(m)/(o) controlled group aggregation; § 4980H(c)(2)(D) seasonal worker 120-day-or-fewer exception".to_string(),
        "26 USC § 4980H(a) 'A PENALTY' — failure to offer MEC; triggered if (1) ALE fails to offer MEC to at least 95% of full-time employees + dependents for any month; AND (2) at least one full-time employee certified as receiving § 36B PTC; penalty = (FT - 30) × 1/12 of annual penalty × months in violation; 2026 ANNUAL PENALTY $3,340 per FT minus 30 (up from $2,900 for 2025); original 2014 $2,000 indexed under § 4980H(c)(5) CPI".to_string(),
        "26 USC § 4980H(b) 'B PENALTY' — unaffordable or non-MV coverage; triggered if (1) ALE offers coverage to 95%+ of full-time employees BUT (2) coverage is UNAFFORDABLE under § 4980H(c)(4) OR fails to provide MINIMUM VALUE under § 36B(c)(2)(C)(ii); AND (3) full-time employee certified as receiving PTC; penalty = per non-compliant FT employee × 1/12 of annual penalty × months; 2026 ANNUAL PENALTY $5,010 per employee receiving PTC (up from $4,350 for 2025); original 2014 $3,000 indexed".to_string(),
        "26 USC § 4980H(c)(4) AFFORDABILITY — employee contribution for self-only coverage cannot exceed affordability percentage of household income: 2026 = 9.96% (up from 9.02% for 2025); § 36B(c)(2)(C)(i) federal poverty line safe harbor; Rev. Proc. 2014-37 + IRS Notice 2015-87 three safe harbors: (1) W-2 wages box 1; (2) rate of pay; (3) federal poverty line".to_string(),
        "26 USC § 36B(c)(2)(C)(ii) MINIMUM VALUE — plan must cover at least 60% of expected healthcare costs; actuarial value calculation per Treas. Reg. § 1.36B-6; HHS minimum value calculator available; failure triggers § 4980H(b) B penalty".to_string(),
        "26 USC § 4980H(d) MINIMUM ESSENTIAL COVERAGE (MEC) — § 5000A(f) includes employer-sponsored group health plan + individual market plan + Medicare + Medicaid + CHIP + TRICARE + VA medical benefits + Peace Corps; NOT MEC: dental-only + vision-only + long-term care + disability income + workers' compensation + accident-only + on-site medical clinics".to_string(),
        "26 USC § 6056 REPORTING — Form 1094-C transmittal + Form 1095-C employee statement: (1) § 6056(a) annual return with IRS showing health coverage offered to each full-time employee; (2) § 6056(c) written statement to each full-time employee by January 31 of year following calendar year reported; (3) § 6721/§ 6722 civil penalties up to $310 per return for 2026 ($630 for intentional failures)".to_string(),
        "26 USC § 414(b)/(c)/(m)/(o) CONTROLLED GROUP AGGREGATION — related employers (parent-subsidiary, brother-sister, affiliated service group) treated as SINGLE EMPLOYER for ALE threshold determination; can push small business across 50-FTE line via related-employer headcount; EACH ENTITY in controlled group SEPARATELY ASSESSED ESRP penalty for its own full-time employees; cannot circumvent mandate by splitting workforce".to_string(),
        "26 USC § 4980H(c)(2)(D) SEASONAL WORKER EXCEPTION — employee employed FOR 120 DAYS OR FEWER in seasonal capacity NOT counted for ALE threshold; common seasonal occupations include agriculture, retail (holiday), summer camp, ski resort, tax preparation, landscaping; eliminates inadvertent ALE classification for seasonal businesses".to_string(),
        "Trader-landlord critical fact patterns: (1) Trader-LLC 200-unit portfolio with 60 maintenance staff + property managers — crosses 50 FTE ALE; fails MEC; one FT receives Exchange PTC — § 4980H(a) 2026 (60 - 30) × $3,340 = $100,200; (2) Trader-S-corp 12% W-2 contribution exceeds 9.96% — UNAFFORDABLE § 4980H(b); 3 PTC FTs × $5,010 = $15,030; (3) 4 related LLCs each with 20-25 employees aggregated under § 414 controlled group = single ALE; (4) 25 FT + 30 seasonal (120 days or fewer) — § 4980H(c)(2)(D) exception, NOT ALE; (5) ALE files Form 1094-C + 1095-C by January 31, failure triggers § 6722 penalty up to $310 per return".to_string(),
        "Companion to section_162m ($1M public-company exec comp deduction — iter 446) + section_409a (NQDC compliance) + section_415 (umbrella limits — iter 452) + section_4973 (excess contribution excise — iter 442) + section_280g (golden parachute change-in-control — iter 444)".to_string(),
    ];

    Section4980hResult {
        combined_fte_count,
        is_applicable_large_employer,
        seasonal_worker_exception_engaged,
        section_4980h_a_engaged,
        section_4980h_b_engaged,
        section_4980h_a_annual_penalty_cents,
        section_4980h_b_annual_penalty_cents,
        affordability_threshold_2026_percent,
        minimum_value_threshold_percent,
        section_6056_reporting_compliant: input.section_6056_reporting_compliant,
        failure_reasons,
        citation: "26 USC § 4980H(a); 26 USC § 4980H(b); 26 USC § 4980H(c)(2); 26 USC § 4980H(c)(2)(C); 26 USC § 4980H(c)(2)(D); 26 USC § 4980H(c)(2)(E); 26 USC § 4980H(c)(4); 26 USC § 4980H(c)(5); 26 USC § 4980H(d); 26 USC § 5000A(f); 26 USC § 36B(c)(2)(C)(i); 26 USC § 36B(c)(2)(C)(ii); 26 USC § 6056(a); 26 USC § 6056(c); 26 USC § 6721; 26 USC § 6722; 26 USC § 414(b); 26 USC § 414(c); 26 USC § 414(m); 26 USC § 414(o); Treas. Reg. § 54.4980H-1 through § 54.4980H-6; Treas. Reg. § 1.36B-6; Rev. Proc. 2014-37; IRS Notice 2015-87; Patient Protection and Affordable Care Act of 2010 (Pub. L. 111-148); Health Care and Education Reconciliation Act of 2010 (Pub. L. 111-152); Form 1094-C; Form 1095-C",
        notes,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn ale_no_coverage() -> Section4980hInput {
        Section4980hInput {
            full_time_employees: 60,
            fte_equivalents: 0,
            seasonal_workers_120_days_or_fewer: 0,
            coverage_status: CoverageStatus::NoMecOffered,
            at_least_one_ft_received_ptc: true,
            ft_employees_receiving_ptc: 1,
            months_in_violation: 12,
            employee_contribution_percent: 800,
            minimum_value_percent: 70,
            controlled_group_aggregation: false,
            section_6056_reporting_compliant: true,
        }
    }

    #[test]
    fn ale_threshold_50_engages() {
        let r = check(&ale_no_coverage());
        assert!(r.is_applicable_large_employer);
        assert_eq!(r.combined_fte_count, 60);
    }

    #[test]
    fn under_50_not_ale() {
        let mut i = ale_no_coverage();
        i.full_time_employees = 45;
        let r = check(&i);
        assert!(!r.is_applicable_large_employer);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("BELOW 50-employee ALE threshold")));
    }

    #[test]
    fn fte_equivalents_aggregate_to_threshold() {
        let mut i = ale_no_coverage();
        i.full_time_employees = 40;
        i.fte_equivalents = 12;
        let r = check(&i);
        assert!(r.is_applicable_large_employer);
        assert_eq!(r.combined_fte_count, 52);
    }

    #[test]
    fn seasonal_worker_exception_engaged() {
        let mut i = ale_no_coverage();
        i.full_time_employees = 60;
        i.seasonal_workers_120_days_or_fewer = 30;
        let r = check(&i);
        assert!(r.seasonal_worker_exception_engaged);
        assert!(!r.is_applicable_large_employer);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("§ 4980H(c)(2)(D) SEASONAL WORKER EXCEPTION")
                && f.contains("120 days or fewer")));
    }

    #[test]
    fn section_4980h_a_penalty_2026_calculation() {
        let r = check(&ale_no_coverage());
        assert!(r.section_4980h_a_engaged);
        let expected = (334_000_u64 / 12) * (60 - 30) * 12;
        assert_eq!(r.section_4980h_a_annual_penalty_cents, expected);
    }

    #[test]
    fn section_4980h_a_penalty_message_pinned() {
        let r = check(&ale_no_coverage());
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("§ 4980H(a) 'A PENALTY' ENGAGED")
                && f.contains("at least 95%")
                && f.contains("$3,340")
                && f.contains("(60 - 30)")));
    }

    #[test]
    fn section_4980h_b_unaffordable_engages() {
        let mut i = ale_no_coverage();
        i.coverage_status = CoverageStatus::MecUnaffordable;
        i.employee_contribution_percent = 1200;
        i.ft_employees_receiving_ptc = 3;
        let r = check(&i);
        assert!(r.section_4980h_b_engaged);
        let expected = (501_000_u64 / 12) * 3 * 12;
        assert_eq!(r.section_4980h_b_annual_penalty_cents, expected);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("§ 4980H(b) 'B PENALTY' ENGAGED")
                && f.contains("UNAFFORDABLE")
                && f.contains("9.96%")));
    }

    #[test]
    fn section_4980h_b_non_minimum_value_engages() {
        let mut i = ale_no_coverage();
        i.coverage_status = CoverageStatus::MecNonMinimumValue;
        i.minimum_value_percent = 50;
        i.ft_employees_receiving_ptc = 2;
        let r = check(&i);
        assert!(r.section_4980h_b_engaged);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("MINIMUM VALUE under § 36B(c)(2)(C)(ii)")
                && f.contains("60% threshold")));
    }

    #[test]
    fn compliant_offering_no_penalty() {
        let mut i = ale_no_coverage();
        i.coverage_status = CoverageStatus::CompliantOffering;
        let r = check(&i);
        assert!(!r.section_4980h_a_engaged);
        assert!(!r.section_4980h_b_engaged);
        assert_eq!(r.section_4980h_a_annual_penalty_cents, 0);
        assert_eq!(r.section_4980h_b_annual_penalty_cents, 0);
    }

    #[test]
    fn no_ptc_certification_no_penalty() {
        let mut i = ale_no_coverage();
        i.at_least_one_ft_received_ptc = false;
        let r = check(&i);
        assert!(!r.section_4980h_a_engaged);
        assert!(!r.section_4980h_b_engaged);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("NO full-time employee certified")
                && f.contains("§ 36B premium tax credit")
                && f.contains("essential prerequisite")));
    }

    #[test]
    fn section_6056_reporting_failure() {
        let mut i = ale_no_coverage();
        i.section_6056_reporting_compliant = false;
        let r = check(&i);
        assert!(!r.section_6056_reporting_compliant);
        assert!(r
            .failure_reasons
            .iter()
            .any(|f| f.contains("§ 6056 REPORTING")
                && f.contains("Form 1094-C")
                && f.contains("Form 1095-C")
                && f.contains("January 31")
                && f.contains("$310 per return")));
    }

    #[test]
    fn controlled_group_aggregation_engaged() {
        let mut i = ale_no_coverage();
        i.controlled_group_aggregation = true;
        let r = check(&i);
        assert!(r.failure_reasons.iter().any(|f| f
            .contains("§ 414(b)/(c)/(m)/(o) CONTROLLED GROUP AGGREGATION")
            && f.contains("SEPARATELY ASSESSED ESRP")));
    }

    #[test]
    fn fewer_than_30_ft_minimal_a_penalty() {
        let mut i = ale_no_coverage();
        i.full_time_employees = 25;
        i.fte_equivalents = 30;
        let r = check(&i);
        assert!(r.is_applicable_large_employer);
        if r.section_4980h_a_engaged {
            assert_eq!(r.section_4980h_a_annual_penalty_cents, 0);
        }
    }

    #[test]
    fn coverage_status_truth_table_four_cells() {
        for cov in [
            CoverageStatus::NoMecOffered,
            CoverageStatus::MecUnaffordable,
            CoverageStatus::MecNonMinimumValue,
            CoverageStatus::CompliantOffering,
        ] {
            let mut i = ale_no_coverage();
            i.coverage_status = cov;
            let r = check(&i);
            match cov {
                CoverageStatus::NoMecOffered => assert!(r.section_4980h_a_engaged, "cov={:?}", cov),
                CoverageStatus::MecUnaffordable | CoverageStatus::MecNonMinimumValue => {
                    assert!(r.section_4980h_b_engaged, "cov={:?}", cov)
                }
                CoverageStatus::CompliantOffering => {
                    assert!(!r.section_4980h_a_engaged);
                    assert!(!r.section_4980h_b_engaged);
                }
            }
        }
    }

    #[test]
    fn no_mec_uniquely_engages_a_penalty_invariant() {
        let no_mec = check(&ale_no_coverage());
        assert!(no_mec.section_4980h_a_engaged);
        assert!(!no_mec.section_4980h_b_engaged);

        let mut unaffordable = ale_no_coverage();
        unaffordable.coverage_status = CoverageStatus::MecUnaffordable;
        let r_un = check(&unaffordable);
        assert!(!r_un.section_4980h_a_engaged);
        assert!(r_un.section_4980h_b_engaged);
    }

    #[test]
    fn partial_year_months_proportional() {
        let mut i = ale_no_coverage();
        i.months_in_violation = 6;
        let r = check(&i);
        let expected = (334_000_u64 / 12) * (60 - 30) * 6;
        assert_eq!(r.section_4980h_a_annual_penalty_cents, expected);
    }

    #[test]
    fn affordability_threshold_2026_pinned() {
        let r = check(&ale_no_coverage());
        assert_eq!(r.affordability_threshold_2026_percent, 996);
    }

    #[test]
    fn minimum_value_threshold_60_percent_pinned() {
        let r = check(&ale_no_coverage());
        assert_eq!(r.minimum_value_threshold_percent, 60);
    }

    #[test]
    fn citation_pins_all_authorities() {
        let r = check(&ale_no_coverage());
        assert!(r.citation.contains("§ 4980H(a)"));
        assert!(r.citation.contains("§ 4980H(b)"));
        assert!(r.citation.contains("§ 4980H(c)(2)"));
        assert!(r.citation.contains("§ 4980H(c)(2)(C)"));
        assert!(r.citation.contains("§ 4980H(c)(2)(D)"));
        assert!(r.citation.contains("§ 4980H(c)(2)(E)"));
        assert!(r.citation.contains("§ 4980H(c)(4)"));
        assert!(r.citation.contains("§ 4980H(c)(5)"));
        assert!(r.citation.contains("§ 4980H(d)"));
        assert!(r.citation.contains("§ 5000A(f)"));
        assert!(r.citation.contains("§ 36B(c)(2)(C)(i)"));
        assert!(r.citation.contains("§ 36B(c)(2)(C)(ii)"));
        assert!(r.citation.contains("§ 6056(a)"));
        assert!(r.citation.contains("§ 6056(c)"));
        assert!(r.citation.contains("§ 6721"));
        assert!(r.citation.contains("§ 6722"));
        assert!(r.citation.contains("§ 414(b)"));
        assert!(r.citation.contains("§ 414(c)"));
        assert!(r.citation.contains("§ 414(m)"));
        assert!(r.citation.contains("§ 414(o)"));
        assert!(r.citation.contains("Treas. Reg. § 54.4980H-1"));
        assert!(r.citation.contains("Treas. Reg. § 1.36B-6"));
        assert!(r.citation.contains("Rev. Proc. 2014-37"));
        assert!(r.citation.contains("IRS Notice 2015-87"));
        assert!(r.citation.contains("Pub. L. 111-148"));
        assert!(r.citation.contains("Pub. L. 111-152"));
        assert!(r.citation.contains("Form 1094-C"));
        assert!(r.citation.contains("Form 1095-C"));
    }

    #[test]
    fn note_pins_esrp_pay_or_play_framework() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("EMPLOYER SHARED RESPONSIBILITY PAYMENT")
                && n.contains("Pub. L. 111-148")
                && n.contains("pay or play")));
    }

    #[test]
    fn note_pins_subsection_c2_ale_definition() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("§ 4980H(c)(2) APPLICABLE LARGE EMPLOYER")
                && n.contains("50 FULL-TIME EMPLOYEES")
                && n.contains("30+ hours/week")
                && n.contains("130/month")
                && n.contains("§ 414")));
    }

    #[test]
    fn note_pins_subsection_a_2026_penalty() {
        let r = check(&ale_no_coverage());
        assert!(r.notes.iter().any(|n| n.contains("§ 4980H(a) 'A PENALTY'")
            && n.contains("$3,340 per FT minus 30")
            && n.contains("$2,000 indexed")
            && n.contains("§ 4980H(c)(5) CPI")));
    }

    #[test]
    fn note_pins_subsection_b_2026_penalty() {
        let r = check(&ale_no_coverage());
        assert!(r.notes.iter().any(|n| n.contains("§ 4980H(b) 'B PENALTY'")
            && n.contains("$5,010 per employee receiving PTC")
            && n.contains("$3,000 indexed")));
    }

    #[test]
    fn note_pins_subsection_c4_affordability() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("§ 4980H(c)(4) AFFORDABILITY")
                && n.contains("9.96%")
                && n.contains("9.02% for 2025")
                && n.contains("three safe harbors")));
    }

    #[test]
    fn note_pins_section_36b_minimum_value() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("§ 36B(c)(2)(C)(ii) MINIMUM VALUE")
                && n.contains("60% of expected healthcare costs")
                && n.contains("Treas. Reg. § 1.36B-6")));
    }

    #[test]
    fn note_pins_subsection_d_mec_inclusions_and_exclusions() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("§ 4980H(d) MINIMUM ESSENTIAL COVERAGE")
                && n.contains("Medicare")
                && n.contains("Medicaid")
                && n.contains("TRICARE")
                && n.contains("NOT MEC")
                && n.contains("dental-only")));
    }

    #[test]
    fn note_pins_section_6056_form_1094_1095() {
        let r = check(&ale_no_coverage());
        assert!(r.notes.iter().any(|n| n.contains("§ 6056 REPORTING")
            && n.contains("Form 1094-C")
            && n.contains("Form 1095-C")
            && n.contains("January 31")
            && n.contains("§ 6721/§ 6722")));
    }

    #[test]
    fn note_pins_controlled_group_aggregation_section_414() {
        let r = check(&ale_no_coverage());
        assert!(r.notes.iter().any(|n| n
            .contains("§ 414(b)/(c)/(m)/(o) CONTROLLED GROUP AGGREGATION")
            && n.contains("parent-subsidiary")
            && n.contains("affiliated service group")
            && n.contains("SEPARATELY ASSESSED ESRP")));
    }

    #[test]
    fn note_pins_seasonal_worker_120_day_exception() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("§ 4980H(c)(2)(D) SEASONAL WORKER EXCEPTION")
                && n.contains("120 DAYS OR FEWER")
                && n.contains("seasonal occupations")
                && n.contains("agriculture")));
    }

    #[test]
    fn note_pins_trader_fact_patterns_five() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("Trader-landlord critical fact patterns")
                && n.contains("$100,200")
                && n.contains("12% W-2 contribution")
                && n.contains("$15,030")
                && n.contains("controlled group")
                && n.contains("§ 4980H(c)(2)(D) exception")
                && n.contains("§ 6722")));
    }

    #[test]
    fn note_pins_companion_modules() {
        let r = check(&ale_no_coverage());
        assert!(r
            .notes
            .iter()
            .any(|n| n.contains("Companion to section_162m")
                && n.contains("section_409a")
                && n.contains("section_415")
                && n.contains("section_280g")));
    }

    #[test]
    fn defensive_overflow_saturating() {
        let mut i = ale_no_coverage();
        i.full_time_employees = u32::MAX;
        i.months_in_violation = u32::MAX;
        let r = check(&i);
        let _ = r.section_4980h_a_annual_penalty_cents;
    }
}
