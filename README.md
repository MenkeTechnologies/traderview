```
 ████████╗██████╗  █████╗ ██████╗ ███████╗██████╗ ██╗   ██╗██╗███████╗██╗    ██╗
 ╚══██╔══╝██╔══██╗██╔══██╗██╔══██╗██╔════╝██╔══██╗██║   ██║██║██╔════╝██║    ██║
    ██║   ██████╔╝███████║██║  ██║█████╗  ██████╔╝██║   ██║██║█████╗  ██║ █╗ ██║
    ██║   ██╔══██╗██╔══██║██║  ██║██╔══╝  ██╔══██╗╚██╗ ██╔╝██║██╔══╝  ██║███╗██║
    ██║   ██║  ██║██║  ██║██████╔╝███████╗██║  ██║ ╚████╔╝ ██║███████╗╚███╔███╔╝
    ╚═╝   ╚═╝  ╚═╝╚═╝  ╚═╝╚═════╝ ╚══════╝╚═╝  ╚═╝  ╚═══╝  ╚═╝╚══════╝ ╚══╝╚══╝
```

[![Tauri](https://img.shields.io/badge/tauri-v2-05d9e8.svg)](https://tauri.app)
[![Axum](https://img.shields.io/badge/axum-0.7-ff2a6d.svg)](https://github.com/tokio-rs/axum)
[![Postgres](https://img.shields.io/badge/postgres-embedded%20%2B%20external-d300c5.svg)](https://www.postgresql.org/)
[![Brokers](https://img.shields.io/badge/brokers-12_importers-ff2a6d.svg)](#0x0a-importing-trades)
[![Reports](https://img.shields.io/badge/reports-20+_TraderVue_parity-d300c5.svg)](#0x0c-status)
[![Asset classes](https://img.shields.io/badge/assets-stocks_options_futures_forex-39ff14.svg)](#0x0c-status)
[![Schedule C](https://img.shields.io/badge/schedule_C-business_expenses_%2B_receipts-39ff14.svg)](#0x0aa-business-expenses-schedule-c)
[![Crates](https://img.shields.io/badge/crates-7-39ff14.svg)](#0x03-crate-graph)
[![Docs](https://img.shields.io/badge/docs-online-05d9e8.svg)](https://menketechnologies.github.io/traderview/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

### `[TRADERVUE-STYLE TRADING JOURNAL // FULL FEATURE PARITY // SELF-HOSTED]`

> *"One workspace. Desktop with embedded Postgres. Multi-user web with axum. Same crates, same UI, same FIFO roll-up."*
>
> *"Executions are the atom. Trades are FIFO-derived. The journal is markdown."*

`traderview` is a TraderVue-style trading journal: import broker CSV → atomic execution rows → FIFO trade roll-up → equity curve + summary stats + per-trade / per-day markdown journal. **One Rust workspace ships two binaries** — a Tauri v2 desktop app that downloads and runs an embedded Postgres on first launch (single-user, auto-login), and an axum web server that talks to an external Postgres (multi-user, JWT auth, registration). Both binaries import the same `traderview-{core,db,import}` library crates; the frontend (vanilla JS + uPlot) is shared verbatim between them. By [MenkeTechnologies](https://github.com/MenkeTechnologies).

### [`Read the Docs`](https://menketechnologies.github.io/traderview/) &middot; [`Engineering Report`](https://menketechnologies.github.io/traderview/report.html) · [`Source`](https://github.com/MenkeTechnologies/traderview) · [`strykelang`](https://github.com/MenkeTechnologies/strykelang) · [`zshrs`](https://github.com/MenkeTechnologies/zshrs)

---

## Table of Contents

- [\[0x00\] Overview](#0x00-overview)
- [\[0x01\] Architecture](#0x01-architecture)
- [\[0x02\] Dual Deploy Targets](#0x02-dual-deploy-targets)
- [\[0x03\] Crate Graph](#0x03-crate-graph)
- [\[0x04\] Schema](#0x04-schema)
- [\[0x05\] HTTP API](#0x05-http-api)
- [\[0x06\] Frontend](#0x06-frontend)
- [\[0x07\] Installation](#0x07-installation)
- [\[0x08\] Running — Desktop](#0x08-running--desktop)
- [\[0x09\] Running — Web](#0x09-running--web)
- [\[0x0A\] Importing Trades](#0x0a-importing-trades)
- [\[0x0AA\] Business Expenses (Schedule C)](#0x0aa-business-expenses-schedule-c)
- [\[0x0AB\] Landlord / Rental Property (Schedule E)](#0x0ab-landlord--rental-property-schedule-e)
- [\[0x0B\] Configuration](#0x0b-configuration)
- [\[0x0C\] Status](#0x0c-status)
- [\[0xFF\] License](#0xff-license)

---

## [0x00] OVERVIEW

- **Full desktop trading suite** — replaces TraderVue ($30/mo journal) + DayTradeDash ($187/mo Warrior Trading scanner) + StockInvest.us in one self-hosted binary. **$2,604/yr saved**, data stays on your machine.
- **One workspace, two binaries** — `traderview-desktop` (Tauri v2 + embedded Postgres) and `server` (axum + external Postgres) both depend on six shared library crates. No code is duplicated between desktop and web.
- **Executions are the atom** — every broker fill is one row in `executions`. Trades are FIFO-derived from those rows and materialized into `trades` for fast UI queries. Re-running the roll-up is always safe.
- **FIFO trade roll-up** — `traderview-core::rollup` matches buy/sell pairs in first-in-first-out order per `(account_id, symbol)`. Open positions stay in `status='open'`; fully-closed positions get `gross_pnl`, `exit_avg`, `closed_at`.
- **Embedded Postgres on the desktop** — `postgresql_embedded` downloads a portable PostgreSQL on first launch (~80 MB, cached in `~/.theseus`), stores data under `$APP_DATA_DIR/traderview/pg/`, and shuts it down cleanly on app exit. Stale-PID lockfile cleanup survives a hard-killed parent. Zero external dependencies for desktop users.
- **Multi-user web on the same crates** — the axum binary swaps the embedded pool for an external `DATABASE_URL`, layers in argon2 password hashing + JWT bearer auth, and serves the same vanilla-JS frontend.
- **Tile launcher (Cmd-K)** — categorized tile grid with live filter replaces the old 77-tab strip. Press `?` anywhere for the in-app tutorial. Topbar carries the most-used routes.
- **Right-click context menus everywhere** — every view registers a `data-context-scope` slug that the global ctxmenu handler resolves on right-click. Per-row scopes (`trade-row`, `symbol-row`, `journal-entry`, `tag-chip`, `webhook-row`, `api-token-row`, `account-row`, `plan-row`, `share-row`, `board-row`, `dashboard-sidebar-item`, `hotkey-row`, `custom-indicator-row`, `alert-rule-row`, `strategy-alert-row`, `watchlist-symbol-row`, `position-row`, `backtest-preset-row`, `budget-row`, `tax-wiz-section`, `categorize-group`, `favorites-manager`, `bookmarks-manager` — 23 scopes) plus 141 symbol-aware view scopes give 846 distinct symbol-nav paths to the active symbol (Charts / Options / Research / Earnings / News / Copy) without leaving the current view. Items are registered in a single `ALL_SCOPED_ITEMS` array; handler bodies are 3-4 LoC each via 6 shared helpers (`clipboardWrite` / `refreshView` / `dataFromTarget` / `toastErr` / `toastOk` / `symbolFromTarget`). Two regression tests pin (a) every emitting tag carries the data-* attrs its handler reads and (b) every registered scope is documented in the audit's required-attrs map.
- **283-shortcut keyboard surface** — `_shortcuts.js` registry: 57 globals (quick-nav `Cmd+Opt+T/J/D/W/C/L/R/M` for Trades/Journal/Dashboard/Watchlists/Charts/Live/Reports/Scanner, and more) + 226 view-scoped binds (`n` for new-trade in trades scope, `r` to refresh in dashboard/live/trades/journal/watchlists/webull/charts, etc.). Every view-scoped bind has a visible `⟳ Refresh` / `+ New` button stamped with `data-shortcut`, so the tooltip augmenter appends `(⌘⌥T)` / `(R)` chips on hover. The cheat-sheet view (`?` or Cmd+K → "Keyboard Shortcuts") lists all 57 globals + the 70+ scoped ctxmenu items in three searchable tables.
- **Non-blocking dialog primitive** — `tConfirm()` / `tPrompt()` (in `dialog.js`) replace every `alert()` / `confirm()` / `prompt()` call site (63 + 37 = 100 swept across 42 view files). Returns a Promise; themed by level (`info` / `warning` / `danger`), Enter/Esc keybound, required-empty input shake, i18n labels via `dialog.btn.*` keys.
- **Vanilla JS + uPlot frontend** — zero npm, zero bundler, zero framework. 765 view modules + pure helper modules + runtime modules across ~225k LOC JS + 4,898 LOC CSS. Per-view race-token machinery prevents post-await DOM crashes; window.onerror + console.error funnel to a Rust-side `/api/client-errors` sink.
- **12 broker importers + Generic CSV wizard** — Webull, Lightspeed, IBKR Flex, ThinkOrSwim, TD Ameritrade, Schwab, Fidelity, ETrade, Robinhood, TradeStation, DAS Trader, TradeZero, plus a column-mapping Generic parser for anything else.
- **17 reports + R-multiple + Monte Carlo forecast + fill-quality TCA + tax-lot tracker** with Schedule-D export.
- **stryke-JIT backtest engine + walk-forward sweeper + custom-indicator AST + strategy alerts (AND/OR/NOT) + webhooks** (Discord / Slack / generic).
- **Expense tracker with on-device receipt OCR + tax dashboard** — `traderview-expense` parses Amazon / BoA / Chase / Apple Card statements; `traderview-ocr` extracts merchant / total / date / itemized lines via Apple Vision (macOS) or Tesseract fallback, then categorizes against a 20-bucket Schedule C taxonomy. Merchant canonicalization, learned-category mappings, bulk re-OCR, full-text search, duplicate detection, top-merchants rollup — no LLMs, no paid APIs, $0 / receipt.
- **MIT licensed**, single-author, single-language workspace.

---

## [0x01] ARCHITECTURE

```
                    ┌──────────────────────────────────────┐
                    │  frontend/  (vanilla JS + uPlot)     │
                    │  dashboard / trades / journal / ...  │
                    └──────────────┬───────────────────────┘
                                   │ HTTP /api/*
                  ┌────────────────┴────────────────┐
                  │                                 │
        ┌─────────▼──────────────┐     ┌────────────▼─────────────┐
        │  src-tauri             │     │  traderview-web (axum)   │
        │  (traderview-desktop)  │     │  bin: server             │
        │  embedded postgres     │     │  external postgres       │
        │  auto-login local user │     │  argon2 + JWT auth       │
        └─────────┬──────────────┘     └────────────┬─────────────┘
                  │                                 │
                  └────────────────┬────────────────┘
                                   │
                  ┌────────────────▼─────────────────┐
                  │  traderview-{core, db, import}   │
                  │  shared library crates           │
                  └──────────────────────────────────┘
```

The desktop and web binaries are thin shells. All domain logic, all SQL, all broker parsing lives in the three library crates. The decision *"embedded vs external Postgres"* and *"local auto-user vs multi-user with auth"* is the only thing that distinguishes them.

---

## [0x02] DUAL DEPLOY TARGETS

| Mode    | Binary                 | Postgres            | Auth                          | Audience                |
|---------|------------------------|---------------------|-------------------------------|-------------------------|
| Desktop | `traderview-desktop`   | embedded (`theseus`)| auto-login `local` user       | single user, offline    |
| Web     | `server`               | external (`DATABASE_URL`) | argon2 + JWT bearer     | multi-user, hosted      |

Same schema, same migrations, same FIFO roll-up, same frontend, same API surface. Swap the pool + auth layer and the rest of the stack is identical.

---

## [0x03] CRATE GRAPH

| Crate                        | Lines  | Purpose                                                                       |
|------------------------------|--------|-------------------------------------------------------------------------------|
| `traderview-core`            | 181,909 | Domain types, FIFO roll-up + tests, per-asset P&L, statistics (R-multiple / SQN / Sharpe / Sortino / expectancy), Kelly + correlation-aware position sizing, Monte Carlo equity forecaster, stryke-JIT backtest engine + walk-forward sweeper, sentiment scoring, custom-indicator AST. |
| `traderview-db`              | 69,856 | ~50 repo modules — trades / executions / tags / journal / screenshots / imports / mentorships / shares / forum / settings / plans / users / watchlists / alerts / hotkeys / paper / disclosures / catalysts / halts / live_ticks / markets / premarket / earnings / news / strategy alerts / rebalance / goals / reviews / custom indicators. sqlx pool + 74 migrations + embedded PG lifecycle (stale-PID cleanup, persisted password). Background pollers for Yahoo / FINRA / EDGAR / Nasdaq RSS / Finnhub WS / Reddit WSB / StockTwits / CoinGecko. Bounded in-memory stores. |
| `traderview-import`          | 2,212  | Generic ColumnMap CSV parser + 12 broker presets — Webull, Lightspeed, IBKR Flex, ThinkOrSwim, TD Ameritrade, Schwab, Fidelity, ETrade, Robinhood, TradeStation, DAS Trader, TradeZero. |
| `traderview-expense`         | 409,494 | Schedule C business-expense parsers (Amazon, BoA, Chase, Apple Card — CSV / XLSX / PDF via `calamine` + `lopdf`), merchant→category rule engine + seed, cross-account transfer dedup. |
| `traderview-ocr`             | ~2.3k  | Receipt + tax-form OCR. Engine ensemble: Apple Vision sidecar (macOS) + Tesseract psm4/psm6 + optional PaddleOCR (via `--features paddle`), structured fields fused per-field. PDF text-layer extraction (lopdf). 20-category Schedule C taxonomy. **W-2 / 1099-NEC / 1099-MISC / 1099-INT / 1099-DIV / 1099-K / 1099-B / 1098 parser** with title detection + label-proximity box extraction. |
| `traderview-tax`             | ~1.0k  | US federal tax compute engine — 2025 brackets, std deduction, SE tax, QBI § 199A, CTC/ODC phase-out, std-vs-itemized comparator. 31 unit tests pinning the math to IRS Rev. Proc. 2024-40. Zero deps beyond `rust_decimal` + `serde`. |
| `traderview-web`             | 77,178 | axum 0.7 router — **2,230 routes** across 174 route files (auth, accounts, trades, executions, tags, journal + AI, screenshots, imports + CSV wizard, 17 reports, mentorships, shares, comments, forum, charts/bars, settings, plans, hotkeys, watchlists, alerts + strategy alerts, paper, options, vol, breadth, fear-greed, sector rotation, sentiment, disclosures, catalysts WS, halts WS, live-ticks WS, webull WS, premarket, markets, news, earnings, custom indicators, backtest + walk-forward, rebalance, goals, reviews, expenses + receipts + Schedule C, dashboards, API tokens, webhooks, client-errors sink). Custom logging middleware (`log_mw`) records every request with elapsed_ms; 4xx/5xx attaches a 4KB body snippet. |
| `src-tauri` (`traderview-desktop`) | 494    | Tauri v2 shell — spawns embedded Postgres + axum on localhost. Worker-thread bring-up, native-dialog on failure, `tracing-appender` non-blocking file log + panic hook, `Embedded` held across `axum::serve` so Postgres can't be dropped mid-request. |

**Dependency direction** is one-way: `desktop` depends on `db + web`. `web` depends on `core + db + import + expense + ocr`. `import`, `expense`, `db` all depend on `core`. Nothing depends on `desktop`.

---

## [0x04] SCHEMA

38 migrations from `0001_initial.sql` through `0038_trade_commissions.sql` define **83 tables, 115 indexes, 24 PostgreSQL enum types**. Each migration adds a self-contained feature; the schema grows by feature, not by big-bang. Money is `NUMERIC(20, 8)` everywhere — no floats. Grouped by domain:

| Domain                     | Tables                                                                 |
|----------------------------|------------------------------------------------------------------------|
| Identity & accounts        | `users`, `accounts`, `api_tokens`, `mentorships`                       |
| Executions & trades        | `executions`, `trades`, `trade_executions`, `trade_tags`, `tags`, `imports` |
| Journal                    | `journal_entries`, `note_templates`, `trade_reviews`, `chart_drawings`, `screenshots` |
| Plans / goals / discipline | `plans`, `trading_goals`, `goal_progress`, `discipline_violations`     |
| Price data & quotes        | `bars`, `quote_snapshots`, `news_items`, `earnings_events`, `dividends` |
| Live feeds                 | `halts`, `catalysts`, `mentions` (sentiment), `tick_snapshots`         |
| Watchlists & screening     | `watchlists`, `watchlist_symbols`, `filter_sets`, `screener_snapshots` (12h background snapshots + shape-flip diffs) |
| Alerts / webhooks / hotkeys | `alerts`, `strategy_alerts`, `strategy_alert_fires`, `hotkeys`, `webhooks`, `webhook_deliveries`, `disclosures_watchers` |
| Backtest & strategy        | `backtest_runs`, `backtest_presets`, `walk_forward_runs`, `custom_indicators` |
| Paper trading              | `paper_accounts`, `paper_orders`, `paper_positions`, `paper_parent_orders` (TWAP/VWAP/POV slicing), `paper_equity_snapshots` (15min background equity sampling, change-compressed), `paper_dividends` (ex-date cash credits, one row per account × symbol × ex-date), `paper_splits` (value-preserving split adjustments, one row per account × symbol × split date) |
| Portfolio / risk           | `rebalance_targets`, `rebalance_runs`, `tax_lots`                      |
| Disclosures                | `disclosures` (Form 4, 13D/G, Senate / House STOCK Act)                |
| Institutional 13F          | `institutional_managers`, `institutional_13f_filings`, `institutional_holdings` + 2 views (`institutional_latest_filings`, `institutional_position_changes`) |
| Community                  | `shares`, `shared_comments`, `forum_categories`, `forum_threads`, `forum_posts`, `boards`, `board_items` |
| Settings & AI              | `user_settings`, `ai_settings`, `ai_journal_cache`, `dashboards`       |
| Expenses + OCR             | `expense_accounts`, `expense_categories`, `expense_transactions`, `expense_rules`, `expense_receipts` |

Sides are typed enums: `side_t = (buy, sell, short, cover)` for executions; `trade_side_t = (long, short)` and `trade_status_t = (open, closed)` for trades. Other enums cover order status, review status, asset class, alert triggers, sentiment sources, halt reason codes, etc.

---

## [0x05] HTTP API

**2,230 axum routes** under `/api/` across 174 route files. Bearer-auth required on everything except `/health`, `/config`, `/auth/*`, and `/client-errors`. Four WebSocket endpoints expose live feeds. Frontend bindings live in `frontend/js/api.js`. Grouped:

| Group                      | Endpoints | Examples                                                          |
|----------------------------|-----------|-------------------------------------------------------------------|
| Auth + config              | ~6        | `GET /config`, `GET /auth/me`, `POST /auth/login`, `POST /auth/register` |
| Trades + executions        | ~20       | `GET/POST /trades`, `POST /trades/rollup`, `POST /trades/merge`, `POST /trades/bulk`, `GET/POST /executions` |
| Journal + AI + reviews     | ~15       | `GET /journal/day/{day}`, `POST /journal-ai/{id}/analyze`, `GET /trade-reviews/needed/{acct}` |
| Reports (17 cuts)          | ~20       | `/reports/{overview, by-symbol, by-day-of-week, by-hour, by-hold, r-distribution, comparison, exit-efficiency, liquidity, drawdown, risk-adjusted, calendar, …}` |
| **Live streams (WS)**      | 13        | `WS /ws/halts`, `WS /ws/catalysts`, `WS /ws/ticks`, `WS /ws/webull`, `WS /ws/after-hours`, `WS /ws/catalyst-correlations`, `WS /ws/uoa-stream`, `WS /ws/gamma-squeeze`, `WS /ws/breadth-divergence`, `WS /ws/rvol-accel`, `WS /ws/insider-stream`, `WS /ws/sentiment-velocity`, `WS /ws/confluence` |
| Research per-symbol        | ~10       | `/symbols/{sym}/{quote, signals, news, earnings, dividends, recommendations, insiders, fundamentals, holders}` |
| Chart transformations      | 14        | `GET /bars/{sym}`, `/bars/{sym}/heikin-ashi`, `/renko`, `/volume-profile`, `/ichimoku`, `/fibonacci`, `/supertrend`, `/swing-points`, `/candlestick-patterns`, `/pivots/{floor,camarilla,woodie,demark}`, `/wolfe-wave` (5-pivot channel pattern over confirmed swings, EPA target per event) — rendered on the trading chart as numbered pivot markers with the EPA price labeled at pivot 5 (last 3 events; older patterns are chart noise) |
| Technical indicators       | 34        | `GET /bars/{sym}/{sma, ema, rsi, macd, bollinger, atr, roc, trix, dpo, coppock, schaff-trend, mass-index, adx, stochastic, williams-r, cci, mfi, donchian, parabolic-sar, anchored-vwap, aroon, awesome-oscillator, vortex, chaikin-volatility, obv, accumulation-distribution, force-index, keltner, vwap-bands, bb-squeeze, rsi-divergence, trend-channel, coppock-rsi, anchored-obv}` — each takes `?interval&from&to&period[&...]` |
| Screener + scanners        | ~4        | `GET /screener/run`, `GET /screener/top`, `GET /scans/run` (24 presets) |
| Options + analytics        | 10        | `/options/{sym}`, `/options/{sym}/{max-pain, gex, iv-skew}`, `/greeks`, `/vol-surface/{sym}`, `/iv/scan`, `/iv/symbols/{sym}` |
| Stateless calculators      | 137       | `POST /calc/{kelly, dynamic-kelly, optimal-f, var-historical, var-gaussian, monte-carlo, risk-parity, risk-on-off, margin-call, margin-runway, buying-power, tax-loss-harvest, tax-aware-rebalance, savings-waterfall, house-hacking, brrrr, paycheck-401k, guyton-klinger, irmaa, break-even, lease-generator, lease-renewal, invoice-generator, landlord-notice, security-deposit-itemization, promissory-note, rent-increase-notice, demand-for-payment, bill-of-sale, fix-and-flip, cash-conversion-cycle, profit-first, markup-margin, inventory-eoq, rent-vs-sell, depreciation-recapture, like-kind-exchange, cost-of-hire, invoice-factoring, ltv-cac, burn-rate, qlac, spousal-ira, pension-survivor, ss-pia, hsa-triple-tax, age-allocation, roth-bracket-fill, mortgage-points, apr-apy, blended-debt, dividend-coverage, spia, debt-yield, price-to-rent, years-to-fi, grm, seller-financing, expense-drag, lease-payment, real-return, cd-penalty, yield-on-cost, trade-expectancy, wage-converter, sales-tax, accrued-interest, stock-split, tbill-yield, dscr, graham-number, take-home-paycheck, ev-ebitda, holding-period-return, altman-z-score, piotroski-f-score, gmroi, roth-contribution, interest-coverage, capital-gains-tax, traditional-ira-deduction, rule-of-40, wacc, dupont-roe, ss-taxation, npv-irr, leverage, two-asset-portfolio, mortgage-recast, tax-equivalent-yield, pmi-removal, free-cash-flow, credit-card-payoff, bond-pricing, cash-out-refinance, margin-analysis, bonus-grossup, rent-escalation, loan-apr, home-sale-exclusion, life-insurance-needs, car-affordability, disability-insurance-needs, true-hourly-wage, property-tax, rental-noi, mortgage-affordability, overtime-pay, solar-payback, portfolio-longevity, second-income, breakeven-occupancy, rent-affordability, real-raise, sde-valuation, freelance-rate, preferred-stock, wash-sale, cost-basis, commission-optimizer, yield-curve, bond-duration, carry-score, currency-exposure, vix-term-structure, fx-carry, margin-interest, qbi-deduction, estate-tax, marriage-penalty, standard-vs-itemized, capture-ratio, rental-total-return, economic-value-added, mirr, equivalent-annual-cost, multi-product-breakeven}` — `tax-aware-rebalance` trades toward target weights but picks sell lots (HIFO to minimize gain, MaxLossHarvest to realize losses) via the shared `tax_lot_optimizer`, leaves drift within a no-trade band alone, and reports the realized gain + estimated tax the rebalance triggers; `savings-waterfall` is the financial order of operations — it allocates a month's savings down the priority ladder (starter buffer, employer match, high-interest debt, full emergency fund, HSA/Roth, max retirement, then taxable), each rung capped by its gap; `house-hacking` nets a small multi-unit's rental income against the full carrying cost (P&I via the shared amortization formula + tax/insurance/maintenance/HOA) to show net housing cost, savings vs renting, and the standalone cash flow once you move out and rent every unit; `brrrr` nets a cash-out refinance (ARV × LTV, P&I via the same amortization) against total cash invested to report the cash recovered, cash left in the deal, post-refi cash flow, and cash-on-cash (infinite when all cash is pulled back out); `paycheck-401k` gives the even per-paycheck deferral to hit the annual limit by year-end and quantifies the employer match forfeited by front-loading (hitting the limit early idles later checks → no match on them, unless the plan trues up); `guyton-klinger` is the dynamic-withdrawal decision for one retirement year — it applies the inflation raise (frozen after a down year), then cuts 10% if the withdrawal rate breaches the upper guardrail (initial × 1.2) or raises 10% below the lower (initial × 0.8), the decumulation companion to the accumulation-side FIRE calculators; `irmaa` maps a two-year-prior MAGI and filing status (single / MFJ / MFS) to its 2026 Medicare Part B/D income-surcharge tier — the surcharge is a cliff (one dollar over a threshold owes the full higher tier all year), so it reports the tier (0–5), the per-person and household annual surcharge, this tier's MAGI band, and the headroom to the next cliff, the planning counterpart to Roth conversions and RMDs that push MAGI into a higher bracket; `break-even` is cost-volume-profit analysis for a small business — each unit's contribution margin (price − variable cost) covers fixed costs, so it reports break-even units and revenue, the contribution-margin ratio, the volume needed for a target profit, and the margin of safety + operating profit at an expected sales level, flagging the no-break-even case when the contribution margin is zero or negative (every unit loses money before fixed costs); `lease-generator` assembles a residential lease agreement from the parties, premises, rent, deposits, dates, and policies — it computes the term in months, prorates the first month for a mid-month move-in (rent × days occupied ÷ days in month), totals the cash due at move-in (first month + security + pet deposit + optional last month), and returns the document as an ordered list of titled clauses the UI renders as a printable/copyable lease (drafting aid, not legal advice; deposit caps and disclosures vary by state); `invoice-generator` builds a business invoice from line items — each line's amount (qty × unit price), the subtotal, an optional discount off the subtotal, tax on the discounted subtotal, and the total, all rounded to cents so the figures reconcile, plus the due date from net-N payment terms; the UI renders an editable line-item table and a printable/copyable invoice; `landlord-notice` generates jurisdiction-agnostic landlord-tenant notices — a Pay Rent or Quit (nonpayment) notice and a Notice to Quit (termination) — in generic language that names whatever state the caller supplies (no jurisdiction baked in), with an optional statute citation included verbatim and the comply-by date computed from the service date + notice period; returned as titled clauses the UI renders printable/copyable with a live inline preview that updates as the form is filled (drafting aid, not legal advice; does not replace any official court form a jurisdiction requires); `fix-and-flip` underwrites a house flip — the 70% rule max allowable offer (ARV × rule% − rehab) and a full deal P&L that nets the sale proceeds (ARV less selling costs) against every cost (purchase, rehab, closing, monthly carry over the hold, loan points + interest) to report net profit, profit margin, cash invested (all-in cost less the loan), cash-on-cash, and annualized ROI; distinct from `brrrr`, which refinances and holds as a rental; `cash-conversion-cycle` measures the days a dollar is tied up in operations — DSO (receivables ÷ revenue × period) + DIO (inventory ÷ COGS × period) − DPO (payables ÷ COGS × period), reporting each component, the operating cycle (DSO + DIO), and the CCC, flagging a negative cycle as self-financing (you collect before you pay); `profit-first` implements Michalowicz's Profit First method — it nets materials/subcontractors off revenue to get real revenue, then splits it across Profit / Owner's Pay / Tax / OpEx by the target allocation band for the revenue size (5/50/15/30 under $250K up to 25/5/15/55 at $5M+), or by custom percentages when the four sum to 100; the business-side analog of the personal `savings-waterfall`; `markup-margin` converts between markup (profit ÷ cost) and margin (profit ÷ price) — given cost and any one of price, markup%, or margin% it returns all four plus per-unit profit, flagging the infeasible margin ≥ 100% case (the conversion small businesses routinely get wrong); `inventory-eoq` is the Wilson economic-order-quantity model — EOQ = √(2·D·S/H) from annual demand, cost per order, and holding cost per unit-year, with the order cadence (orders/year, days between), the ordering vs holding cost split (equal at the EOQ), and the reorder point (daily demand × lead time + safety stock); `rent-vs-sell` decides whether to keep a property as a rental or sell and invest the proceeds — it compares end-of-horizon wealth from selling now (net of costs/mortgage/cap-gains, compounded at an alternative return) against holding (appreciation + reinvested rental cash flow, then selling at the horizon), reporting the wealth under each path and the keep-vs-sell advantage; `depreciation-recapture` splits a rental sale's gain into the unrecaptured § 1250 portion (the depreciation taken, taxed at a max 25% rate per IRC § 1(h)(1)(E)) and the LTCG portion (appreciation above original basis), capping recapture at the lesser of depreciation or total gain and reporting the tax on each, the effective rate, and the after-tax gain (a sale at a loss recaptures nothing); `like-kind-exchange` models a § 1031 swap — it computes the realized gain, the boot (cash received + net debt relief when trading down), the recognized gain (lesser of realized gain or boot, taxed now), the deferred gain, and the replacement property's carryover basis (cost − deferred gain), flagging a fully-deferred exchange; `cost-of-hire` compares the fully-loaded cost of a W-2 employee (base + employer payroll tax + benefits + retirement match + workers' comp + overhead) against a 1099 contractor, reporting the burden percent over base, each side's total annual cost, and the effective hourly (the employee's PTO reduces productive hours, raising their true hourly); `invoice-factoring` shows the true cost of selling a receivable — the cash advanced (invoice × advance rate), the factor fee, the reserve held back and released net of the fee, the net proceeds, and the annualized effective APR (fee ÷ advance × 365 ÷ term), which turns a flat 3% fee on a 30-day invoice into ~46% APR; `ltv-cac` is customer unit economics — lifetime value (monthly gross profit × lifetime, where lifetime = 100 ÷ monthly churn %) over acquisition cost, reporting the LTV:CAC ratio against the 3:1 rule of thumb (healthy / marginal / unprofitable) and the CAC payback in months; `burn-rate` is the startup cash-runway tool — gross burn (expenses) and net burn (expenses − revenue), with the runway simulated month-by-month so growing revenue extends it (and reaching break-even before depletion means an unlimited runway), plus the months to break-even; `qlac` models a Qualified Longevity Annuity Contract — it caps the premium at the SECURE 2.0 limit ($210,000 for 2025–2026, also bounded by the account balance) and computes the RMD with and without the QLAC (RMD = (balance − premium) ÷ the Uniform Lifetime divisor), reporting the annual RMD reduction from excluding the premium from the RMD base; `spousal-ira` checks non-working-spouse IRA eligibility — each spouse's limit (base + age-50+ catch-up, $7,500/$1,100 for 2026) and whether the couple's combined earned income covers both contributions (total IRA contributions can't exceed earned income), reporting the allowed amount per spouse and the max combined; `pension-survivor` evaluates the single-life vs joint-and-survivor pension election — the monthly/annual reduction that buys survivor protection, the survivor's continued benefit (J&S × survivor %), and the pension-max comparison (take single-life, buy life insurance: if the payment minus premium still beats the J&S amount, pension-max wins); `ss-pia` estimates the Social Security Primary Insurance Amount from AIME via the progressive bend-point formula — 90% of AIME up to the first bend point, 32% to the second, 15% above (2026 bend points $1,286 / $7,749, web-verified and overridable) — returning the per-tier contribution, the monthly/annual PIA, and the replacement rate that shows the formula's progressivity; `hsa-triple-tax` quantifies the HSA's unique deductible-in / tax-free-growth / tax-free-out treatment by projecting an HSA (full pre-tax contribution, tax-free throughout) against a taxable account funded with the same gross income (after-tax contribution, gains taxed at the long-term rate at the horizon), reporting each ending value, the upfront deduction savings, and the dollar advantage; `age-allocation` applies the rule-of-N heuristic (equity % = N − age, clamped 0–100, with N = 100/110/120) to give the stock/bond split, the dollar allocation, and a glidepath at 10-year steps showing equity declining as you age; `roth-bracket-fill` sizes a Roth conversion that tops off a tax bracket without spilling into the next — the headroom (bracket ceiling − current taxable income, capped at the traditional balance), the tax it triggers at the bracket's marginal rate, and the resulting taxable income; `mortgage-points` decides whether to buy discount points — it derives the bought-down rate (base − points × reduction-per-point), the points cost (points% × loan), the amortized payment with and without points, the monthly savings, the break-even in months (cost ÷ savings), and the lifetime net savings over the full term; `apr-apy` converts between a nominal APR and the effective APY at any compounding frequency (APY = (1 + APR/n)^n − 1, and the inverse), reporting both rates, the compounding boost between them, and the continuous-compounding ceiling (e^APR − 1); `blended-debt` computes the balance-weighted average APR across a list of debts (Σ balance×APR ÷ Σ balance), the total monthly interest, and a consolidation comparison — flagging whether a single consolidation-loan rate beats the blend and the monthly interest it would save; `dividend-coverage` tests whether a stock's dividend is sustainable — the payout ratio (DPS ÷ EPS), earnings coverage (EPS ÷ DPS), retention, and an optional FCF payout (DPS ÷ free cash flow), with a health rating that flags a payout over 100% (dividend exceeds earnings) as unsustainable; `spia` prices a single-premium immediate annuity — the guaranteed monthly income that exhausts the premium over the payout period at the insurer's assumed rate (the present-value annuity payment), plus the payout rate (annual income ÷ premium), the nominal total received, and the interest credited (period-certain approximation); `debt-yield` is the commercial-RE lender-sizing tool — it computes the debt yield (NOI ÷ loan), LTV (loan ÷ value), and LTC (loan ÷ cost) for a proposed loan, the max loan each ceiling allows (NOI ÷ min-debt-yield, value × max-LTV, cost × max-LTC), the binding constraint, and whether the proposed loan fits; `price-to-rent` is the buy-vs-rent market screen — home price ÷ annual rent with the conventional verdict (<15 favors buying, 15–20 borderline, >20 favors renting) and the gross rental yield (the inverse); `years-to-fi` computes the financial-independence timeline — the FI number (annual expenses ÷ safe withdrawal rate) and the years for current savings plus the annual income-minus-expenses surplus to reach it at an expected return (year-by-year), surfacing the savings rate as the dominant lever; `grm` is the gross-rent-multiplier valuation screen — price ÷ gross annual rent, then nets vacancy/credit loss and adds other income into effective gross income to give an effective GRM on the income actually collected; `seller-financing` models a carryback note where the seller is the bank — the note amount (price − down), the amortized monthly payment, the balloon balance owed at the balloon date (or full amortization), and the seller's interest income over the note's life; `expense-drag` shows the long-run cost of a fund's expense ratio — it grows the same initial + annual contributions at the gross return vs the net return (gross − expense ratio) and reports the dollars and percent lost to fees, far larger than the headline rate because the fee dollars never compound; `lease-payment` breaks down a car-lease payment — the depreciation fee ((cap cost − residual) ÷ term) plus the finance fee ((cap cost + residual) × money factor) plus tax, with the equivalent APR (money factor × 2400) and the total lease cost; `real-return` applies the Fisher equation — exact real return (1+nominal)/(1+inflation)−1 vs the nominal−inflation shortcut, the after-tax real return, and the principal's purchasing power (real future value) over a horizon; `cd-penalty` computes the cost of breaking a CD early — interest earned (principal × rate × months held) minus the penalty (the same rate over the penalty months), the net proceeds, the annualized yield actually realized, and a flag when the penalty exceeds the interest and dips into principal; `yield-on-cost` is the dividend-growth metric — the dividend against your cost basis (YOC) vs current yield (against today's price), a projected YOC after N years at the dividend growth rate, and the dividend's doubling time (ln 2 / ln(1+growth)); `trade-expectancy` measures a strategy's edge — expectancy = (win% × avg win) − (loss% × avg loss), the reward:risk ratio, the break-even win rate (avg loss ÷ (avg win + avg loss)), the expectancy in R, and the expected profit over 100 trades, flagging whether the system has a positive edge; `wage-converter` converts an hourly rate to an annual salary or back (annual = hourly × hours/week × weeks/year, and the inverse) and breaks it out per hour, week, two weeks, month, and year, with hours/week and weeks/year set so dropping weeks accounts for unpaid time off; `sales-tax` adds sales tax to a pre-tax price or backs it out of a tax-inclusive total across a stack of jurisdiction rates (state + county + city, or a single VAT rate) — it reports the net, the total tax, the gross, and a per-jurisdiction breakdown apportioned on the net base so the parts reconcile to the total in both directions; `accrued-interest` computes the interest a bond has accrued since its last coupon and the dirty (full) price the buyer pays — coupon_payment × (days accrued ÷ days in period) under either a 30/360 (corporate/muni) or Actual/Actual (Treasury) day count, with the clean price plus the accrual giving the dirty price; `stock-split` scales a holding by a new:old ratio (4:1 forward, 1:10 reverse) — adjusted shares (× factor), price and cost basis per share (÷ factor), the whole/fractional split with the cash paid in lieu of the fraction, and confirmation that total value and total cost are unchanged; `tbill-yield` converts a Treasury bill's price or quoted bank-discount rate into the yields the market uses — the bank-discount rate (360-day, on face), the money-market / CD-equivalent yield (360-day, on price), the coupon-equivalent investment rate (the 31 CFR 356 Appendix B method: the simple `(100−P)/P × y/r` for bills ≤ half a year, the semiannual-compounding quadratic for longer bills), and the effective annual yield, plus the dollar purchase price and discount; `dscr` is the rental/commercial loan-sizing ratio — net operating income ÷ annual debt service (P&I amortized from the loan terms) against a target the lender sets (commonly 1.20–1.25) — reporting the DSCR, whether it clears the target, the annual cash-flow cushion, the largest debt service and loan that still clear the target (present-valuing the capped payment stream), and the fraction of NOI that can erode before the ratio hits the target; distinct from `debt-yield` (NOI ÷ loan); `graham-number` runs Benjamin Graham's defensive-investor value screens — the Graham number √(22.5 × EPS × BVPS) as the maximum justified price (the 22.5 is a 15× P/E × 1.5× P/B cap, defined only when both are positive), the margin of safety below it, the P/E × P/B ≤ 22.5 combined-multiple test, and the net-net screen (net current asset value per share with the classic two-thirds-of-NCAV buy threshold); `take-home-paycheck` turns a gross paycheck into net per period and per year — FICA is exact (Social Security 6.2% to the 2026 $184,500 wage base, Medicare 1.45% on all wages plus the 0.9% surtax above the filing threshold), while federal and state income tax are effective rates on taxable wages (which depend on the W-4, so they aren't bracket-modeled here — that's `income-tax-estimator`); pre-tax deductions cut the income-tax base but not FICA (401(k) deferrals are still payroll-taxed), post-tax deductions cut net only, and it reports the full annual breakdown plus the effective tax rate and take-home percent; `ev-ebitda` builds enterprise value (market cap + total debt + preferred + minority interest − cash) and the capital-structure-neutral EV/EBITDA multiple (undefined when EBITDA is not positive), plus net debt, and — when revenue is supplied — EV/Sales and the EBITDA margin; `holding-period-return` is the total return on a position split into the price change and the income (dividends/interest) collected, then geometrically annualized over the actual days held — `(1 + total return)^(365/days) − 1` — so a 25% gain annualizes to 25% over a year, ~11.8% over two years, or ~56% over six months; reports the price/income/total returns, the annualized rate (undefined for a >100% loss or a zero holding period), and the dollar P&L when shares are given; distinct from `cagr` (begin/end only, no income); `altman-z-score` is Edward Altman's bankruptcy-distress model for public manufacturers — `Z = 1.2·(WC/TA) + 1.4·(RE/TA) + 3.3·(EBIT/TA) + 0.6·(equity/liabilities) + 1.0·(sales/TA)` — reporting the five component ratios, the weighted Z-Score, and the zone (safe > 2.99, grey 1.81–2.99, distress < 1.81); `piotroski-f-score` runs Joseph Piotroski's 9-point financial-strength test across two years — profitability (positive ROA, positive operating cash flow, rising ROA, cash flow exceeding net income), leverage/liquidity (falling long-term-debt ratio, rising current ratio, no share dilution), and efficiency (rising gross margin, rising asset turnover) — scoring 0–9 with a strong (≥7) / moderate (4–6) / weak (≤3) rating; `gmroi` is the retail gross-margin return on inventory investment — gross margin ÷ average inventory at cost (equivalently margin-on-cost × inventory turns), with the inventory turnover, days of inventory, and gross-margin percent; a GMROI above 1.0 means each inventory dollar returns more than a dollar of margin; `roth-contribution` computes the Roth IRA contribution allowed after the modified-AGI phase-out — full below the threshold, zero above it, and in between `limit × (1 − (MAGI − start)/(end − start))` per IRS Pub 590-A (rounded up to the next $10 with a $200 floor) — using 2026 defaults ($7,500 base + $1,100 catch-up at 50+; $153k–$168k single, $242k–$252k married-joint, $0–$10k married-separate), with the limit and catch-up overridable for other years; `interest-coverage` measures corporate solvency from the income statement — times interest earned (EBIT ÷ interest), EBITDA interest coverage ((EBIT + D&A) ÷ interest), and fixed-charge coverage ((EBIT + lease) ÷ (interest + lease)) — flagging whether EBIT covers interest at least once; distinct from `dscr`, which covers property debt service including principal; `capital-gains-tax` computes the federal tax on a sale — long-term gains stack on top of ordinary taxable income and are taxed in slices across the 2026 0% / 15% / 20% brackets (single 0% ≤ $49,450 / 20% ≥ $545,500, married-joint $98,900 / $613,700, overridable), short-term gains are taxed at the ordinary rate supplied — reporting the per-bracket amounts, tax owed, effective rate, and after-tax gain; distinct from `income-tax-estimator` (ordinary brackets) and `ltcg-harvesting` (0%-bracket harvesting); `traditional-ira-deduction` computes how much of a traditional IRA contribution is deductible after the MAGI phase-out — fully deductible when neither spouse is covered by a workplace plan, otherwise phased out over the 2026 ranges (covered single $81k–$91k, covered married-joint $129k–$149k, spouse-covered-but-you-aren't $242k–$252k, married-separate $0–$10k) using the same IRS round-up/$200-floor proration, and reports the nondeductible remainder you can still contribute; the deductibility companion to `roth-contribution`; `rule-of-40` is the SaaS/growth-company health check — revenue growth % + profit margin % against a 40 bar — reporting the score, pass/fail, the surplus or gap, the margin you'd need at the current growth (and the growth needed at the current margin) to clear it, and each lever's share of the score; `wacc` is the weighted average cost of capital — `(E/V)·Re + (D/V)·Rd·(1 − tax)` — reporting the equity/debt weights from market values, the after-tax cost of debt, and the blended rate, with the cost of equity either supplied or derived from CAPM (`risk-free + beta·(market − risk-free)`); the standard DCF discount rate; `dupont-roe` is the five-step DuPont decomposition of return on equity — tax burden (NI/EBT) × interest burden (EBT/EBIT) × operating margin (EBIT/revenue) × asset turnover (revenue/assets) × equity multiplier (assets/equity), which multiply to ROE (NI/equity) — with the first three collapsing to net profit margin, so you can see whether ROE comes from profitability, efficiency, or leverage; `ss-taxation` computes how much of a year's Social Security is taxable per IRS Pub 915 — provisional income (other income + tax-exempt interest + half the benefits) against the statutory thresholds ($25k/$34k single, $32k/$44k married-joint, never inflation-indexed) drives the 0% / up-to-50% / up-to-85% tier, with the taxable amount the lesser of the 85% cap and the worksheet's tiered formula; distinct from `ss-pia` (the benefit) and `social-security-age` (claiming age); `npv-irr` is capital-budgeting analysis for a uniform-period cash-flow series — NPV at a chosen discount rate, the IRR (bisection-solved rate that zeroes NPV), the profitability index (PV of future flows ÷ initial outlay), and both simple and discounted payback (the period cumulative cash flow turns positive, linearly interpolated) — distinct from `position-irr` (date-based XIRR for trading positions); `leverage` measures the degree of operating, financial, and combined leverage — DOL (contribution margin ÷ EBIT) amplifies a sales change into EBIT, DFL (EBIT ÷ pre-tax income) amplifies it again into EPS, and DCL is their product (contribution margin ÷ pre-tax income), so a DCL of 3 means a 1% sales change swings EPS 3%; `two-asset-portfolio` is the Markowitz closed form for a two-asset mix — expected return, portfolio volatility `√(wa²σa² + wb²σb² + 2·wa·wb·σa·σb·ρ)`, the weighted-average volatility, the diversification benefit (how far correlation below 1 pulls vol under that average), and the Sharpe ratio; at ρ = 1 there's no benefit, at ρ = −1 vol is minimized; `mortgage-recast` re-amortizes a loan after a lump-sum principal payment — same rate and remaining term, so the payment drops (unlike extra payments, which shorten the term, or a refinance, which resets rate/term) — reporting the old/new payment, monthly savings, old/new total interest, interest saved, and net interest saved after the lender's recast fee; `tax-equivalent-yield` compares a tax-free muni to a taxable bond after tax — muni interest escapes federal tax and the 3.8% NIIT (and state tax when in-state), so it reports the combined taxable rate, the tax-equivalent yield (`muni after-tax ÷ (1 − combined rate)`), each bond's after-tax yield, and which wins; `pmi-removal` finds when private mortgage insurance drops off — solving the amortization for the month the balance reaches 80% LTV (borrower can request) and 78% (automatic termination under the Homeowners Protection Act) of the original home value, with the monthly payment, original LTV, and balance thresholds; `free-cash-flow` computes FCF (operating cash flow − capex) and the ratios off it — FCF margin (÷ revenue), FCF yield (÷ market cap), and FCF-to-net-income as a cash-conversion quality check (above 1 means earnings are backed by cash); `credit-card-payoff` simulates the minimum-payment trap — paying only the declining minimum (`max(floor, %×balance)`) stretches a small balance into decades of interest, and it can never amortize when the minimum falls below the monthly interest (flagged); it compares that to a fixed monthly payment and reports the months, total interest, and interest saved; `bond-pricing` prices a coupon bond from its yield to maturity — `coupon·(1−(1+y)^−n)/y + face·(1+y)^−n` — classifying it premium/discount/par, with the annual coupon, current yield, and the capital gain pulled in to par at maturity; the inverse of `yield-to-call` (which solves yield from price); `cash-out-refinance` sizes a homeowner's equity pull — the max new loan at the lender's LTV cap, the gross and net cash out (after paying off the old balance and closing costs), the new monthly payment, current and post-refi LTV, and remaining equity; the consumer counterpart to `brrrr`'s investment-deal cash-out; `margin-analysis` is the income-statement margin waterfall — gross profit (revenue − COGS), operating income (− opex), pre-tax (− interest), and net income (− tax on positive pre-tax) — with the margin as a percent of revenue at each level; distinct from `break-even` (CVP), `dupont-roe` (ROE), and `leverage` (DOL); `bonus-grossup` is the net-to-gross payroll calc — the gross payment needed so the recipient nets a target after tax (`net ÷ (1 − combined rate)`), using the 22% federal supplemental flat rate plus state and the 7.65% FICA, with the per-tax withholding breakdown; the reverse of `take-home-paycheck`; `rent-escalation` is the true cost of a lease with annual rent bumps and free-rent concessions — rent steps up each anniversary and the first months can be waived — reporting the total nominal rent, its NPV at a discount rate, the concession value, the fully-escalated final month, and the effective monthly rent (total spread over the full term) that tenants compare offers on; `loan-apr` computes the true APR on an amortizing loan once upfront fees are folded in — the payment is set by the note rate on the full loan but the borrower only nets the loan less fees, so APR (the rate equating the net proceeds to the payment stream, bisection-solved) sits above the note rate; reports the payment, net proceeds, APR premium, and total interest; `home-sale-exclusion` computes the capital gain on selling a primary residence net of the IRC §121 exclusion ($250k single / $500k married-joint) — amount realized less adjusted basis (purchase + improvements − depreciation), with the depreciation portion treated as unrecaptured §1250 (taxed at 25%, not excludable) and the remaining gain excluded up to the limit then taxed at the LTCG rate; reports the excluded gain, recapture, taxable LTCG, tax, and after-tax gain; `life-insurance-needs` sizes a policy by the DIME method — income replacement (annual income × years) plus mortgage, other debts, education, and final expenses, less existing coverage and liquid savings — reporting the total need, offsets, and the coverage gap to buy (or surplus if over-insured); `car-affordability` applies the 20/4/10 rule — 20% down, ≤4-year loan, vehicle spending under 10% of gross income — working back from the income cap (less any insurance/fuel) through the loan present value to the max car price and the down payment it needs; `disability-insurance-needs` sizes the monthly benefit to replace lost income if you can't work — replacement % × gross monthly income, less existing employer LTD coverage — reporting the target benefit, the monthly/annual gap to buy, and whether the target covers essential expenses (the monthly counterpart to `life-insurance-needs`); `true-hourly-wage` is the "Your Money or Your Life" real wage — net pay after taxes and job-related expenses divided by all job-related hours (work + commute + prep × weeks) — reporting the nominal hourly, the true hourly, and the erosion between them; distinct from `wage-converter` (gross hourly↔salary) and `cost-of-hire` (the employer side); `property-tax` computes annual and monthly property tax — market value × assessment ratio less exemptions, taxed at the mill rate ($/$1,000 of taxable value) — plus the effective rate against market value; `rental-noi` builds net operating income from the rental income statement — potential gross (rent + other income) less vacancy gives effective gross income, then operating expenses (taxes, insurance, maintenance, a management fee as a percent of EGI, utilities, repairs, HOA, other) are subtracted — reporting EGI, total opex, NOI, and the operating-expense ratio; the input that feeds `real-estate-cap-rate`, `dscr`, and `debt-yield`; `mortgage-affordability` solves the 28/36 rule to a max home price — the tighter of the front-end cap (28% of gross monthly income) and back-end room (36% less other debts) sets the PITI budget, then since property tax and the loan payment both scale with price it solves `price = (max PITI − insurance/12 + down·pf)/(pf + tax rate/12)` — reporting the binding constraint, max loan, and max price; `overtime-pay` computes weekly gross from regular, overtime (1.5×), and double-time (2×) hours — reporting each bucket, the blended effective hourly, the premium over straight time, and the annualized gross; `solar-payback` evaluates a rooftop PV system — net cost after the 30% federal credit and incentives, year-by-year savings that grow with utility-rate inflation and shrink with panel degradation — reporting the payback year (interpolated, `None` if never), lifetime savings, net profit, and ROI; `portfolio-longevity` simulates how long a nest egg lasts under inflation-adjusted withdrawals — each year the balance grows at the return then the (inflation-grown) withdrawal comes out — reporting the full withdrawal years sustained, the withdrawal rate, whether the draw survives the horizon (sustainable), and the final balance; distinct from FIRE (the target), `guyton-klinger` (guardrails), and `sequence-of-returns` (ordering risk); `second-income` answers whether a second earner's income is worth it — taxed at the household's marginal rate, then childcare, commute, and work expenses come out — reporting the after-tax income, total work costs, net benefit, the keep rate (net ÷ gross), and a worth-it verdict; `breakeven-occupancy` is the rental cushion metric — the occupancy at which income covers operating expenses + debt service ((opex + debt service) ÷ gross potential rent) — with the vacancy cushion (100% − breakeven) and the cash flow at an expected occupancy; `rent-affordability` is the renter analog of `mortgage-affordability` — the max rent under the 30% rule (= the landlord 40× annual rule) and a debt-adjusted back-end cap (rent + debts), with the lower of the two as the recommended ceiling and which rule binds; `real-raise` tells whether a pay raise beats inflation — the Fisher real raise `(1+raise)/(1+inflation) − 1`, the inflation-adjusted break-even salary, the real purchasing-power change in dollars, and a real-raise-vs-real-cut verdict; `sde-valuation` values an owner-operated small business by Seller's Discretionary Earnings — net income plus owner compensation, D&A, interest, and discretionary add-backs — times an industry multiple, with the SDE, total add-backs, business value, and SDE margin; the small-business analog of `ev-ebitda`; `freelance-rate` works back from a 1099 contractor's desired take-home through self-employment tax, income tax, business expenses, and self-funded benefits to the revenue needed and the hourly rate to bill over their billable hours — which is why a freelance rate runs far above an employee wage; distinct from `true-hourly-wage` (employee), `cost-of-hire` (employer), and `wage-converter`; `preferred-stock` values a preferred share as a perpetuity — fair value = annual dividend (par × rate) ÷ required yield — with the current yield at a market price and an under/over/fair verdict; distinct from `bond-pricing` (finite maturity) and the growing-dividend DDM; `margin-interest` is the carry cost of a brokerage margin loan — interest accrues daily on a 360-day basis (`borrowed × rate × days ÷ 360`), so it reports the daily, period, and annual interest, the leverage (position ÷ own cash), and the total-position return needed just to cover the carry; distinct from `margin-call` (the call price) and `margin-runway` (time to a call); `qbi-deduction` is the IRC § 199A 20% pass-through deduction (it backs the `qbi-199a` view) — 20% of qualified business income, capped above a taxable-income threshold by the W-2-wage / UBIA limit (`max(50% × W-2, 25% × W-2 + 2.5% × UBIA)`), phased out entirely for a specified service business (SSTB), then capped at 20% of (taxable income − net capital gain); within the threshold-plus-phase-in range the wage limit and SSTB cut apply on a sliding scale, with the 2026 OBBBA $400 minimum for active QBI ≥ $1,000; thresholds and phase-in are overridable (2026 defaults: $201,750 single / $403,500 MFJ, ±$75k/$150k); `estate-tax` is the federal estate tax — gross estate less deductible debts, the unlimited marital deduction, and charitable bequests gives the taxable estate, lifetime gifts are added back to the base, and the amount above the unified exclusion (plus any DSUE ported from a predeceased spouse) is taxed at the top rate (2026 OBBBA defaults: $15M per person, $30M for a couple via portability, 40% — both overridable); reports the taxable estate, base, exclusion used/remaining, amount taxed, tax, net to heirs, and the effective rate, with the flat-top-rate model exact for any taxable estate since the multi-million exclusion fully shelters the graduated 18–40% brackets; `marriage-penalty` runs each spouse's income through the single brackets (with the single standard deduction) and the combined income through the MFJ brackets, then reports the difference — a positive marriage penalty (joint costs more) or negative bonus (joint costs less), with each side's tax, the combined effective rates, and the penalty as a share of income; on the web-verified 2026 schedule the MFJ brackets are exactly 2× single through the 32% rate, so a penalty appears only at very high combined income (37% bracket: $768,700 MFJ vs 2 × $640,600 single) while unequal incomes produce a bonus; standard deductions are overridable; `standard-vs-itemized` decides whether to itemize on Schedule A — it sums SALT (state/local + property tax, capped at the web-verified 2026 OBBBA $40,400 limit that phases down 30% of AGI over $505,000 to a $10,000 floor), home mortgage interest, charitable gifts, and medical expenses above the 7.5%-of-AGI floor, then takes the larger of that total and the standard deduction; reports the SALT cap after phase-out, SALT lost over the cap, the medical floor, the itemized total, and the signed itemizing advantage (2026 defaults $16,100 single / $32,200 MFJ, all overridable); `capture-ratio` is the Morningstar up/down capture analysis from paired strategy/benchmark return series — periods are split by the benchmark's sign, each side geometrically compounded, and the capture is the ratio (up capture above 100 = beats the benchmark in rising markets; down capture below 100 = loses less when it falls); reports the up/down period counts, each side's compounded return, up and down capture, and the capture ratio (up ÷ down, above 1 = the favorable asymmetry), distinct from the single-number Sharpe/Sortino metrics; `rental-total-return` decomposes a leveraged rental's first-year return into the four wealth-building sources — cash flow (NOI less the amortized debt service), appreciation (price × rate), loan paydown (the first-year principal from a 12-month amortization), and the depreciation tax shield (27.5-year straight-line on the building × marginal rate) — and expresses each plus the total as a percent of cash invested (down payment + closing costs); the total runs well above the cap rate because leverage, appreciation, amortization, and the deduction all compound on a small outlay, distinct from `real-estate-cap-rate` (unlevered NOI yield); `economic-value-added` is the Stern Stewart EVA — economic profit above the cost of capital: NOPAT (EBIT × (1 − tax rate)) minus the capital charge (invested capital × WACC), equivalently (ROIC − WACC) × invested capital; reports NOPAT, the capital charge, EVA, ROIC, the EVA spread (ROIC − WACC), and the EVA margin, flagging value creation vs destruction (positive EVA means the business returns more than its capital costs even when accounting profit is positive), distinct from `free-cash-flow`, `dupont-roe`, and `wacc`; `mirr` is the modified internal rate of return — it fixes plain IRR's unrealistic assumption that interim flows are reinvested at the IRR by discounting the negative cash flows to today at a finance rate, compounding the positive flows forward to the final period at a reinvestment rate, and taking `(FV of inflows / |PV of outflows|)^(1/n) − 1`; reports the period count, PV of outflows, FV of inflows, net undiscounted, and the single unambiguous MIRR (no multiple-root problem), distinct from `npv-irr`'s IRR; `equivalent-annual-cost` is the level yearly cost with the same present value as owning and running an asset over its life — the correct way to compare assets with unequal lifespans (a cheap machine replaced often vs a durable one): it computes the annuity factor `(1 − (1+r)^−n)/r`, the PV of costs (initial + annual operating × AF − salvage PV), the capital recovery cost ((initial − salvage PV) / AF), and the EAC (PV of costs / AF = annual operating + capital recovery); distinct from `npv-irr` and `mirr`; `multi-product-breakeven` extends cost-volume-profit to a product mix — each product's per-unit contribution margin (price − variable cost) is weighted by its unit sales mix into a weighted-average contribution margin, fixed costs over the WACM is the total break-even volume, and that splits back across products by their mix proportions; reports the WACM, weighted-average price, weighted CM ratio, total break-even units and revenue, and per-product break-even units/revenue, with the answer shifting as the mix shifts — distinct from `break-even` (a single product) |
| Trade analytics (POST)     | 35        | `POST /analytics/{tilt-detector, discipline-score, emotion-tags, overtrading, streaks, losing-streak-probability, winloss-asymmetry, pyramid-rules, cagr-simple, cagr-rolling, profit-factor, sortino, treynor, information-ratio, sharpe-by-window, high-water-mark, drawdown-duration, earnings-move-straddle, earnings-move-iv, pead, gap-analysis, calendar-bias, halt-risk, trade-quality, exit-timing, mae-stop-tuning, bracket-order, probability-of-touch, portfolio-greeks, concentration, sector-exposure, beta, beta-hedge, hedge-ratio, spread-payoff}` |
| Microstructure + regime    | 24        | `POST /microstructure/{order-book-imbalance, order-flow-classify, order-flow-aggregate, liquidity, market-impact, per-symbol-slippage, vwap-slippage, order-staleness}`, `/heatmaps/{intraday, dow-hour}`, `/regime/{equity, news-event}`, `/discipline/{time-in-force, open-type, trade-plan-checklist, stop-loss-backtest, stop-loss-best-of, pyramid-plan}`, `/options/calc/{iv-rank, iv-backtest, oi-change}`, `/clusters/{trade-features, correlation}`, `/setups/by-setup` |
| Discipline + calendar + extras | 29    | `POST /discipline/{daily-loss-limit, drawdown-throttle, goal-tracker, triple-screen, chandelier-stop, vol-stop-close}`, `/options/calc/{margin-naked-short, margin-vertical}`, `/portfolio/{position-aging, position-irr, mtm-reconciliation}`, `/sentiment/calc/put-call-ratio`, `/tax/reconcile-1099b`, `/calc/risk-reward`, `/analytics/{rolling-zscore, strategy-correlation, spread-attribution, pair-trade-signal}`, `/microstructure/twap`, `/charts/atr-cone`, `/bars/alligator`, `/calendar/{is-trading-day, next-trading-day, prior-trading-day, add-trading-days, trading-days-between, earnings-window, earnings-analysis}`, `/filter/symbols` |
| Markets + breadth          | ~8        | `/markets/snapshot`, `/premarket/snapshot`, `/breadth/snapshot`, `/fear-greed`, `/sector-rotation`, `/heatmap`, `/dividends/calendar` — all served from background-refreshed caches, never computed on request. The dividend calendar precomputes the full 90-day Nasdaq feed on a 6h interval and enriches the soonest ~60 names' yields (annualized ÷ price) server-side, so the route just narrows the cached superset to the requested 1–90 day window and the view opens with no per-date fan-out or client-side quote sweep |
| Strategy tools suite       | 74        | `POST /calc/{grid-trading, fixed-ratio, anti-martingale, risk-of-ruin, scale-out, merger-arb, buyback-accretion, cef-discount, adr-premium, sbc-dilution, sum-of-parts, odd-lot-tender, crack-spread, crush-spread, spark-spread, curve-trade, cheapest-to-deliver, rebalance-bands, iv-cone, fund-fees, win-rate-confidence, equity-curve-filter, double-barrier, futures-sizing, impermanent-loss, average-down, leveraged-etf-decay, short-carry, asset-location, alpha-horizon, options-quick-math, lynch-fair-value, taylor-rule, sahm-rule, misery-index, valuation-gauges, variance-risk-premium}`, `POST /sim/dual-momentum`, `POST /options/calc/{heston, heston-calibrate, probability-of-profit, conversion-reversal, seagull, diagonal-spread, implied-dividend, warrant, early-assignment, event-vol, gamma-theta-breakeven}`, `POST /bonds/calc/bootstrap-zero-curve`, `GET /symbols/{sym}/{turn-of-month, day-of-week, santa-rally, vol-cone, deep-value, overnight-split, best-days, drawdown-episodes, opex-week, pre-holiday, ex-div-study, character-sheet}`, `POST /symbols/{sym}/{event-study, vol-rich-cheap}`, `GET /correlation/{regime, pair-sheet}`, `POST /screeners/{seasonality, risk, momentum, mean-reversion}`, `POST /analytics/report-card`, `GET /bars/{sym}/{vix-fix, laguerre-rsi, rs-line}` — all behind the tabbed Strategy Tools view (82 tools, type-to-filter) |
| Backtest + custom indicators | ~19     | `POST /backtest/run`, `POST /backtest/walk-forward`, `POST /custom-indicators/eval/{sym}`, `POST /algo/tournament` (every single-symbol registry strategy, default rules, same bars — ranked by Sharpe/return/profit-factor; zero-trade strategies sink, multi-symbol ones reported as skipped, buy-and-hold baseline included — a row below it is negative alpha regardless of rank; traded rows carry a deploy button that opens the create-strategy form prefilled with the winning kind), `POST /algo/strategies/{id}/walk-forward` (rolling optimize-then-validate over the saved strategy's grid; reports per-window IS/OOS scores + walk-forward efficiency = avg OOS / avg IS — the overfit detector the plain optimizer can't provide; both the optimizer and walk-forward take apply_gates (default ON in the UI) — ONE bt_gates_for mapping shared with the backtest route so the three simulations can't disagree about which gates exist, replayed inside every grid cell and in both IS optimization and OOS validation, because optimizing the ungated system finds params for a system nobody runs), `POST /algo/strategies/{id}/backtest-mc` (resamples the backtest's actual trade PnLs into thousands of orderings — ending-equity/max-DD percentile distributions + P(ruin); the single backtest's drawdown is one draw, budget off the p95; takes apply_gates like the optimizer — resampling UNGATED trades distributes a system nobody runs), `POST /algo/strategies/{id}/backtest-regimes` (buckets every trade by the market regime at its entry bar — trend-up/down, range, chop — per-regime win rate/PnL/avg-R/PF with an explicit unclassified bucket; answers WHEN the strategy earns; takes apply_gates so the bucketed trades are the gated system's trades, `GET /algo/strategies/{id}/live-vs-backtest` (live fills reconstructed FIFO into closed round trips, win rate tested against the latest persisted backtest via binomial z — verdicts degraded/watch/healthy/outperforming, refused under 10 closed trades; the response also carries the live record's trade-quality stats (expectancy, PF, Kelly, streaks) and the hold-asymmetry flag — TripStats/HoldStats moved to core beside the Trip type, one implementation serving paper and algo; a 12h background sweep runs the same comparison across every active strategy and publishes strategy_drift events to the live feed for degraded/watch verdicts — drift is detected even when nobody clicks; a parallel 12h sweep plans every paper rebalance target and publishes rebalance_drift when max drift crosses the target's own drift_threshold_pct — within-tolerance portfolios stay silent. Both drift watches also fan out to the user's configured webhooks (Slack/Discord/ntfy via the existing delivery machinery) with TRANSITION dedup — notified on entering the bad state, not on every 12h sweep while it persists; recovery clears the state so a relapse re-notifies; the live feed still carries every event. A morning digest (12:00 UTC = pre-market ET year-round) composes everything the watchers know — paper equity day-change, drifting strategies, 24h gate-fire count, held positions reporting earnings within 3 days, rebalance targets above tolerance, margin proximity (accounts whose marked equity sits inside 1.25× the maintenance requirement — the SAME margin_calls sweep the hourly watcher runs at 1.0, parameterized by headroom, so the warning and the call can't disagree about the math; required is always the true 25% figure, never the scaled trigger), realized-correlation warnings (account pairs whose actual daily equity returns correlate above 0.7 — the SAME matrix the holdings panel renders, so digest and panel can't disagree; 'momo↔value ρ 0.92' is two names for one bet), household exposure (net/gross/long/short at current marks — the SAME rollup the holdings view serves, with the unmarked-rows honesty carried into the sentence), PDT proximity (sub-$25k accounts at 3+ day trades in the 5-trading-day window — one trade before the flag, with an already-tripped flag stated plainly; the SAME pdt_status the route serves, so the badge, the digest, and the API can't disagree about the count), early-assignment risk (SHORT option positions in the money within 5 days of expiry, intrinsic from the cached underlying quote — unquotable underlyings skipped, not guessed; equity shorts excluded since assignment doesn't apply), and circuit-breaker proximity (enabled strategies whose current realized drawdown — same realized_drawdown the gate itself uses — is at ≥70% of their max_drawdown_usd cap: the warning BEFORE the auto-pause, while there's still cap left to act inside; breached strategies are paused hence not enabled, so they drop out naturally) — into one live-feed event + webhook per user; empty digests stay silent, and empty sections are omitted from non-empty ones. The delivery hour is per-user (digest_prefs, default 12 UTC, settable in Settings); the scheduler ticks hourly and last_sent_on makes delivery exactly-once-per-day across process restarts — an empty digest still marks delivered so later hours don't re-assemble it. POST /paper-rebalance/execute/{id} submits the plan as paper market orders — RE-PLANNED fresh at execute time (prices move between preview and click), sells first so freed cash funds the buys, failed legs reported per symbol rather than hidden (a rebalance is N independent orders, not an atomic spread). Auto-invest: `/paper/accounts/{id}/recurring` schedules notional buys ('$500 of SPY weekly', fractional shares at notional/price) executed by an hourly background pass — schedules advance from the SCHEDULED time so a late pass never pushes future buys later, monthly clamps short months (Jan 31 → Feb 28), missed periods catch up without scheduling in the past, transient quote failures retry next pass. A recurring buy may target a REBALANCE TARGET instead of one symbol: each run buys the target's most underweight holding (deepest negative drift, untargeted holdings excluded) — cash-flow rebalancing, drift reduced with deposits instead of sells; when everything is at/above target the run records 'skipped: all holdings at or above target' and the schedule still advances). Sizing per strategy: risk-based (risk% of equity ÷ per-share stop distance, capped at max position % of equity) or fixed-notional flat-stake (every entry is N dollars of stock regardless of stop distance — small-account mode; the max position % cap still applies, so a stake above the cap is a config error caught by the cap, not honored — pinned; one shared size_shares serves the live engine AND the backtest, so simulated sizing can't diverge from live). Per-strategy risk gates: max concurrent positions, daily-loss auto-pause, consecutive-losses auto-pause, position-notional cap, and an earnings-blackout window (entries skipped when the symbol reports within N days — forward-looking, exits never blocked), and an entry window (US-Eastern wall-clock HH:MM-HH:MM, e.g. 10:00-15:30 to skip the open and the close; start inclusive, end exclusive, malformed strings disable rather than misfire), and a max-entries-per-day overtrading cap (orders now carry an entry/exit kind tag, backfilled by order-class heuristic), and a post-loss cooldown (revenge-trading brake: N minutes after a losing round trip closes, reconstructed FIFO from the strategy's own fills), and an entry-correlation concentration gate (|rho| of daily returns vs every open position over a configurable lookback — stacking correlated longs is one position with extra commissions, and a perfect hedge is the same trade mirrored; thin-history symbols skip rather than block). Every gate fire is audited to algo_gate_fires and surfaced via `GET /algo/strategies/{id}/pnl-curve` (cumulative realized PnL by trip close time from the shared strategy_trips reconstruction — the strategy's own equity-curve analogue, realized only, with total/peak/trip-count and the SAME current-drawdown figure the circuit breaker evaluates, so the chart and the gate can't disagree; rendered as a uPlot line atop the metrics modal), `GET /algo/strategies/{id}/gate-fires` (per-gate counts over a trailing window + recent rows) — gate tuning from data, not vibes. Fifteenth gate: a per-symbol position cap (max_positions_per_symbol) — the pending-order guard only dedups same-side orders IN FLIGHT, so once a fill lands a re-signal can stack a second trade on the same symbol with no bound; 1 = never add to an existing position, higher = controlled pyramiding, absent = the engine's historical unlimited behavior unchanged. Counts open trade rows for (account, symbol) — the same store the exit pass walks, so what it counts is exactly what can be exited; audited as 'symbol_position_cap', entry-side in both bar paths (the pairs path caps its primary leg), live-only. Fourteenth gate: an equity-curve meta-filter (equity_curve_filter_trips ≥ 2) — entries skipped while the strategy's own cumulative realized PnL sits below its N-trip SMA, the classic trade-the-system-only-when-IT-is-working filter: it can't fix a bad system, it shortens a decaying one's losing streaks at the cost of whipsaw re-entries (and unlike the drawdown latch it is MEANT to whipsaw — a recovery win releases it, pinned). One curve_above_ma decision (start-invariant, warm-up allows, equality counts as above — pinned in core beside the existing whole-series analyzer it extends) serves the live gate over strategy_trips AND the backtest replay with its own skip counter. Thirteenth gate: an account-level exposure cap (max_account_notional_usd) — skip entries while the summed open ENTRY notional across ALL strategies on the account is at/over the cap; per-strategy caps bound one strategy, but five strategies each inside their own cap can still stack the account, and this is the aggregate brake. Entries only, checked AFTER the exit pass so positions can always flatten, and it does NOT trip the kill switch — exposure recedes as positions close, so the gate self-clears, unlike drawdown which needs a human; entry notional (deterministic, no quote dependency — the borrow-fee convention), audited as 'account_exposure', live-only since it's portfolio-dependent like the correlation gate. Time-stop overlay (max_hold_minutes): any position held at least N minutes is force-flattened at the next exit pass with reason 'time_stop' at the last bar's close — it overrides the strategy's own exit opinion in the forced direction only (exits are forced here, never blocked, consistent with the engine-wide exits-never-gated rule); boundary inclusive — a 60-minute cap fires ON minute 60, pinned. The backtest replays it (BtGates.max_hold_minutes): the trade closes with reason time_stop at the CLOSE of the first bar where held-minutes reaches the cap — ranked after intrabar SL/TP (price-path events that happened regardless) but before the strategy signal it overrides; pinned end-to-end on 1-minute bars including the bars-held count, the close-price fill, and the no-cap control riding to end-of-data, and the per-reason exit histogram gains exits_by_time_stop so it still sums to the trade count. Twelfth gate: an entry-days filter (entry_days = "mon,tue,wed,thu" style weekday list — the no-Mondays/no-Fridays discipline rule) judged in US-Eastern wall time because the rule means the SESSION day, not the UTC day (00:30 UTC Saturday is still Friday evening in New York — pinned); a typo'd day list disables the gate rather than silently trading an excluded day, empty and all-seven both mean no restriction, exits never blocked; replayable in backtests via apply_gates with its own skip counter, sharing the exact parse/judgment functions with the live gate. Eleventh gate: a max-drawdown circuit breaker (max_drawdown_usd) — auto-pauses when cumulative realized PnL's CURRENT drawdown from its all-time peak reaches the cap, computed from the strategy's full FIFO trip history via the shared strategy_trips reconstruction (the same one live-vs-backtest divergence uses); 'current' is the operative word — a strategy that recovered to a new peak is not in drawdown regardless of past dips, and the slow multi-day bleed this catches is exactly what the daily loss cap never sees; one shared check serves the single- and multi-symbol paths, 0/absent disables. Tenth gate: a higher-timeframe trend filter (htf_interval + htf_ema_period — longs only above the HTF EMA, shorts only below, the classic multi-timeframe confirmation applied engine-wide; missing HTF history allows + logs rather than banning the symbol). Backtests can replay the time-replayable gates (entry window, entry days, daily cap, loss cooldown, time stop, equity-curve filter, and the max-drawdown circuit breaker — replayed as a LATCH: once cumulative trade PnL falls the cap below its peak, all later entries are blocked for the rest of the run, mirroring the live kill switch that stays tripped until a human re-enables; peak-relative, so +500 then −110 breaches a $100 cap with PnL still positive) via apply_gates so the simulated system matches the gated live one, with per-gate skip counts in the result. Strategies carry a THESIS (notes — why this should work, written before it runs; the algo-side plan note, 📝 on the row with the text on hover): documentation, not config, so revision restores keep the CURRENT thesis rather than reverting it, forks copy the champion's, exports/imports carry it. The thesis surfaces where decisions happen: the drift WEBHOOK carries it ('— thesis: …') so the moment the live record diverges is the moment you reread why you believed, and the metrics modal shows it above the numbers. Every strategy save snapshots the PRIOR config to algo_strategy_revisions (atomic INSERT…SELECT, no race with the update) — `GET /algo/strategies/{id}/revisions` answers 'what changed before performance fell off', pairing with drift detection; any revision restores with one click (the restore goes through the same snapshot-first update, so restores are reversible, and live-mode revisions restore as paper). GET /algo/strategies/{id}/export emits the strategy's portable config (format-tagged traderview-strategy-v1; environment-specific identity deliberately excluded — no ids, watchlist_id, account_id, or broker_mode, because an export describes WHAT the strategy does, not where it runs) and POST /algo/strategies/import recreates it with the same safety posture as fork — DISABLED in internal_sim regardless of what the source ran as, watchlist universes arrive without their watchlist and report no_universe until one is picked (visible, not silent), unknown format tags reject. POST /algo/strategies/{id}/fork creates a champion/challenger copy — disabled, internal_sim regardless of the champion's mode (experiments never inherit live execution) — and the per-fill algo:<name> auto-tagging keeps the pair separately attributable in the journal. Every algo fill auto-tags its journal trade with `algo:<strategy name>` through the existing tags system, so `/reports/by-tag` gives per-strategy P&L attribution with zero schema change (tagging failures never fail the fill pipeline), `POST /algo/portfolio` (2–21 strategy kinds over the same bars — pairwise return-correlation matrix, per-leg stats, equal-weight combined curve, diversification benefit = combined Sharpe − avg individual Sharpe; low-correlation mediocre legs can beat any single strategy; buy-and-hold baseline included), `POST /algo/tournament-matrix` (strategies × symbols over a 2–20 symbol list — per-column best, per-symbol buy-and-hold baseline, rows in stable registry order so they align across columns; thin-data symbols reported as skipped; each per-symbol best cell carries a deploy button into the same prefilled create form) |
| Paper trading              | ~20       | scale (ladder) orders via `POST /paper/accounts/{id}/scale-orders` fan a target size into 2–50 evenly-priced limit rungs across a band (whole-share split for equities, fractional for crypto/FX), each submitted through the normal limit path so the Risk Gate and buying power apply per rung and the ladder simply stops filling when capital runs out — no special-case engine code, and the response reports how many rungs rested vs. were rejected. Every order entry point (manual, bracket, scale, parent) runs the pre-trade Risk Gate against paper-account equity — no ungated path to practice rule-breaking on. Buying power: before this existed, cash could go arbitrarily negative — unlimited free leverage silently invalidating every return number. Per-account margin_multiplier (1 = cash, 2 = Reg-T default, 4 = day-trading) enforces gross entry-basis exposure ≤ m × entry-basis equity with equity > 0, at ONE point — the end of apply_fill — so every fill path (ticket, brackets, spreads, rolls, background fills) is covered and the violating transaction rolls back whole; entry basis not marked value (the borrow-fee convention, stated), shorts handled via the signed book (proceeds raise cash, the liability is the negative book — pinned including the cash-account overbuy, the exact Reg-T boundary, short proceeds, and the blown-account equity≤0 refusal); a resting order the account can no longer afford at fill time CANCELS with the reason instead of wedging the 5s ticker retrying forever. GET /paper/accounts/{id}/pdt is the FINRA pattern-day-trader status: round trips opened AND closed in the same US-Eastern session (00:30 UTC Saturday is still Friday's session — pinned) counted over today + the 4 prior TRADING days (weekends/holidays don't burn window days — pinned against the calendar), flagged at 4+ under $25k marked equity; equity at/above the floor is never flagged regardless of count, and UNKNOWN equity refuses to flag — a false PDT flag is an accusation, the count still reports. Status only: the sim counts and warns (header badge goes red one trade before the flag), it deliberately does not block. The after-the-fill counterpart: an hourly maintenance-margin watcher marks every account at current quotes (same sources as the equity sampler — cached quotes, chain mids × 100 for OCC; a partially-marked book is SKIPPED, not judged, because calling a margin call on stale marks is worse than calling it one pass late) and fires margin_call to the live feed AND the user's webhooks when marked equity falls below 25% of gross marked exposure — shorts count at magnitude as exposure, pinned at the exact boundary both ways; transition dedup like the drift watches, recovery clears, relapse re-fires. Opt-in auto-liquidation (default OFF — a forced sale is destructive and must be chosen): on ENTERING breach, whole positions flatten largest-gross-first at market until maintenance restores, through the NORMAL submit path so friction, audit rows, and fill events all apply, each order plan-noted 'auto-liquidation: margin call'; closing at the mark is equity-INVARIANT (a position becomes cash at the same value) so each close only shrinks gross and the plan provably terminates — pinned: largest-first, stop-when-restored, blown accounts (equity ≤ 0) flatten everything since no subset restores them, healthy accounts plan nothing; a margin_liquidation event names what was flattened. Named multi-account (`/paper/accounts/{create, {id}/rename, {id}/delete}`, 12-account cap, one per strategy; deletes cascade to orders/positions/history; `GET /paper/accounts/comparison` ranks accounts by return vs starting cash with max-drawdown/underwater flags — the strategy leaderboard; GET /paper/holdings is the consolidated household view across every account — per-symbol net qty with the per-account legs, qty-weighted average entry that REFUSES when legs have mixed signs (averaging a long's basis with a short's is meaningless and the net is a synthetic position nobody entered at any price — pinned, including the net-zero case), shown when more than one account exists; GET /paper/account-correlations is the realized companion to the algo portfolio tool — pairwise Pearson of the accounts' ACTUAL daily equity returns (last snapshot per day, pinned so intraday snapshots don't mix horizons; the same shared correlation::pearson every other surface uses), pairs aligned on common days with sub-10-day overlaps refused as — , |ρ|>0.7 flagged as the same bet wearing different names; rows carry live marks (simple_spot for equities/crypto, chain mid × 100 for OCC), market value, and unrealized P&L computed leg-wise as Σ qty×(mark − avg) — well-defined exactly where the average refuses on mixed-sign legs, pinned with the long-plus-short example; unquotable symbols still list with — marks rather than vanishing; a household exposure line totals net/gross/long/short and unrealized across every account, with unmarked rows COUNTED OUT LOUD and excluded from the sums — pinned — so a partial total can't masquerade as a full one). `POST /paper/accounts`, `GET /paper/accounts/{id}/positions`, `POST /paper/accounts/{id}/orders` (market fills now; well-formed limit/stop REST as pending and fill from the 5s ticker when the quote crosses the trigger; trailing stops ratchet a high/low-water extreme and fire on a $ or % retrace (the `trailing_stop_limit` variant freezes into a LIMIT at the trail level on trigger — `stop_price` carries the limit offset, 0 = fill at the level — so a hard gap past it keeps resting instead of slipping to market, reusing limit_price + stop_triggered with no new columns); MOC/LOC on-close orders rest until the next 16:00 US Eastern close — stamped at submit via the same holiday/DST-aware day_order_expiry the DAY TIF uses — then MOC fills at last unconditionally while LOC fills at limit-or-better and CANCELS otherwise (an on-close order does not survive into the next session; a limit-less LOC cancels rather than filling at an unintended price — all pinned); MOC/LOC ignore time-in-force because a DAY cancel_at lands at exactly 16:00 and the expire sweep runs before the fill pass — it would cancel the order at its own trigger instant; stop-limit orders (in the order-type enum since 0011 but never implemented until now) trigger when the stop crosses, then fill ONLY at limit-or-better — a gap through the stop does not fill at the gapped price, which is the entire point of the type, and the stop→limit transition persists in stop_triggered so a later recovery to the limit fills even across a process restart (re-checking the stop after the gap would be the bug); the ticket's SIZE helper computes qty = (equity × risk%) / |price − stop| — it requires a stop because sizing without one is just exposure; an optional plan note persists with the order (📝 + hover in history) and satisfies the RequirePlanBeforeTrade risk rule, which was previously unsatisfiable at the manual ticket; time in force GTC/DAY/GTD/IOC/FOK — DAY expires at the 16:00 US Eastern close, holiday-aware; IOC/FOK fill at submit if marketable and otherwise cancel instead of resting (the engine fills fully or not at all, so immediate-or-cancel and fill-or-kill collapse to one rule); Crypto spot pairs (BTC-USDT, ETH-USD — strictly BASE-USDT/USD, pinned narrow so equities and OCC can't collide) trade through the SAME engine as everything else: live venue last with a 5s in-process quote cache (the resting-order ticker re-quotes every 5s per order, and hammering the venue per order per tick is rude), a 0.1% taker fee on notional as the entire friction model (no per-share slippage tier, no SEC fee — the price itself is unadjusted), fractional qty native, and the equity sampler + margin-call sweep both mark crypto positions at the same cached spot so the curve, maintenance, and statements all see them — every order type, brackets, trailing stops, margin, statements, and TWR apply unchanged because the engine was already asset-class-generic above the quote layer. Regulatory honesty for the new class: crypto pairs are EXCLUDED from the wash-sale scan (§1091 covers securities; crypto is property under current rules — flagging a BTC rebuy would be a false accusation, and the exclusion is one labeled line to flip if that changes) and from the PDT count (FINRA 4210 covers equities/options — a BTC scalp is not a day trade under the rule); short borrow fees DO apply to crypto shorts (borrowing the asset is real), DAY time-in-force still expires at the 16:00 ET equity close even though crypto trades 24/7 — defined behavior, stated rather than reinvented. Auto-invest and rebalance targets price crypto through the same shared simple_spot seam the ticket uses — '$100 of BTC weekly' DCA, crypto holdings in rebalance targets, and cash-flow rebalancing into the most underweight crypto leg all work; equity quotes still come from the cached market-data path, one helper, two consumers. Bars too: get_bars fills the SAME price store from the venue's candles for crypto pairs (paginated via the live-verified after cursor, oldest-first, 3000-bar hard cap so a runaway range stops instead of looping; 10s candles refused — the venue doesn't serve them), which lights up the whole bar-consuming stack for crypto at once: backtests, the optimizer and walk-forward, Monte Carlo, regime attribution, the HTF trend gate, the correlation gate, and benchmark overlays — none of them changed, they just read bars. OCC option symbols trade through the SAME path as equities — every order type (market/limit/stop/trailing, TIF, brackets) quotes from the chain bid/ask mid at the 100× multiplier with $0.65/contract commission; zero-bid contracts never fill at 0, expired contracts reject at submit and auto-cancel while resting, option positions mark at the chain mid in the equity sampler, and the risk gate sees AssetClass::Option with the real notional; POST /paper/accounts/{id}/spreads fills 2–4-leg spreads ATOMICALLY — every leg quotes before the book is touched, one transaction, any unquotable/expired leg fails the whole submit (legging in is what the single ticket is for) — with signed net premium (credit positive) and per-contract fees returned; verticals from the UI plus a preset builder for straddles, strangles, iron condors (sell = the classic credit structure), 1-2-1 butterflies, plus covered calls (the buy-write: 100×contracts shares + the short call in ONE transaction through its own endpoint, since the spread path is options-only and the structure spans asset classes; both legs quote before the book is touched so it can never half-build, the call credit posts FIRST so the buying-power check sees the premium before the stock debit, equity friction on the shares + per-contract commission on the call, both orders plan-noted 'covered call'; a covered PUT is rejected as a different structure — pinned), plus calendars and diagonals (near expiry sold / far bought on the long structure, far-after-near enforced, C/P selectable; their preview carries an explicit intrinsic-only caveat — the far leg's remaining time value at near expiry is not in the payoff figure, so a long calendar's real value is understated, stated rather than silently wrong) — legs constructed client-side from underlying + expiry + strikes in the exact OCC format the server parser pins (verified against the same fixture contract), strike ordering validated per preset, previewed and filled atomically through the identical spread path as hand-typed legs; condors/ratio spreads also via API ratios; POST /paper/accounts/{id}/roll rolls a held option ATOMICALLY through the same spread path — close the source contract and open another on the same underlying in one fill (out-rolls, up/down-rolls, and diagonals all valid; a different underlying is two trades, not a roll, and rejects), sides derived from the position (long: sell-to-close + buy-to-open; short the mirror — pinned), qty bounded by the holding, net credit/debit returned; since every leg quotes before the book is touched, a roll can never close the old contract and then fail to open the new one; POST /paper/spreads/preview quotes the legs and returns the expiry payoff profile — max profit/loss, breakevens over spot ±30% — through the shared spread_payoff core, priced by the same chain source the fill uses; both preview surfaces (manual legs and the preset builder) now also RENDER the payoff curve as a diagram — the points were always in the response, the UI just ignored them. POST /paper/accounts/{id}/exercise exercises a LONG American option early (shorts are assigned, not exercised — that right belongs to the other side, rejected; expired contracts belong to expiry settlement, rejected; expiry DAY itself still exercises since the American right runs through the close — all pinned): the option closes at $0 in one transaction with the share leg — the premium burn is realized because the option's value arrives as shares at STRIKE (calls buy, puts sell 100/contract, possibly opening a short that buying power judges), total economics exact; OTM exercise is allowed (the holder's right, occasionally rational around dividends) with the intrinsic stated in the order note so history shows what it was worth. The mirror, POST /paper/accounts/{id}/assign, practices ASSIGNMENT on a short option (longs hold the right and are never assigned — pinned; assigned call writers DELIVER at strike, assigned put writers take delivery, the exact mirror of exercise sides — pinned): manual by design, because the sim has no counterparty to decide assignment and inventing one would be fabrication — the digest's assignment-risk warning says when a real one likely would. Expired option positions cash-settle the day AFTER expiry against the underlying's spot — ITM pays intrinsic × 100, OTM expires worthless with the premium burn realized — via an idempotent 6h background pass with a synthetic order row as the audit trail; without it expired positions would zombie and stall the equity sampler. GET /paper/accounts/{id}/option-greeks returns BS greeks per OCC position (chain IV + spot, qty × 100 scaled, shorts negative) with account-level net Δ/Γ/Θ/vega — contracts the chain quotes no IV for are listed but excluded from nets, visibly), `POST /paper/accounts/{id}/brackets` (entry + OCO stop-loss/take-profit legs that activate on entry fill; first to fill cancels the other; entry may be market, limit, stop, or stop-limit — the stop entry is the breakout bracket, a buy stop above resistance with the exits attached, and the price-relationship validation anchors stop entries at the STOP where the position comes alive, not a stop-limit's limit which only bounds slippage past the trigger — pinned across all four entry types; the stop leg may instead be a TRAILING stop (exactly one of stop_loss / trail_value) — the trail ratchets from the ENTRY's own fill price: legs born pending seed the extreme from the fill directly, and held legs get it stamped by the promotion sweep from the parent's filled_price, without which a promoted trailing leg would rest forever since the ratchet has nothing to ratchet from; the trailing variant validates the target on the profit side of the entry hint and the trail through the same order_well_formed as the ticket — pinned both directions plus bad-trail and wrong-entry-side rejects), `POST /paper/accounts/{id}/protect` (attach an OCO stop/target pair to a position ALREADY held — the gap brackets can't cover, since a bracket bundles its own entry while a market buy, recurring-invest fill, or DRIP share has no exits; sides derive from the position's sign — long → sell legs, short → cover legs — and the positions table also carries a one-click FLAT button (full position closed at market, same side derivation) — qty bounded by what's held, the price relationship checked by the same validate_bracket as real brackets with the CURRENT quote as entry hint so a stop above the market on a long rejects at attach instead of instantly filling, and the legs rest as plain pending OCO orders so the first-fill-cancels-sibling machinery applies unchanged; the OCO prompts PREFILL with ATR-scaled suggestions from GET /paper/stop-suggestion — Wilder ATR(14) over daily bars through the shared core indicator and the shared bars store, so equities and crypto suggest alike: stop 2×ATR away, target 3×ATR, a 1.5R structure by construction, side-aware for shorts, refused under 15 bars because an ATR off a week of data is noise wearing units; suggestion failure degrades to the old blank prompts; the order ticket and the bracket ticket carry the same ATR button — the ticket fills its stop side-aware (feeding the SIZE helper a real stop instead of a guess), the bracket fills both legs from the side selector), `POST /paper/orders/{id}/cancel` (resting orders; cancelling an entry cancels its held legs), `GET /export/paper-orders/{account_id}.csv` (the full order history as a downloadable CSV — every order with submitted/filled timestamps, prices, fees, status, reject reasons, and plan notes, RFC-4180 quoting via the shared csv_cell helper; ownership checked against paper_accounts, not the broker table), `POST /paper/orders/{id}/replace` (cancel/replace in one step — amend a resting order's qty/prices in place, keeping the order row and its submitted_at audit trail; None fields keep current values, the amendment is refused if it would strand the order without the price its type needs — through the same order_well_formed check submit uses, one implementation — and the claim UPDATE re-checks status='pending' so a fill racing the replace wins cleanly; held bracket legs are deliberately not replaceable since amending a leg's qty would desync it from its entry), `POST /paper/accounts/{id}/parent-orders` (TWAP equal slices, VWAP U-curve weighted, or POV participation-rate × observed volume delta), `GET /paper/parent-orders`, `POST /paper/parent-orders/{id}/cancel`, `GET /paper/accounts/{id}/equity-history` (background-sampled curve + return/max-drawdown summary + a `?benchmark=` overlay (user-selectable in the panel — SPY default, QQQ, sector ETFs, or a crypto pair like BTC-USDT, since the overlay rides the same bars store the backtests do) normalized to the first snapshot's equity — aligned bar-at-or-before each snapshot, leading gaps None rather than borrowing a future price — with the account-vs-benchmark alpha stated; the summary's headline return is flow-aware TIME-WEIGHTED (segments split at each cash flow, the flow stripped from its segment's numerator, then compounded — pinned including the flow-at-a-snapshot boundary owning the interval it ends, out-of-window flows ignored, and a withdrawal reading +5% not −15%), with the naive incl.-flows figure shown alongside when they differ, and the vs-benchmark alpha computed from the SAME flow-aware figure (a deposit must not read as outperformance); the account-comparison leaderboard likewise ranks by lifetime modified Dietz over net deposits, so a funded account doesn't outrank an unfunded one on deposits; an underwater chart under the curve draws drawdown-from-peak % over time — the time dimension of the summary's single max-DD figure, computed on the RAW equity series with the same convention summarize() uses so chart and summary can never disagree, which also means a deposit shrinks the drawdown there exactly as it does in the summary while the TWR headline stays the flow-honest number), `GET /paper/accounts/{id}/attribution` (per-symbol realized decomposition — closed-trip trading P&L reconstructed FIFO from the fill ledger since paper_positions deletes closed rows, plus dividends, minus fees; options dollar-true via pre-scaled prices so commissions stay unscaled; dividend-only symbols still listed; unrealized deliberately excluded; plus a by-class realized split (equity / options / crypto — one shared asset_class_of classifier, shown only when more than one class has activity since a single-class split is noise; dividend-only symbols count toward their class), plus a chronological by-month rollup — trips bucketed by close time, dividends by ex-date, dividend-only months still appear; plus trade-quality stats over the same trips — expectancy/trade, win rate, avg win/loss, profit factor (None with no losses, never infinity), largest win/loss, zero-PnL trips counted as losses since they paid fees for nothing; and a hold-duration discipline check — avg hold of winners vs losers with a behavioral flag when losers are held ≥1.5× longer, the classic cut-winners-ride-losers asymmetry; plus the record-implied Kelly fraction (f* = W − (1−W)/R over the actual avg win/loss, None without both sides — never 'bet everything' off an undefeated sample, half-Kelly stated as the sizing) and win/loss streaks (longest both ways + signed current, chronological by close time); and a planned-vs-unplanned split — trips inherit the OPENING order's plan flag (closing fills' flags ignored, flip remainders take the flip fill's), so plan-before-trade has measurable payoff evidence: expectancy with a written plan vs without, side by side, `GET /paper/accounts/{id}/correlations` (pairwise daily-return ρ of current equity holdings, 90d default lookback — same Pearson/returns math as the algo correlation gate so the view and the gate can't disagree; |ρ|>0.7 pairs flagged worst-first as 'same trade, extra commissions'; options excluded visibly since they correlate via their underlying; sub-20-session overlaps render as — not fake zeros; `GET /paper/accounts/{id}/var` — historical-simulation 1-day VaR/ES at 95/99 of the current equity book: today's signed-notional weights over gross exposure (a hedged book shows REDUCED VaR) applied to the joint trailing return history, dollar figures against gross book value, refused under 60 common sessions because a VaR from a few weeks is noise stated as a number; `GET /paper/accounts/{id}/stress` — the book's own worst OBSERVED 1/5/20-day windows (realized history, no model) plus OLS-beta-scaled benchmark shocks at −5/−10/−20% (the shock benchmark follows the equity panel's selectable benchmark — a crypto account stress-tests against a BTC-USDT break, not an irrelevant SPY one) — 'what HAS this book done' and 'what would a market break do' in one line; same shared book-returns assembly as VaR), `GET /paper/accounts/{id}/dividends` (dividend cash posted by the 6h background pass — share count going into each ex-date reconstructed from the fill ledger, longs credited, shorts debited, 30-day ex-date lookback; per-account DRIP reinvests every positive credit into the paying symbol at market immediately after the credit commits — a failed buy leaves the cash, short-dividend debits never reinvest, and paper_dividends.reinvested records which credits compounded). Cash sweep interest: per-account cash_apy_pct (0..=20, 0 = off, set inline next to the DRIP toggle) accrues idle cash daily at ACT/365 simple through a 6h background pass — exactly-once per day via a last_interest_on claim UPDATE, gap days derived from the paper_interest audit trail (capped at 30: the balance the missed days would have applied to is unknowable after that), credit-only so margin debits never get silently charged an unconfigured borrow rate; GET /paper/accounts/{id}/interest lists the credits. The Cash & funding ledger panel merges the interest ledger with the deposit/withdrawal history into one newest-first view (kind-labeled, APY × days detail per accrual row) — every non-trading cash movement in one place. GET /paper/accounts/{id}/statement?month=YYYY-MM (or a bare YYYY for the tax-year annual statement — the same composition over wider bounds, not separate code; pinned Jan-1-to-Jan-1) composes the brokerage-style statement read-only from the existing stores: opening/closing equity from snapshots (opening = last snapshot BEFORE the month, None for a mid-month account opening rather than a fake zero; period return only when both ends exist), realized P&L as trips CLOSED in the month from the same full-history FIFO reconstruction as attribution (a trip closed this month on lots bought months ago lands here) with a by-class sub-line (equity/options/crypto, same asset_class_of classifier as attribution, omitted when only one class traded since it would restate the total), fill count, fees, dividends by ex-date, and the interest ledger split into sweep credits and borrow fees (debits reported as positive costs); month bounds pinned including the December year-wrap and the exclusive end (leap-February irrelevant by construction). Statements also PUSH: within the first 3 days of each month, last month's per-account summary (return, P&L, trips, fees; silent accounts omitted, a fully-silent month sends nothing) goes to the live feed and the user's webhooks exactly once — the claim INSERT's (user, month) primary key survives restarts and overlapping passes, the same exactly-once shape as the digest. POST /paper/transfers/position is the in-kind sibling — move a position (or part) between accounts at COST BASIS: no cash moves, no PnL realizes, both sides get synthetic bookkeeping fills AT the basis so every FIFO reconstruction stays correct (the source trip closes at exactly its basis — moving a position is not a gain — and the destination opens at the same basis); the destination merge is pinned (empty dest takes the basis, same-sign weights — 100@50 + 50@80 = 150@60, shorts likewise, OPPOSITE signs refuse because netting realizes and realization belongs to trades), and the destination's buying power is checked post-state like any fill — an account that can't carry the exposure can't receive it. POST /paper/transfers moves cash between two of the user's accounts ATOMICALLY — the capital-reallocation op the strategy-per-account layout needs: one transaction, source debit overdraw-guarded by CASH in the same UPDATE, both ledgers record cross-referencing flows ('transfer to X' / 'transfer from Y'), and each side's flow-aware returns see it correctly since TWR/Dietz already treat flows as capital, not performance. Accounts fund mid-life via POST /paper/accounts/{id}/cash-flows (deposit positive, withdraw negative — capped by CASH, not equity: money that is currently stock cannot leave, enforced in the same claim UPDATE that checks ownership); the statement carries the period's net deposits and its return is flow-aware modified Dietz (half-weight convention, the stated approximation) — pinned so a 20k deposit with zero trading gain reads 0%, not +20%, a withdrawal is not a loss, no-flow reduces to the simple return, and a degenerate denominator is None rather than a nonsense percentage. The cost mirror: per-account borrow_apy_pct (0..=50) debits equity shorts daily at the same ACT/365 through the same exactly-once pass — charged on ENTRY notional (avg × |qty|), a stated simplification that keeps the pass deterministic with no quote dependency to fail mid-sweep; options shorts are margin, not stock borrow, and are excluded (pinned); audit rows share paper_interest with kind = cash_sweep | short_borrow | margin_interest, debits negative. The third leg: margin_apy_pct (0..=25) charges negative cash daily at the same ACT/365 through the same exactly-once pass — a margin loan and a hard-to-borrow locate are different costs, so the rate is separate from borrow_apy_pct; the statement carries margin loan interest as its own line (it would otherwise land negative inside the sweep-interest bucket), `GET /paper/accounts/{id}/splits` (positions held through a stock split get qty × ratio and avg ÷ ratio from the 6h background pass — value-preserving so the equity curve doesn't record a fake 75% drawdown on a 4:1; accounts with fills on/after the split date are skipped, no clean fix without lot history). `GET /paper/accounts/{id}/wash-sales` — FIFO wash-sale scan over the same fill reconstruction as attribution: a realized loss with a repurchase of the same symbol within ±30 days (IRS §1091) is flagged with the disallowed portion prorated by replacement÷sold; the before-leg counts only window buys STILL HELD after the sale's matching (the sold lots can't self-flag), gain sales never flag, short-side sells are skipped not mis-scored — a flagging tool, not a filing engine: each loss is judged against its own window and only exact-symbol matches are detected (shares→options on the same underlying is substantially identical to the IRS but invisible here). The positions table carries a marked-totals footer (signed total, unrealized, and a class split when more than one class is held — OCC at the 100× multiplier; unquoted rows counted out loud and kept OUT of the sums, the exposure-line convention applied per-account). The order-history table filters by symbol prefix and status client-side (display-toggle over the rendered rows, with a showing-X-of-Y count so hidden rows are stated, not silent). Background fills (resting limit/stop, trailing, bracket legs) publish `paper_fill` events to the live feed — fills that happen while nobody is watching still get seen |
| Alerts + webhooks + hotkeys | ~15      | `GET/POST /alerts`, `GET/POST /strategy-alerts`, `GET/POST /hotkeys`, `GET/POST /webhooks`, `POST /webhooks/{id}/test` |
| Sentiment                  | ~5        | `/sentiment/{feed, ranked, symbol/{sym}, series/{sym}, poll}` |
| Crypto                     | 6         | `/crypto/markets`, `/crypto/global`, `/crypto/btc/chain`, `POST /crypto/calc/funding-arb` (perp cash-and-carry ledger: funding APR, breakeven days vs 4 taker legs, basis edge reported separately — no expiry forces convergence; the live variant POST /crypto/calc/funding-arb-live pulls spot/perp/funding from OKX public endpoints (verified reachable; Binance/Bybit geo-blocked from this deployment), normalizes the venue's variable funding interval to the 8h convention, and returns the snapshot alongside the ledger so the numbers are auditable, plus a realized-funding persistence read over the last ~30 intervals — the share matching the current sign separates a regime (~70%+) from a one-interval spike, which is a trap; history failures degrade to None rather than failing the ledger. The scanner takes a custom comma-list universe (?bases=, validated + deduped + capped at 20 since each base is ~4 venue calls; invalid tokens land in failed BY NAME rather than vanishing; custom lists bypass the 5-minute cache — caching protects the venue on the default sweep, and keying by base list would grow an unbounded map) or defaults to the 12 majors. The scanner carries the same persistence read per row (snapshot + history fetched concurrently per base in one join_all sweep, not a second sequential pass), so a steady 60%-APR carry is distinguishable from an equally-rich one-interval spike at a glance. An hourly background watcher sweeps the same scan and publishes funding_regime to the live feed when a base ENTERS the alert state (|APR| ≥ 15% AND ≥70% same-sign persistence — the regime test, so a one-interval spike never alerts; the 0.7 threshold is the same one the scanner UI colors green, one convention), with the drift watches' transition dedup — alert on entering, recovery clears, relapse re-alerts; live-feed only, deliberately no webhook fan-out since funding is global market data, not per-user. POST /crypto/positioning is the live positioning dashboard — open interest (level + 24h change from the venue's daily series), the OI×price quadrant (new longs / short covering / new shorts / long liquidation, ±0.05% reads flat — pinned), long/short ACCOUNT ratio now vs 24h ago (retail head-count, not notional), 24h taker buy share (aggressor flow, venue tuple is [ts, sellVol, buyVol] — sell first, confirmed against the OKX docs page), and current funding; eight endpoints fetched concurrently, ticker + OI required, every enrichment degrades to None independently so one dark endpoint doesn't kill the read — every endpoint live-verified before any code (and the rubik liquidation-volume endpoint recorded as REFUTED: it 404s, which is why liquidation pressure reads the public liquidation-orders feed instead — recent filled liquidations aggregated long-vs-short at bankruptcy-price × ctVal with the contract spec fetched live, the feed's actual span reported rather than a fixed window assumed; heavy long liquidations read cascade-down, heavy short read squeeze-up). POST /crypto/carry-basis is the term-structure leg of the same trade family: spot vs every dated future on the underlying (expiries parsed from the venue's instId YYMMDD tail — live-verified against BTC-USDT-260626), raw and simple-annualized basis per contract, positive = contango = buy-spot-short-future locked carry; contracts expiring today are skipped and listed because their basis annualizes to noise, not signal. POST /crypto/vol-surface reads the {base}-USD options board (opt-summary live-verified: markVol + BS delta per contract, instIds parsed from the END like the OCC parser since the family prefix varies — pinned against the live-observed id): per expiry, ATM IV (the contract whose |Δ| sits nearest 0.50) and the 25Δ risk reversal (call IV − put IV at the wings — positive = upside bid, negative = downside fear), REFUSED when either wing has no contract within 0.10 of target delta because a skew read off the wrong strikes is worse than no read (pinned); expired and zero-IV contracts dropped. POST /crypto/vrp pairs that surface with the asset's own movement — front ATM implied (nearest expiry ≥ 7 days: sub-week ATM is pin risk and event noise, not a vol level) vs 30-day close-to-close realized annualized at √365 because crypto trades every day, not 252 (the candles endpoint live-verified; the realized-vol pin derives its expected value from the same series rather than asserting a magic constant, plus a 0.01×√365 sanity band); IV/RV > 1 = options rich (the documented seller edge), realized refuses under 10 returns — a week of data stated as an annualized vol is noise wearing units. POST /crypto/book-depth reads the live spot book (top 400 levels, endpoint live-verified): spread in bps, notional depth resting within ±band of mid per side, and the bid-share imbalance (>50% = buyers stacked) — crossed/empty books refuse rather than emitting a fake mid (pinned, including the exact band-filtering and 0.4975 imbalance arithmetic); the footer states the microstructure caveat that resting depth is a snapshot, not a promise — it can be pulled faster than you can hit it; pass impact_notional_usd and the SAME book is walked both ways for a market-impact preview — size-weighted avg fill, worst level touched, slippage in bps vs mid (positive = cost on both sides), and an explicit EXHAUSTED flag when the visible ladder can't absorb the notional, because the avg then describes only what filled and the real order would be worse (pinned on exact two-level arithmetic including the partial-fill case); GET /crypto/funding-scan sweeps a 12-major USDT-perp universe concurrently, ranked by |APR| with the collect-direction stated (negative funding pays longs), failures reported per base, 5-minute cached) |
| Forex                      | 4         | `GET /forex/pairs` (USD majors served from a 60s background-refreshed snapshot — like the world-markets and dividend-calendar caches, so the desk opens instantly and the last good snapshot survives a transient Yahoo failure instead of blanking), `GET /forex/sessions` (24/5 session clock — Sydney/Tokyo/London/NY in fixed-UTC windows, weekend-closed, London/NY overlap flagged), `POST /forex/pip-value`, `POST /forex/position-size` (the lot whose loss at the stop equals the risk budget). FX is first-class: dashless `EURUSD`-form pairs detect via `is_forex_pair`, quote and chart through the same Yahoo seam equities use (the `=X` suffix is appended only at the fetch boundary; caches stay keyed on the canonical pair), and fill in the paper engine with a spread cost model (`spread_pips × pip_size × units`) instead of equity slippage or the crypto taker fee. `pip_size` is one source of truth in `forex_calc`; carry / CIP forwards reuse `/calc/fx-carry`, not duplicated. |
| Tax + analytics            | ~10       | `/tax-lots/{acct}`, `/r-distribution/{acct}`, `/discipline/{acct}`, `/mood-analytics/{acct}`, `/equity-forecast`, `/fill-quality/{acct}` |
| Webull (read-only)         | 2         | `POST /webull/connect` (tokens in memory only), `GET /webull/snapshot` |
| Expenses + OCR             | ~25       | `GET/POST /expense/transactions`, `POST /expense/import`, `POST /expense/receipts`, `POST /expense/receipts/bulk-reocr`, `GET /expense/receipts/search?q=`, `GET /expense/receipts/duplicates`, `GET /expense/receipts/by-merchant`, `GET /tax/merchants?year=`, `GET /expense/report/schedule_c?year=` |
| Tax filing wizard          | ~7        | `GET/PUT /tax-filing/returns/:year`, `POST /tax-filing/returns/:year/autopopulate`, `GET /tax-filing/returns/:year/compute`, `GET /tax-filing/returns/:year/pdf`, `POST /tax-filing/forms/upload` (W-2/1099 OCR), `GET /tax-filing/forms/:year` |
| Community                  | ~12       | `/shares`, `/shared/{slug}`, `/forum/threads`, `/mentorships`, `/boards` |
| Watchlists + filter-sets   | ~10       | `GET/POST /watchlists`, `GET /watchlists/{id}/{symbols,quotes}`, `GET/POST /filter-sets` |
| Custom dashboards          | ~5        | `GET/POST /dashboards`, `GET /dashboards/{id}` |
| Disclosures + earnings + news | ~10    | `GET /disclosures`, `GET /earnings/calendar`, `GET /news/recent`, `GET /news/search` |
| API tokens + import sources | ~7       | `GET/POST /api-tokens`, `PATCH /api-tokens/{id}/rate-limit`, `GET /imports/sources`, `POST /imports` |
| Client error sink          | 1         | `POST /client-errors` (no auth; browser-side error funnel) |

Desktop mode auto-logs in as the local user; the frontend talks to the embedded server on a random localhost port. Web mode requires `POST /api/auth/login` → returns JWT → `Authorization: Bearer …` on subsequent calls. A custom logging middleware (`log_mw.rs`) records every request with elapsed_ms; 4xx/5xx responses get a 4 KB body snippet attached to the log for offline debugging.

---

## [0x06] FRONTEND

`frontend/` is **vanilla JS + uPlot**. Zero npm, zero bundler, zero framework. **765 view modules + pure helper modules + runtime modules**, ~225k LOC JS + 4,898 LOC CSS. All views render into `<main id="app">` via hash-routed dispatch. **Tile launcher (Cmd-K)** is the primary entry point; topbar carries 11 shortcuts and the rest is the launcher. `?` opens the in-app tutorial.

| Category              | Tiles | Notable views                                                       |
|-----------------------|-------|---------------------------------------------------------------------|
| Live Markets          | 6     | Live Scanner (Finnhub WS), Halts, Catalysts, Pre-market, Tape, Heatmap |
| Trading               | 7     | Webull (read-only broker), Live Positions, Paper Trade, New Trade, Plans, Position Size (Kelly + correlation-aware), Hotkeys |
| Journal               | 9     | Journal (per-trade + daily + general), AI Journal, Trade Reviews, Trade Compare, Replay, Tape Replay, Discipline, Mood Analytics, Goals |
| Charts & Research     | 25    | Charts, Research, Watchlists, Screener, Scanners (24 presets), Top Signals, Compare, Pairs, Correlation, Sectors, Sector Rotation, Breadth, Fear/Greed, Sentiment, Dark Pool, Short Interest, Vol, Vol Surface, Options, Earnings Cal, Earnings IV, Disclosures, Economy, News, Crypto |
| Reports               | 11    | Dashboard, Reports (17 cuts), R-Multiple, Equity Forecast, Fill Quality, Risk, Rebalance, Tax Lots, Expenses, Calendar, Accounts Overview |
| Strategy & Automation | 7     | Backtest (stryke-JIT), Backtest Presets, Walk-forward, Custom Indicators, Strategy Alerts, Alerts, Webhooks |
| Community             | 4     | Shares, Community (forum), Mentorship, Boards |
| Admin & Data          | 9     | Import (12 brokers), CSV Wizard, Exports, Accounts, Tags, Search, Settings, Developer (API tokens), **Tutorial** (`?`) |

**Race-safe view dispatch** — `app.js` maintains a per-dispatch token (`currentViewToken()`) bumped on every navigation. Every view captures the token at render start and bails after each `await` if the token is stale, preventing the "`document.getElementById(...)` returns null after navigation" crash that hits naïve SPAs when slow async resolves into a replaced DOM. WebSocket reconnect loops and `setInterval` callbacks are also token-gated so leaving a view tears down its streams.

`js/api.js` wraps `fetch` with the JWT header, error reporting, JSON parsing, and an `ApiError` class. `js/error_reporter.js` funnels `window.onerror`, `unhandledrejection`, and overridden `console.error` to `POST /api/client-errors` (queue-capped at 200). `js/charts.js` owns all uPlot setup — every uPlot chart (dashboard equity/drawdown/volume/win-rate, OHLC, bar panels) ships a shared `zoomPlugin`: wheel-zoom at cursor, drag-pan, dblclick or ⟳ reset, +/− buttons. `js/hud-theme.js` provides the cyberpunk chrome (5 color schemes, CRT scanlines, neon-border pulse). `js/alert_engine.js` polls alerts and fires sound + SpeechSynthesis voice + Notification (all SecurityError-guarded under Tauri's custom scheme).

uPlot is vendored under `frontend/lib/` by `./scripts/vendor-uplot.sh` — pinned, reproducible, no CDN at runtime.

---

## [0x07] INSTALLATION

```sh
git clone https://github.com/MenkeTechnologies/traderview
cd traderview

# Vendor uPlot into frontend/lib/ (one-time, reproducible)
./scripts/vendor-uplot.sh
```

Build prerequisites: Rust stable, `pnpm` or any Node for `tauri-cli` if you want the desktop dev loop, Docker (optional) for the web Postgres.

---

## [0x08] RUNNING — DESKTOP

```sh
# First launch downloads PostgreSQL (~80 MB), cached under ~/.theseus
cargo tauri dev
```

The desktop app:
- Downloads + extracts a portable PostgreSQL on first launch (via `postgresql_embedded`).
- Stores cluster data under `$APP_DATA_DIR/traderview/pg/`.
- Auto-creates a single `local` user with `is_local = true` and auto-logs in.
- Starts the axum router on a random localhost port; the WebView talks to it via `fetch`.
- Shuts the embedded Postgres down cleanly on window close.

Release build:

```sh
cargo tauri build
```

---

## [0x09] RUNNING — WEB

```sh
# 1. Bring up Postgres (or point at an existing one)
docker compose up -d postgres

# 2. Configure
export DATABASE_URL=postgres://traderview:traderview@localhost:5432/traderview
export TRADERVIEW_JWT_SECRET=$(openssl rand -hex 32)

# 3. Run
cargo run -p traderview-web --bin server
```

Open <http://localhost:8080>, register (or log in), import a Webull CSV.

`TRADERVIEW_JWT_SECRET` is required in web mode — `server` refuses to start without it. Rotate the secret to invalidate all outstanding tokens.

---

## [0x0A] IMPORTING TRADES

**Webull** — export `Account Statement → Orders` as CSV. Drop the file into the Import view. The parser:

1. Inserts the raw row into `imports` for audit.
2. Maps each row to an `execution` and inserts under the dedupe key `(account_id, broker_order_id, executed_at, symbol, side, qty, price)`.
3. Re-runs the FIFO roll-up for affected `(account_id, symbol)` pairs and updates `trades`.

Re-importing the same CSV is idempotent — the dedupe constraint silently drops duplicate fills. Importing a *new* statement that overlaps an old one is also safe; only the new fills are inserted.

Other brokers (`ibkr`, `tos`, schwab, fidelity) are scheduled — the importer ships a generic mapping-wizard backend once the Webull baseline is solid.

---

## [0x0AA] BUSINESS EXPENSES (SCHEDULE C)

A separate flow from trade ingestion. Tracks business-deductible spending so the year-end Schedule C is one click away.

### What goes in

| Source            | Formats supported  | Account kind   | Notes                                                  |
|-------------------|--------------------|----------------|--------------------------------------------------------|
| Amazon orders     | CSV, XLSX          | marketplace    | Header-less position schema (23 cols). Total at col 7. |
| Bank of America   | CSV, XLSX          | bank           | Two-section export; parser skips the summary block.    |
| Chase             | CSV                | credit_card    | Header-based; respects Chase's signed `Amount`.        |
| Apple Card        | CSV, XLSX, PDF     | credit_card    | Wallet export (header schema) or born-digital PDF statement; purchases positive, payments negative (negated to expense convention). |
| Generic           | CSV, XLSX, ODS     | any            | Column-mapped importer for bespoke expense sheets (handyman log, truck-expense workbook, rental tab) — caller maps date/amount/description columns; optional header skip + amount negation. Unparseable rows (title/blank) skipped. |

Format detection is automatic — drop in whatever the export tool gave you. ZIP-magic bytes route through `calamine` (xlsx/ods/xls), `%PDF` routes through `lopdf`, everything else is treated as CSV. No format flag needed.

### Categorization

Transactions are tagged with one of 23 IRS Schedule C lines (8 through 27a: Advertising, Car & truck, Supplies, Travel, Meals, Utilities, etc.). The merchant→category mapping is a learned rule table — first time you see `STAPLES.COM` you tag it `office`, every future row from that merchant auto-categorizes. Default seed covers ~70 common US merchants (AWS, Adobe, Uber/Lyft, Chevron/Shell, Starbucks, etc.).

The `meals_50` category has a 0.5 deduction percentage baked in so the year-end report applies the IRS 50% rule without you doing math.

### Transfer dedup

When a credit-card payment shows up in **both** your bank statement (money out) and the credit-card statement (money in), the importer detects the pair by amount + date proximity + account kind and marks both as `is_transfer = true`. Schedule C report excludes them.

### Receipts (image / PDF)

Drop a JPG, PNG, WebP, HEIC, or PDF onto the Expenses tab. OCR runs in the background on a tokio-semaphore-bounded worker pool (≤ `min(4, num_cpus)` in flight at once so a 10k-receipt folder scan can't fork-bomb the machine). PDF receipts use the text-layer fast path via `lopdf`; scanned PDFs prompt you to re-upload as an image.

#### OCR backends — ensemble fusion

Every image runs through **every available backend in parallel**, and the structured outputs are fused field-by-field:

| Backend                       | Platform              | Cost  | Notes                                              |
|-------------------------------|-----------------------|-------|----------------------------------------------------|
| **Apple Vision**              | macOS (when sidecar)  | $0    | Runs on Apple Neural Engine, on-device, no network |
| **Tesseract LSTM `--psm 4`**  | Linux + macOS         | $0    | Single-column variable-size text                   |
| **Tesseract LSTM `--psm 6`**  | Linux + macOS         | $0    | Single uniform block — catches tightly-packed receipts the psm4 model misses |
| **PaddleOCR (DBNet + SVTR)**  | All (cargo `paddle` feature) | $0 | Pure-Rust ONNX via tract; orientation-classifier pre-pass handles rotated/skewed phone photos better than Vision or Tesseract |

Fusion strategy (see `traderview_ocr::combine_results`):

- **Scalar fields** (`merchant`, `total`, `date`, `address`, `tax`, `subtotal`, `time`) — highest-confidence non-`None` wins. A Tesseract hit on a field Vision missed still lands.
- **Items** — union by `(lower(name), line_total)` so duplicates collapse but unique finds from either engine are kept.
- **Confidence** — `max` over all backends (a single high-conf extraction isn't diluted by a weak one).
- **Engine label** — `ensemble:apple_vision+tesseract_psm4+tesseract_psm6`. The receipt-match modal renders it as `Ensemble (3)` with the backend list as a tooltip; bulk re-OCR exposes a `non_ensemble` filter to upgrade old single-engine receipts.

Image preprocessing for the Tesseract path: decode → grayscale → Lanczos3 upscale (shorter side ≥ 1600 px) → **Sauvola adaptive thresholding** via integral images. Sauvola handles uneven lighting, shadows, and faded thermal print on phone-photographed receipts where a global threshold would fail. **No paid APIs, no LLMs — everything is on-device.**

Build the Vision sidecar once on macOS:

```sh
tools/tv-ocr-vision/build.sh        # writes target/release-sidecars/tv-ocr-vision
```

Install Tesseract on Linux / non-macOS hosts:

```sh
# macOS (only needed if the Vision sidecar isn't built)
brew install tesseract

# Linux
apt install tesseract-ocr tesseract-ocr-eng
```

Enable PaddleOCR (optional — adds ~5 min to a cold compile because of `tract-onnx`+`ndarray`):

```sh
cargo build --features ocr-engine -p traderview-web --bin server
cargo tauri dev --features ocr-engine
```

Then drop the five PaddleOCR English-mobile model files (`det.onnx`, `rec.onnx`, `line_ori.onnx`, `doc_ori.onnx`, `dict.txt`) into `$APP_DATA_DIR/traderview/models/paddleocr/`. The ensemble runner picks up the backend automatically once the files are present; until then it stays skipped without breaking the rest of the pipeline.

#### Bulk re-OCR

After installing the Vision sidecar (or upgrading Tesseract), the Receipts page exposes a **Re-OCR** affordance with these filters:

- **non-Vision receipts** — the common case after a Vision install: re-process every receipt that ran on the Tesseract fallback.
- **failed receipts** — retry receipts the engine choked on.
- **low-confidence (<80%)** — re-process receipts whose mean confidence is below the threshold.
- **all receipts** — full re-scan.

Progress is polled at `/api/expense/receipts/bulk-reocr/progress` (server-side aggregate over `ocr_status`).

#### Merchant canonicalization + learned categories

Migration `0043` adds two tables that turn one-off receipt edits into a learning loop:

- `merchant_aliases` — translates noisy OCR strings (`WAL-MART STORE 4892`, `Wal*mart #482`) into a stable canonical name via per-user regex patterns. Top-merchants reports, duplicate detection, and learned-category lookups all group on the canonical.
- `learned_merchant_categories` — UPSERT-incremented every time the user changes a line-item category. The next OCR on the same canonical merchant applies the highest-use_count category as the default for ALL items on the receipt.

The `#categorize` view (More ▾ menu) groups uncategorized items by canonical merchant and lets you batch-apply one category to every receipt at that merchant.

#### Full-text search + duplicate detection

Migration `0044` adds a `ocr_text_tsv` tsvector column (generated stored, `setweight A` on merchant + `B` on body) with a GIN index. The Receipts page exposes:

- **Search box** — `plainto_tsquery` over every receipt's merchant + OCR text, ranked by `ts_rank`, with `ts_headline`-highlighted snippets (`«matched»` markers rendered as `<mark>` after escaping).
- **Find duplicates** — groups receipts where `(canonical_merchant, total ±$0.50, date ±3 days)` overlap. Modal renders side-by-side with a "Keep this one, delete the others" action that refuses to silently delete CSV-attached receipts without an explicit confirm.

After OCR, the receipt is matched against your last week of transactions using amount + date + merchant-token Jaccard scoring. You confirm the best match and the receipt is permanently attached to the transaction row.

### Schedule C report

`GET /api/expense/report/schedule_c?year=2026` (or the UI button) returns per-line totals for the calendar year, applying each category's deduction percentage. The report also surfaces:

- uncategorized business expenses that aren't rolled in yet
- excluded transfers + excluded personal rows (transparency on what was filtered out)

### Tax-filing wizard (`#file-taxes`)

TurboTax / Cash-App-Taxes-style guided interview that walks Form 1040 end-to-end:

| Section      | What it collects                                                                   |
|--------------|-------------------------------------------------------------------------------------|
| Personal     | Filing status (drives bracket thresholds, std deduction, CTC phase-out)             |
| Income       | W-2s · 1099-INT / DIV / NEC · LTCG · interest · dividends · Schedule C · Schedule E |
| Adjustments  | HSA · IRA · student loan interest · half of SE tax (auto)                           |
| Deductions   | Standard vs Itemized **live comparator** (medical / SALT / mortgage / charitable)   |
| Credits      | Child Tax Credit · ODC · EITC (manual entry — Pub 596) · estimated payments         |
| Other Taxes  | SE tax (Schedule SE) — base, SS portion, Medicare, additional Medicare              |
| Review       | Full result summary + QBI manual-review flag when above SSTB threshold              |
| Download     | Multi-page PDF: cover + 1040 + Sch C + Sch SE + Sch E                               |

**Auto-populate**: one click pulls Schedule C net profit from receipts tagged `tax_bucket='business'`, Schedule E net income from `tax_bucket='rental'`, and estimated payments from the `estimated_tax_payments` table.

**W-2 / 1099 OCR**: upload any tax form image and the OCR ensemble (Apple Vision + Tesseract psm4/psm6 + optional PaddleOCR) extracts the boxed values. The `traderview_ocr::tax_forms` parser recognizes W-2 / 1099-NEC / 1099-MISC / 1099-INT / 1099-DIV / 1099-K / 1099-B (proceeds, cost basis, wash-sale loss disallowed) / 1098 (mortgage interest, points) by title, then walks each form's box labels and pulls the dollar amount via same-line-or-next-line proximity. Confidence is reported per upload; below 0.7 the wizard prompts box-by-box review.

**Tax math** lives in a separate `traderview-tax` crate so it's testable in isolation. **31 unit tests pin the 2025 brackets, SE tax (15.3% × 92.35% × $176,100 SS cap), QBI § 199A with SSTB phase-out, and CTC/ODC phase-out** to the exact numbers in **IRS Rev. Proc. 2024-40** and the **SSA 2025 wage-base announcement**. Bracket math is integer-safe (basis points × dollar amount ÷ 10,000) so there's no float drift at boundaries.

**Operational reality (baked into the UX)**: we do NOT e-file. Direct submission to the IRS requires Authorized e-File Provider status + an EFIN, which a self-hosted app cannot obtain. The wizard ends with **"Download Tax Return PDF"** + a link to [IRS Free File Fillable Forms](https://www.irs.gov/e-file-providers/free-file-fillable-forms), where the user transcribes the computed values for free direct e-filing. This is the same end-state as Cash App Taxes for users whose situation isn't supported (multi-state, MFS in community property, Form 1116, etc.).

The wizard surfaces as a dashboard widget (`tax_filing_status`) showing the current year's refund/owed at a glance, plus a nav entry under the More ▾ menu.

---

## [0x0AB] LANDLORD / RENTAL PROPERTY (SCHEDULE E)

Same discipline as the Schedule C surface above, but for **Form 1040 Schedule E Part I** (Rental Real Estate). Migration `0032_rental_properties.sql` adds:

- **`rental_properties`** — one row per unit with `property_type` (IRS codes 1-8: single-family / multi-family / vacation-short-term / commercial / land / royalties / self-rental / other), purchase basis, land value (excluded from depreciation), `placed_in_service_at`, `recovery_period_years` (27.5 residential, 39 commercial), `fair_rental_days` + `personal_use_days` for line 2, QJV (spouse co-owner) and QBI safe-harbor (Rev. Proc. 2019-38, 250 hours/yr) flags.
- **`rental_tenants`** + **`rental_leases`** — tenancy term with `rent_amount` + `rent_frequency` + `rent_due_day` + `grace_days` + `late_fee_fixed` / `late_fee_pct` + `security_deposit` + `pet_deposit` + `deposit_held_by`. Status: `draft` / `active` / `expired` / `terminated_early`.
- **`rental_income`** — every receipt as an atom (`rent` / `late_fee` / `deposit_forfeit` / `reimbursement` / `royalty` / `parking` / `laundry` / `storage` / `other`) with `period_start`/`period_end`, optional `transaction_id` back-link into the bank-statement side. Dedupe index on `(property_id, posted_at, amount, payer_raw, kind)`.
- **`schedule_e_categories`** — 24 stable codes mapping to Schedule E lines 5-19 (Advertising / Auto-Travel / Cleaning-Maint / Commissions / Insurance / Legal-Prof / Mgmt-Fees / Mortgage-Interest / Other-Interest / Repairs / Supplies / Taxes / Utilities / Depreciation), plus 9 line-19 "Other" detail codes (HOA / landscaping / pest control / permits / appliance / property-mgmt software / bank fees / eviction / security).
- **`rental_expenses`** — categorized outflow with `is_capitalized` flag + `capital_useful_life` (improvement vs ordinary repair per Reg. §1.263(a)-3 — capitalized rows are excluded from line 14 and recovered via depreciation instead).
- **`rental_mileage`** — odometer log; `rate_per_mile` is persisted at log time so a 2024 trip stays at $0.67 even after the IRS publishes the 2025 rate. Folds into line 6 (Auto and Travel).
- **`rental_maintenance`** — work orders with `status` (open / in_progress / done / cancelled) + `priority` (low / normal / high / emergency) + back-link to the `rental_expenses` row that paid for it.
- **`rental_services_log`** — 250-hour QBI safe-harbor tracker. Each row is `hours` + `activity` + `performer` (self / employee / contractor). Section 199A allows a 20% deduction on rental income when the property qualifies as a trade or business; the safe harbor requires 250 hours of rental services per year per enterprise.
- **`receipts.rental_expense_id`** — additive column wiring the existing OCR receipt store into the rental side so a contractor invoice PDF can attach to a Schedule E line-14 expense the same way a receipt attaches to a Schedule C transaction.

